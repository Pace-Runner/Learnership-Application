
> frontend@0.0.0 test
> vitest run --coverage


[1m[46m RUN [49m[22m [36mv4.1.3 [39m[90mE:/UNI/Year 3/Learnership-Application/frontend[39m
      [2mCoverage enabled with [22m[33mv8[39m

 [31mΓ¥»[39m src/App.oauth.test.jsx [2m([22m[2m14 tests[22m[2m | [22m[31m4 failed[39m[2m)[22m[32m 72[2mms[22m[39m
[31m     [31m├ù[31m getRoleForEmail function handles admin email assignment[39m[32m 32[2mms[22m[39m
[31m     [31m├ù[31m isProviderProfileComplete checks all required fields[39m[32m 6[2mms[22m[39m
[31m     [31m├ù[31m isApplicantProfileComplete validates all profile fields[39m[32m 11[2mms[22m[39m
     [32mΓ£ô[39m getProviderLandingRoute redirects incomplete profile to profile page[32m 2[2mms[22m[39m
     [32mΓ£ô[39m getLandingRoute handles all three roles[32m 2[2mms[22m[39m
     [32mΓ£ô[39m OAuth email validation handles null/empty emails[32m 1[2mms[22m[39m
     [32mΓ£ô[39m Role selection creates user in database[32m 1[2mms[22m[39m
     [32mΓ£ô[39m ProtectedRoute component redirects unauthenticated users[32m 2[2mms[22m[39m
     [32mΓ£ô[39m ProviderWorkspaceRoute enforces profile completion[32m 1[2mms[22m[39m
     [32mΓ£ô[39m ProviderProfileRoute stays accessible for editing[32m 1[2mms[22m[39m
[31m     [31m├ù[31m Configured admin emails fallback to defaults[39m[32m 5[2mms[22m[39m
     [32mΓ£ô[39m Session persistence uses browser cookies[32m 1[2mms[22m[39m
     [32mΓ£ô[39m Auth error state is displayed to user[32m 1[2mms[22m[39m
     [32mΓ£ô[39m Routes are protected at application level[32m 1[2mms[22m[39m
 [31mΓ¥»[39m src/App.supabase.test.jsx [2m([22m[2m6 tests[22m[2m | [22m[31m6 failed[39m[2m)[22m[32m 30[2mms[22m[39m
[31m     [31m├ù[31m getRoleForEmail returns role when user exists[39m[32m 18[2mms[22m[39m
[31m     [31m├ù[31m getRoleForEmail throws when fetch error present[39m[32m 2[2mms[22m[39m
[31m     [31m├ù[31m getApplicantLandingRouteForEmail returns /profile when no user row[39m[32m 1[2mms[22m[39m
[31m     [31m├ù[31m getApplicantLandingRouteForEmail returns /dashboard when profile complete[39m[32m 1[2mms[22m[39m
[31m     [31m├ù[31m getProviderLandingRouteForEmail returns /provider/profile when no user[39m[32m 1[2mms[22m[39m
[31m     [31m├ù[31m getProviderLandingRouteForEmail returns provider route when profile exists[39m[32m 1[2mms[22m[39m
 [31mΓ¥»[39m src/App.pure.test.jsx [2m([22m[2m5 tests[22m[2m | [22m[31m5 failed[39m[2m)[22m[32m 26[2mms[22m[39m
[31m     [31m├ù[31m getLandingRoute returns correct paths[39m[32m 17[2mms[22m[39m
[31m     [31m├ù[31m isProviderProfileComplete validation[39m[32m 2[2mms[22m[39m
[31m     [31m├ù[31m getProviderLandingRoute returns correct route[39m[32m 1[2mms[22m[39m
[31m     [31m├ù[31m isApplicantProfileComplete validation[39m[32m 1[2mms[22m[39m
[31m     [31m├ù[31m getConfiguredAdminEmails returns default list when unset[39m[32m 1[2mms[22m[39m
 [31mΓ¥»[39m src/App.routes.test.jsx [2m([22m[2m6 tests[22m[2m | [22m[31m6 failed[39m[2m)[22m[32m 114[2mms[22m[39m
[31m     [31m├ù[31m ProtectedRoute shows loading shell when loading and not signed in[39m[32m 57[2mms[22m[39m
[31m     [31m├ù[31m ProtectedRoute redirects unauthenticated users to home[39m[32m 17[2mms[22m[39m
[31m     [31m├ù[31m ProtectedRoute redirects wrong role users to home[39m[32m 6[2mms[22m[39m
[31m     [31m├ù[31m ProtectedRoute allows correct role to render children[39m[32m 10[2mms[22m[39m
[31m     [31m├ù[31m ProviderWorkspaceRoute redirects provider without correct landing route[39m[32m 9[2mms[22m[39m
[31m     [31m├ù[31m ProviderProfileRoute blocks non-provider users[39m[32m 7[2mms[22m[39m
 [31mΓ¥»[39m src/pages/ProviderPage.test.jsx [2m([22m[2m7 tests[22m[2m | [22m[31m1 failed[39m[2m)[22m[33m 943[2mms[22m[39m
   [32mΓ£ô[39m PROVIDER-DASHBOARD: renders provider dashboard heading and welcome message[32m 132[2mms[22m[39m
   [33m[2mΓ£ô[22m[39m PROVIDER-DASHBOARD: renders stats and listing overview [33m 622[2mms[22m[39m
   [32mΓ£ô[39m PROVIDER-DASHBOARD: logout button is clickable and triggers callback[32m 163[2mms[22m[39m
   [32mΓ£ô[39m PROVIDER-ACCEPTANCE-1: Provider model has organisation_name field in schema[32m 1[2mms[22m[39m
   [32mΓ£ô[39m PROVIDER-ACCEPTANCE-2: Google OAuth entry point exists on registration page[32m 1[2mms[22m[39m
   [32mΓ£ô[39m PROVIDER-ACCEPTANCE-3: OAuth callback supports Provider role assignment flow[32m 1[2mms[22m[39m
[31m   [31m├ù[31m PROVIDER-ACCEPTANCE-4: Successful Provider login landing route is /provider[39m[32m 19[2mms[22m[39m
 [31mΓ¥»[39m src/pages/AdminPage.test.jsx [2m([22m[2m8 tests[22m[2m | [22m[31m2 failed[39m[2m)[22m[33m 1228[2mms[22m[39m
   [32mΓ£ô[39m ADMIN-DASHBOARD: renders admin workspace heading and console title[32m 268[2mms[22m[39m
   [33m[2mΓ£ô[22m[39m ADMIN-DASHBOARD: logout button is clickable and triggers callback [33m 671[2mms[22m[39m
   [32mΓ£ô[39m ADMIN-DASHBOARD: non-admin users see access denied[32m 12[2mms[22m[39m
   [32mΓ£ô[39m ADMIN-ACCEPTANCE-1: Admin email addresses are seeded in database with Admin role[32m 2[2mms[22m[39m
[31m   [31m├ù[31m ADMIN-ACCEPTANCE-2: OAuth callback checks email against admin list and assigns Admin role[39m[32m 19[2mms[22m[39m
[31m   [31m├ù[31m ADMIN-ACCEPTANCE-3: Admin dashboard shell exists at /admin route with moderation controls[39m[32m 6[2mms[22m[39m
   [32mΓ£ô[39m ADMIN-ACCEPTANCE-4: Non-admin users visiting /admin are redirected away[32m 1[2mms[22m[39m
   [32mΓ£ô[39m ADMIN-ACCEPTANCE-5: Admin can access and view the platform moderation console[32m 231[2mms[22m[39m
 [31mΓ¥»[39m src/App.test.jsx [2m([22m[2m44 tests[22m[2m | [22m[31m4 failed[39m[2m | [22m[90m12 todo[39m[2m)[22m[33m 2678[2mms[22m[39m
     [32mΓ£ô[39m Provider model includes organisation name field[32m 8[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m Google OAuth entry is available for provider registration [33m 623[2mms[22m[39m
     [32mΓ£ô[39m Provider role assignment is wired in OAuth callback flow[32m 1[2mms[22m[39m
     [32mΓ£ô[39m Provider selection leads to provider profile first[32m 1[2mms[22m[39m
     [32mΓ£ô[39m Provider route is protected by provider workspace gate[32m 1[2mms[22m[39m
     [32mΓ£ô[39m Unauthenticated users can't access /admin[32m 1[2mms[22m[39m
     [32mΓ£ô[39m Wrong role gets redirected away from /admin[32m 1[2mms[22m[39m
     [2m[90mΓûí[39m[22m Admin email gets the Admin role assigned
     [2m[90mΓûí[39m[22m Non-admin email never gets the Admin role
[31m     [31m├ù[31m Admin lands on /admin after login[39m[32m 28[2mms[22m[39m
     [32mΓ£ô[39m Admin can see the dashboard[32m 2[2mms[22m[39m
     [2m[90mΓûí[39m[22m Admin session survives a page refresh
     [2m[90mΓûí[39m[22m Logout from admin clears the session
     [2m[90mΓûí[39m[22m Admin moderation UI has the right controls
     [2m[90mΓûí[39m[22m Example admin emails are in the database
[31m     [31m├ù[31m Applicant role redirects to /dashboard[39m[32m 9[2mms[22m[39m
     [32mΓ£ô[39m Provider role redirects to profile setup first[32m 2[2mms[22m[39m
[31m     [31m├ù[31m Admin role redirects to /admin[39m[32m 7[2mms[22m[39m
     [32mΓ£ô[39m Unauthenticated users blocked from protected routes[32m 1[2mms[22m[39m
     [2m[90mΓûí[39m[22m Role isolation enforced
     [32mΓ£ô[39m ProtectedRoute component guards all authenticated routes[32m 1[2mms[22m[39m
     [32mΓ£ô[39m Provider workspace gate blocks access until profile is complete[32m 1[2mms[22m[39m
     [32mΓ£ô[39m Provider profile route stays open for editing[32m 2[2mms[22m[39m
     [32mΓ£ô[39m Redirect logic implemented for unauthorized access[32m 1[2mms[22m[39m
     [2m[90mΓûí[39m[22m Auth loading state displays during verification
[31m     [31m├ù[31m getLandingRoute function routes each role correctly[39m[32m 6[2mms[22m[39m
     [2m[90mΓûí[39m[22m Protected route middleware prevents cross-role navigation
     [32mΓ£ô[39m All role routes mounted in App.jsx[32m 1[2mms[22m[39m
     [32mΓ£ô[39m Provider route mounts Provider component[32m 1[2mms[22m[39m
     [32mΓ£ô[39m Admin route mounts Admin dashboard[32m 1[2mms[22m[39m
     [32mΓ£ô[39m Dashboard route mounts Applicant dashboard[32m 1[2mms[22m[39m
     [32mΓ£ô[39m Applicant listing detail route is mounted for applicants[32m 3[2mms[22m[39m
     [32mΓ£ô[39m Each route requires correct role in ProtectedRoute[32m 1[2mms[22m[39m
     [32mΓ£ô[39m Vitest + v8 coverage configured[32m 1[2mms[22m[39m
     [32mΓ£ô[39m Coverage reports generated and integrated[32m 1[2mms[22m[39m
     [2m[90mΓûí[39m[22m Role-sensitive components have high coverage
     [2m[90mΓûí[39m[22m GitHub Actions CI runs test pipeline
     [2m[90mΓûí[39m[22m Tests pass in CI environment
     [33m[2mΓ£ô[22m[39m Home page renders all three landing sections with proper hierarchy [33m 541[2mms[22m[39m
     [32mΓ£ô[39m Home page includes scroll animation hooks[32m 266[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m Home page topbar links are visible [33m 540[2mms[22m[39m
     [32mΓ£ô[39m Environment variables for Supabase are configured[32m 1[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m Sign-in page displays auth status message [33m 338[2mms[22m[39m
     [32mΓ£ô[39m Sign-in page is ready for user interaction when Google button is present[32m 276[2mms[22m[39m
 [32mΓ£ô[39m src/pages/UserPages.test.jsx [2m([22m[2m6 tests[22m[2m)[22m[33m 1280[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m Listings section and search controls visible [33m 379[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m Profile page renders with document actions [33m 456[2mms[22m[39m
stderr | src/App.routing.test.jsx > App routing tests > renders home page for unknown routes
No routes matched location "/unknown-route" 

stderr | src/App.routing.test.jsx > App routing tests > renders home page for unknown routes
No routes matched location "/unknown-route" 

 [32mΓ£ô[39m src/App.integration.coverage.test.jsx [2m([22m[2m9 tests[22m[2m | [22m[33m1 skipped[39m[2m)[22m[33m 3793[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m shows a config error when Supabase is missing [33m 1506[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m shows a restore-session error when getSession fails [33m 520[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m renders the provider workspace routes for a signed-in provider [33m 306[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m shows a role-selection overlay for a new user and reports insert failures [33m 620[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m surfaces Google sign-in failures [33m 530[2mms[22m[39m
 [32mΓ£ô[39m src/pages/AdminModerationQueue.test.jsx [2m([22m[2m21 tests[22m[2m)[22m[33m 5163[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m 4. approve/remove tab shows the queue, delete tab, and download button [33m 1017[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m 5. delete stats count apprenticeships, internships, and learnerships [33m 363[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m 6. delete action removes a listing and logs the listing type [33m 554[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m 8. approve action updates listing status and removes from queue [33m 373[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m 9. remove action updates listing status and removes from queue [33m 322[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m 10. approve action is logged in admin_actions payload [33m 323[2mms[22m[39m
 [32mΓ£ô[39m src/pages/Dashboard.acceptance.test.jsx [2m([22m[2m6 tests[22m[2m)[22m[33m 3407[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m handles multiple search operations with timeout cleanup [33m 1032[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m filters and displays listings by type selection [33m 873[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m resets search when type filter changes [33m 1054[2mms[22m[39m
 [32mΓ£ô[39m src/App.routing.test.jsx [2m([22m[2m5 tests[22m[2m)[22m[33m 5266[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m shows home page when unauthenticated [33m 2986[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m renders home page for unknown routes [33m 2074[2mms[22m[39m
 [32mΓ£ô[39m src/pages/ApplicantProfile.coverage.test.jsx [2m([22m[2m16 tests[22m[2m)[22m[33m 8368[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m loads profile, education, skills, and uploaded files from Supabase [33m 1658[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m creates a new applicant profile and saves education plus skills to Supabase [33m 1956[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m validates CV uploads, persists CV url, and opens uploaded documents [33m 556[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m keeps unsaved personal details and selected skills after CV upload [33m 335[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m blocks submission when required fields are missing [33m 363[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m supports multiple education entries and saves all of them [33m 737[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m allows selecting multiple skills and saves all selected values [33m 1031[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m edits an existing profile and re-saves updated values [33m 377[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m deletes the existing profile picture and refreshes the avatar state [33m 334[2mms[22m[39m
 [32mΓ£ô[39m src/pages/ApplicantSearchListings.test.jsx [2m([22m[2m13 tests[22m[2m | [22m[33m1 skipped[39m[2m)[22m[33m 7566[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m renders search bar and listing type filter on the dashboard [33m 1060[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m returns matching approved listings for a keyword search [33m 885[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m finds listings by location entered into the search bar [33m 667[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m uses the search term and type filter together [33m 602[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m updates the results panel without a full page reload [33m 596[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m restores the full approved listings list after clearing the search and resetting the filter [33m 1113[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m shows a clear no-results message when nothing matches [33m 583[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m search is case insensitive [33m 599[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m never shows pending listings even when the keyword matches [33m 589[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m never shows removed listings even when the keyword matches [33m 608[2mms[22m[39m
 [32mΓ£ô[39m src/pages/ProviderEditDeleteListing.acceptance.test.jsx [2m([22m[2m16 tests[22m[2m)[22m[33m 2431[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m 3. Saving an edited listing updates the database payload [33m 622[2mms[22m[39m
stderr | src/pages/ProviderProfile.acceptance.test.jsx > Provider profile acceptance tests > 9. Logo upload failure still saves the profile with the preview data
Logo upload failed: { message: 'storage unavailable' }

 [32mΓ£ô[39m src/RoleBasedRouting.test.jsx [2m([22m[2m10 tests[22m[2m | [22m[33m4 skipped[39m[2m)[22m[33m 2158[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m redirects unauthenticated users from protected route to home [33m 724[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m clicking Google sign-in triggers the OAuth flow [33m 937[2mms[22m[39m
 [32mΓ£ô[39m src/pages/ProviderProfile.acceptance.test.jsx [2m([22m[2m9 tests[22m[2m)[22m[33m 2358[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m 1. Provider profile page renders saved profile information from Supabase [33m 325[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m 2. Provider profile form saves to provider_profiles and updates the visible summary [33m 806[2mms[22m[39m
 [32mΓ£ô[39m src/pages/ProviderPostListing.acceptance.test.jsx [2m([22m[2m10 tests[22m[2m)[22m[33m 3010[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m 1. Listing form renders all required fields [33m 340[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m 3. Listing form blocks submission when required fields are empty [33m 799[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m 4. Closing date field rejects past dates [33m 501[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m 5/6/7. Successful submission saves listing, sets Pending, and links the signed-in provider [33m 414[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m 10. Provider dashboard filters listings by status without leaving the page [33m 425[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m 11. Provider cannot submit listing with invalid stipend [33m 344[2mms[22m[39m
 [32mΓ£ô[39m src/pages/ProviderListingForm.acceptance.test.jsx [2m([22m[2m4 tests[22m[2m)[22m[33m 2127[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m loads qualification options and shows the stipend preview [33m 439[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m shows validation errors when required fields are missing [33m 657[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m rejects a past closing date and an invalid stipend [33m 516[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m submits a valid listing and creates the requirements row [33m 505[2mms[22m[39m
 [32mΓ£ô[39m src/pages/Provider.test.jsx [2m([22m[2m12 tests[22m[2m | [22m[33m1 skipped[39m[2m)[22m[33m 2067[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m shows provider stats for active listings, pending, and approved counts [33m 467[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m shows Profile, New Listing, and Logout buttons in header [33m 1049[2mms[22m[39m
 [32mΓ£ô[39m src/pages/Dashboard.coverage.test.jsx [2m([22m[2m3 tests[22m[2m)[22m[33m 2436[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m shows approved listings and the empty search state with listings prop [33m 313[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m searching for a missing listing shows the no-results state [33m 1375[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m searching by location keeps only the matching approved listing [33m 744[2mms[22m[39m
 [32mΓ£ô[39m src/pages/index.smoke.test.jsx [2m([22m[2m10 tests[22m[2m)[22m[33m 437[2mms[22m[39m
 [32mΓ£ô[39m src/pages/ApplicantDashboardApplications.acceptance.test.jsx [2m([22m[2m4 tests[22m[2m)[22m[33m 2129[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m shows each submitted application with the requested status label [33m 677[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m shows unread notifications, marks them as read on click, and lets the applicant reveal read history [33m 893[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m refreshes the status display when a realtime application update arrives [33m 310[2mms[22m[39m
 [32mΓ£ô[39m src/pages/Dashboard.branch.test.jsx [2m([22m[2m3 tests[22m[2m)[22m[33m 675[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m falls back to built-in listings when Supabase is unavailable [33m 356[2mms[22m[39m
 [32mΓ£ô[39m src/pages/Dashboard.test.jsx [2m([22m[2m2 tests[22m[2m)[22m[33m 476[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m exports Dashboard component [33m 462[2mms[22m[39m
 [32mΓ£ô[39m src/pages/ApplicantListingDetail.acceptance.test.jsx [2m([22m[2m6 tests[22m[2m)[22m[33m 1523[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m 1. renders the listing detail page and one-click apply button [33m 887[2mms[22m[39m
 [32mΓ£ô[39m src/App.render.test.jsx [2m([22m[2m2 tests[22m[2m)[22m[33m 442[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m exports App component [33m 434[2mms[22m[39m
 [32mΓ£ô[39m src/pages/ProviderManageApplicationStatuses.acceptance.test.jsx [2m([22m[2m10 tests[22m[2m)[22m[33m 2382[2mms[22m[39m
     [33m[2mΓ£ô[22m[39m 2. Updating an applicant status writes the mapped status to the applications table [33m 932[2mms[22m[39m
 [32mΓ£ô[39m src/pages/Admin.test.jsx [2m([22m[2m2 tests[22m[2m)[22m[32m 228[2mms[22m[39m
 [32mΓ£ô[39m src/pages/ProviderProfile.logic.test.jsx [2m([22m[2m10 tests[22m[2m)[22m[32m 29[2mms[22m[39m
 [32mΓ£ô[39m src/App.logic.test.jsx [2m([22m[2m19 tests[22m[2m)[22m[32m 52[2mms[22m[39m
 [32mΓ£ô[39m src/pages/ApplicantProfile.logic.test.jsx [2m([22m[2m10 tests[22m[2m)[22m[32m 20[2mms[22m[39m
 [32mΓ£ô[39m src/pages/Dashboard.expanded.test.jsx [2m([22m[2m10 tests[22m[2m)[22m[32m 19[2mms[22m[39m
 [32mΓ£ô[39m src/pages/Admin-ConnorLaptop.test.jsx [2m([22m[2m1 test[22m[2m)[22m[32m 192[2mms[22m[39m
 [32mΓ£ô[39m src/pages/Admin.logic.test.jsx [2m([22m[2m11 tests[22m[2m)[22m[32m 21[2mms[22m[39m
 [32mΓ£ô[39m src/pages/ApplicantListingDetail.logic.test.jsx [2m([22m[2m10 tests[22m[2m)[22m[32m 13[2mms[22m[39m
 [32mΓ£ô[39m src/pages/ProviderListingForm.logic.test.jsx [2m([22m[2m6 tests[22m[2m)[22m[32m 11[2mms[22m[39m
 [32mΓ£ô[39m src/App.auth.integration.test.jsx [2m([22m[2m2 tests[22m[2m)[22m[32m 6[2mms[22m[39m
 [32mΓ£ô[39m src/App.unit.test.jsx [2m([22m[2m5 tests[22m[2m)[22m[32m 8[2mms[22m[39m
[90mstdout[2m | src/debug-import.test.jsx[2m > [22m[2mdebug exports
[22m[39mApp exports: [ [32m'default'[39m ]

 [32mΓ£ô[39m src/debug-import.test.jsx [2m([22m[2m1 test[22m[2m)[22m[32m 5[2mms[22m[39m

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ» Failed Tests 28 ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»

 FAIL  src/App.oauth.test.jsx > App OAuth and role assignment logic > getRoleForEmail function handles admin email assignment
AssertionError: expected '// Main app: handles Google OAuth, roΓÇª' to contain 'getConfiguredAdminEmails'

- Expected
+ Received

- getConfiguredAdminEmails
+ // Main app: handles Google OAuth, role-based routing, and admin dashboard

+ // TRICKY PARTS:

+ // - OAuth flow: Google login ΓåÆ email lookup ΓåÆ role selection (if new user)

+ // - Session persistence: browser cookies restored on page refresh

+ // - Route guards: ProtectedRoute blocks unauthorized access

+ // - State management: tracking auth, loading, roles, and pending signups

+ // - 3D animation: particle effect that reacts to mouse movement

+ 

+ import { useCallback, useEffect, useMemo, useRef, useState } from 'react'

+ import './App.css'

+ import { Routes, Route, useNavigate, useLocation, Navigate } from 'react-router-dom'

+ import Admin from './pages/Admin'

+ import Dashboard from './pages/Dashboard'

+ import ApplicantProfile from './pages/ApplicantProfile'

+ import ApplicantListingDetail from './pages/ApplicantListingDetail'

+ import Provider from './pages/Provider'

+ import ProviderProfile from './pages/ProviderProfile'

+ import ProviderListingForm from './pages/ProviderListingForm'

+ import ProviderListingEdit from './pages/ProviderListingEdit'

+ import ProviderListingApplications from './pages/ProviderListingApplications'

+ import { hasSupabaseConfig, supabase } from './lib/supabaseClient'

+ import {

+   getLandingRoute,

+   getRoleForEmail,

+   getApplicantLandingRouteForEmail,

+   getProviderLandingRouteForEmail,

+ } from './app-helpers'

+ const AdminDashboardShell = Admin

+ 

+ // ProtectedRoute: Guard component that restricts access to role-specific pages

+ // USAGE: <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}><Admin /></ProtectedRoute>

+ // LOGIC:

+ // 1. If still checking auth ΓåÆ show loading shell (don't flash wrong page)

+ // 2. If not signed in ΓåÆ redirect to home (must log in)

+ // 3. If wrong role ΓåÆ redirect to home (prevent unauthorized access)

+ // 4. All checks pass ΓåÆ render the protected content

+ function ProtectedRoute({ role, allowedRole, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   // Show loading state while session is being verified from browser cookies

+   // If we're still loading but already know the user is signed in, render

+   // the protected content so a refresh doesn't lock the user out temporarily.

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   // Unauthenticated users cannot access protected routes

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // Role mismatch: user is logged in but lacks permission for this page

+   // Example: Applicant (role="Applicant") trying to access /admin (allowedRole="Admin")

+   if (role !== allowedRole) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // All security checks passed: render the page

+   return children

+ }

+ 

+ function ProviderWorkspaceRoute({ role, signedIn, isLoading, providerLandingRoute, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (providerLandingRoute !== '/provider') {

+     return <Navigate to="/provider/profile" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function ProviderProfileRoute({ role, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function App() {

+   // Authentication state management:

+   const [role, setRole] = useState(null) // Current user's role: 'Admin', 'Provider', 'Applicant', or null

+   const [signedIn, setSignedIn] = useState(false) // True if OAuth session is active

+   const [isLoadingAuth, setIsLoadingAuth] = useState(true) // True while checking session from browser cookies

+   const [authError, setAuthError] = useState('') // Display auth errors to user

+   const [pendingEmail, setPendingEmail] = useState('') // New user email waiting for role selection

+   const [isSavingRole, setIsSavingRole] = useState(false) // True while inserting user into database

+   const [applicantLandingRoute, setApplicantLandingRoute] = useState('/dashboard')

+   const [providerLandingRoute, setProviderLandingRoute] = useState('/provider/profile')

+   const oauthTimeoutRef = useRef(null)

+ 

+   // Animation state for the 3D particle effect

+   const [pointer, setPointer] = useState({ x: 0, y: 0 })

+   const [isScrolled, setIsScrolled] = useState(false)

+   const navigate = useNavigate()

+   const location = useLocation()

+   const oauthRedirectTo =

+     import.meta.env.VITE_AUTH_REDIRECT_URL?.trim() || `${window.location.origin}/`

+ 

+   const clearOAuthTimeout = useCallback(() => {

+     if (oauthTimeoutRef.current) {

+       window.clearTimeout(oauthTimeoutRef.current)

+       oauthTimeoutRef.current = null

+     }

+   }, [])

+ 

+ // helper functions are implemented in ./app-helpers and imported above

+ 

+   // AUTH BOOTSTRAP: Restore user session on app load and listen for auth changes

+   // FLOW:

+   // 1. Check if session exists in browser cookies (user already logged in before)

+   // 2. Lookup user's role in database by email

+   // 3. Set up listener to detect sign-in/sign-out events

+   // 4. When sign-in happens, role lookup and optional role selection for new users

+   // WHY: Ensures app state matches Supabase auth session, prevents stale data on refresh

+   useEffect(() => {

+     let isMounted = true

+ 

+     const initializeAuth = async () => {

+       setIsLoadingAuth(true)

+ 

+       if (!hasSupabaseConfig) {

+         if (isMounted) {

+           setAuthError('Missing Supabase environment variables. Add VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const { data, error } = await supabase.auth.getSession()

+ 

+       if (error) {

+         if (isMounted) {

+           setAuthError('Unable to restore your session. Please sign in again.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const existingSession = data.session

+ 

+       if (!existingSession?.user?.email) {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(existingSession.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(existingSession.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(existingSession.user.email)

+             : getLandingRoute(resolvedRole)

+         if (isMounted) {

+           setSignedIn(true)

+           setRole(resolvedRole)

+           setPendingEmail(resolvedRole ? '' : existingSession.user.email)

+           setApplicantLandingRoute(resolvedLandingRoute)

+           setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+           setAuthError('')

+         }

+       } catch {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setAuthError('Signed in, but role lookup failed. Check your users table and policies.')

+         }

+       } finally {

+         if (isMounted) {

+           setIsLoadingAuth(false)

+         }

+       }

+     }

+ 

+     initializeAuth()

+ 

+     if (!hasSupabaseConfig) {

+       return () => {

+         isMounted = false

+       }

+     }

+ 

+     const {

+       data: { subscription },

+     } = supabase.auth.onAuthStateChange(async (event, session) => {

+       if (!isMounted) {

+         return

+       }

+ 

+       if (event === 'SIGNED_OUT' || !session?.user?.email) {

+         clearOAuthTimeout()

+         setSignedIn(false)

+         setRole(null)

+         setPendingEmail('')

+         setIsSavingRole(false)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setIsLoadingAuth(false)

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(session.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(session.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(session.user.email)

+             : getLandingRoute(resolvedRole)

+         if (!isMounted) {

+           return

+         }

+         clearOAuthTimeout()

+         setSignedIn(true)

+         setRole(resolvedRole)

+         setPendingEmail(resolvedRole ? '' : session.user.email)

+         setApplicantLandingRoute(resolvedLandingRoute)

+         setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+         setAuthError('')

+       } catch {

+         if (!isMounted) {

+           return

+         }

+         setSignedIn(false)

+         setRole(null)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setAuthError('OAuth succeeded, but role lookup failed. Please verify Supabase table policies.')

+       } finally {

+         if (isMounted) {

+           clearOAuthTimeout()

+           setIsLoadingAuth(false)

+         }

+       }

+     })

+ 

+     return () => {

+       clearOAuthTimeout()

+       isMounted = false

+       subscription.unsubscribe()

+     }

+   }, [clearOAuthTimeout])

+ 

+   // AUTO-REDIRECT: Send logged-in users to their role-appropriate dashboard

+   // WHEN: User has loaded auth state, is signed in, has a role, and is on home page

+   // REDIRECTS: Admin ΓåÆ /admin, Provider ΓåÆ /provider, Applicant ΓåÆ /dashboard

+   useEffect(() => {

+     const redirectedFromProtectedRoute = Boolean(location.state?.from)

+     const keepApplicantOnHome = redirectedFromProtectedRoute && role === 'Applicant'

+     const landingRoute =

+       role === 'Applicant'

+         ? applicantLandingRoute

+         : role === 'Provider'

+           ? providerLandingRoute

+           : getLandingRoute(role)

+ 

+     if (!isLoadingAuth && signedIn && role && location.pathname === '/' && !keepApplicantOnHome) {

+       navigate(landingRoute, { replace: true })

+     }

+   }, [isLoadingAuth, signedIn, role, applicantLandingRoute, providerLandingRoute, location.pathname, location.state, navigate])

+ 

+   // Clear session and reset all state when logging out

+   const handleLogout = async () => {

+     clearOAuthTimeout()

+ 

+     if (hasSupabaseConfig) {

+       await supabase.auth.signOut() // Clear session cookie

+     }

+     setSignedIn(false)

+     setRole(null)

+     setPendingEmail('')

+     setIsSavingRole(false)

+     setApplicantLandingRoute('/dashboard')

+     setProviderLandingRoute('/provider/profile')

+     navigate('/')

+   }

+ 

+   // NEW USER ROLE SELECTION: First-time users must choose their role

+   // WHY: roles (Admin/Provider/Applicant) determine which dashboard they access

+   // PROCESS:

+   // 1. User logs in with Google (new email, not in database yet)

+   // 2. We show role selection popup

+   // 3. User picks role ΓåÆ we insert into 'users' table with their email + role

+   // 4. Next login, step 2-3 are skipped (role already in database)

+   const handleRoleSelection = async (selectedRole) => {

+     if (!pendingEmail) {

+       return

+     }

+ 

+     setIsSavingRole(true)

+     setAuthError('')

+ 

+     const { data: insertedUser, error } = await supabase

+       .from('users')

+       .insert({ email: pendingEmail, role: selectedRole })

+       .select('role')

+       .single()

+ 

+     if (error) {

+       setIsSavingRole(false)

+       setAuthError('Could not save role selection. Please try again.')

+       return

+     }

+ 

+     const resolvedRole = insertedUser?.role || selectedRole

+     setRole(resolvedRole)

+     setPendingEmail('')

+     setIsSavingRole(false)

+ 

+     if (resolvedRole === 'Applicant') {

+       setApplicantLandingRoute('/profile')

+       navigate('/profile', { replace: true })

+       return

+     }

+ 

+     if (resolvedRole === 'Provider') {

+       setProviderLandingRoute('/provider/profile')

+       navigate('/provider/profile', { replace: true })

+       return

+     }

+ 

+     setApplicantLandingRoute(getLandingRoute(resolvedRole))

+     navigate(getLandingRoute(resolvedRole), { replace: true })

+   }

+ 

+   // Generate particle positions for 3D animation (748 dots in 34x22 grid)

+   // Complex math creates radial gradient effect that reacts to mouse movement

+   const dots = useMemo(() => {

+     const result = []

+     const columns = 34

+     const rows = 22

+ 

+     for (let row = 0; row < rows; row += 1) {

+       for (let col = 0; col < columns; col += 1) {

+         const index = row * columns + col

+         const nx = (col - (columns - 1) / 2) / ((columns - 1) / 2)

+         const ny = (row - (rows - 1) / 2) / ((rows - 1) / 2)

+         const r = Math.sqrt(nx * nx + ny * ny)

+         const z = (1 - Math.min(r, 1)) * 110 - Math.abs(nx) * 32

+         const dx = ((col % 5) - 2) * 1.6

+         const dy = ((row % 5) - 2) * 1.4

+         const dz = ((index % 7) - 3) * 2.2

+         const delay = `${(index % 11) * 0.18}s`

+         const duration = `${3.6 + (index % 6) * 0.6}s`

+         result.push({ nx, ny, z, id: `${row}-${col}`, dx, dy, dz, delay, duration })

+       }

+     }

+ 

+     return result

+   }, [])

+ 

+   // Track mouse position for 3D particle effect

+   const handleVisualMove = (event) => {

+     const rect = event.currentTarget.getBoundingClientRect()

+     const px = ((event.clientX - rect.left) / rect.width - 0.5) * 2

+     const py = ((event.clientY - rect.top) / rect.height - 0.5) * 2

+     setPointer({ x: px, y: py })

+   }

+ 

+   const resetVisualMove = () => setPointer({ x: 0, y: 0 })

+ 

+   // OAUTH ENTRY POINT: Initiate Google sign-in

+   // HOW: Calls Supabase.auth.signInWithOAuth() which opens Google login popup

+   // THEN: Auth listener (above) automatically detects successful login and looks up role

+   // NOTE: Don't manually handle role lookup hereΓÇölet the auth listener do it (keeps logic centralized)

+   const handleGoogleContinue = async () => {

+     setAuthError('')

+ 

+     if (!hasSupabaseConfig) {

+       setAuthError('Set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY before using Google login.')

+       return

+     }

+ 

+     setIsLoadingAuth(true)

+ 

+     clearOAuthTimeout()

+     oauthTimeoutRef.current = window.setTimeout(() => {

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in timed out. Please try again.')

+     }, 12000)

+ 

+     const { error } = await supabase.auth.signInWithOAuth({

+       provider: 'google',

+       options: {

+         redirectTo: oauthRedirectTo,

+       },

+     })

+ 

+     if (error) {

+       clearOAuthTimeout()

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in failed. Verify Supabase redirect URLs include your local host and VITE_AUTH_REDIRECT_URL value.')

+     }

+   }

+ 

+   // Fade in sections as they scroll into view

+   useEffect(() => {

+     if (location.pathname !== '/') {

+       return undefined

+     }

+ 

+     const nodes = document.querySelectorAll('.scroll-animate')

+ 

+     if (!nodes.length) {

+       return undefined

+     }

+ 

+     const observer = new IntersectionObserver(

+       (entries) => {

+         entries.forEach((entry) => {

+           if (entry.isIntersecting) {

+             entry.target.classList.add('is-visible') // Add CSS class to trigger fade-in

+           } else {

+             entry.target.classList.remove('is-visible') // Remove class when scrolled past

+           }

+         })

+       },

+       { threshold: 0.18 }, // Element must be 18% visible to trigger

+     )

+ 

+     nodes.forEach((node) => observer.observe(node))

+ 

+     return () => {

+       nodes.forEach((node) => observer.unobserve(node))

+       observer.disconnect()

+     }

+   }, [location.pathname])

+ 

+   // Track if user has scrolled past the hero section (used for topbar styling)

+   useEffect(() => {

+     const onScroll = () => setIsScrolled(window.scrollY > 120)

+     onScroll()

+     window.addEventListener('scroll', onScroll, { passive: true })

+     return () => window.removeEventListener('scroll', onScroll)

+   }, [])

+ 

+   // All routes: home page with auth, and protected role dashboards

+ return (

+   <Routes>

+     <Route path="/" element={

+       <main className="foundry-clone">

+ 

+         <header className={`global-topbar ${isScrolled ? 'is-scrolled' : ''}`}>

+           <div className="topbar-main">

+             <div className="topbar-brand">SA LEARNERSHIP FOUNDRY</div>

+             <nav className="topbar-nav">

+               <a href="#">Why Portal</a>

+               <a href="#">Pathways</a>

+               <a href="#">Team</a>

+               <a href="#">Insights</a>

+               <a href="#">Contact</a>

+             </nav>

+           </div>

+ 

+           <div className="topbar-auth">

+             {signedIn && role ? <span className="role-display">{role}</span> : null}

+             <button className="google-auth" onClick={signedIn ? handleLogout : handleGoogleContinue}>

+               {isLoadingAuth ? 'Authenticating...' : signedIn ? 'Logout' : 'Log In with Google'}

+             </button>

+           </div>

+         </header>

+ 

+         <section className="page-one">

+           <div className="hero-main">

+             <h1 className="left-title">

+               BUILDING TALENT

+               <br />

+               PATHWAYS THAT

+             </h1>

+ 

+             <div

+               className="interactive-model"

+               onMouseMove={handleVisualMove}

+               onMouseLeave={resetVisualMove}

+               style={{ '--px': pointer.x, '--py': pointer.y }}

+             >

+               <div className="orbital-core">

+                 <span className="orbit orbit-a"></span>

+                 <span className="orbit orbit-b"></span>

+                 <span className="orbit orbit-c"></span>

+               </div>

+ 

+               <div className="dot-volume">

+                 {dots.map((dot) => (

+                   <span

+                     key={dot.id}

+                     className="dot"

+                     style={{

+                       '--x': `${dot.nx * 340}px`,

+                       '--y': `${dot.ny * 240}px`,

+                       '--z': `${dot.z}px`,

+                     }}

+                   />

+                 ))}

+               </div>

+             </div>

+ 

+             <h1 className="right-title">

+               PUSH THE

+               <br />

+               FUTURE FORWARD

+             </h1>

+           </div>

+ 

+           <div className="hero-bottom">

+             <p>

+               A venture lab style platform for learnership access, application intelligence,

+               and practical support that moves candidates from skill to real work.

+             </p>

+ 

+             <div className="status-pill">

+               {signedIn && role

+                 ? `${role} workspace unlocked`

+                 : signedIn && pendingEmail

+                   ? 'Choose your role to continue'

+                   : 'Sign in with Google to continue'}

+             </div>

+           </div>

+           {signedIn && !role && pendingEmail ? (

+             <div className="role-onboarding-overlay" role="dialog" aria-modal="true" aria-labelledby="role-onboarding-title">

+               <div className="role-onboarding">

+                 <p className="role-onboarding-eyebrow">One more step</p>

+                 <h2 id="role-onboarding-title">Choose your role</h2>

+                 <p>Select how you want to use the platform for {pendingEmail}.</p>

+                 <div className="role-select">

+                   <button onClick={() => handleRoleSelection('Applicant')} disabled={isSavingRole}>

+                     Applicant

+                   </button>

+                   <button onClick={() => handleRoleSelection('Provider')} disabled={isSavingRole}>

+                     Provider

+                   </button>

+                 </div>

+               </div>

+             </div>

+           ) : null}

+           {authError ? <p className="auth-error">{authError}</p> : null}

+         </section>

+ 

+         <section className="page-two">

+           <div className="ethos-row scroll-animate">

+             <div>

+               <p className="mini-label">Why this platform</p>

+               <h2>

+                 OPEN DOORS

+                 <br />

+                 TO REAL WORK

+               </h2>

+             </div>

+ 

+             <div className="ethos-copy">

+               <p>

+                 Learnership Foundry helps applicants, providers, and moderators collaborate in one

+                 flow. Discover pathways, publish opportunities, and keep quality high with

+                 visibility from intake to placement.

+               </p>

+             </div>

+           </div>

+ 

+           <div className="card-strip scroll-animate swipe-left">

+             <article className="signal-card tone-dark">

+               <h3>Discover</h3>

+               <p>APPLICANT VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Find learnerships that match your interests and readiness level.</small>

+             </article>

+ 

+             <article className="signal-card tone-cyan">

+               <h3>Publish</h3>

+               <p>PROVIDER VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Create listings and track engagement from qualified candidates.</small>

+             </article>

+ 

+             <article className="signal-card tone-orange">

+               <h3>Moderate</h3>

+               <p>ADMIN VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Flag risk, request fixes, and approve opportunities confidently.</small>

+             </article>

+ 

+             <article className="signal-card tone-light">

+               <h3>Move</h3>

+               <p>OUTCOMES</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Turn applications into skills, credentials, and workplace entry points.</small>

+             </article>

+           </div>

+         </section>

+ 

+         <section className="page-three">

+           <span className="dot-plane" aria-hidden="true"></span>

+           <div className="focus-copy scroll-animate">

+             <p className="mini-label">Built for momentum</p>

+             <h2>

+               From FIRST CLICK

+               <br />

+               TO FIRST PAYCHECK

+             </h2>

+             <p>

+               Scroll the platform, choose your role, and move into the right workspace to continue.

+             </p>

+           </div>

+         </section>

+ 

+       </main>

+     } />

+ 

+     

+    <Route path="/dashboard" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <Dashboard onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+ <Route path="/dashboard/listings/:listingId" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ApplicantListingDetail onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   <Route path="/profile" element={

+     <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+       <ApplicantProfile onLogout={handleLogout} />

+     </ProtectedRoute>

+   } />

+ 

+ <Route path="/provider" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <Provider onLogout={handleLogout} />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/profile" element={

+   <ProviderProfileRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ProviderProfile

+       onLogout={handleLogout}

+       onProfileSaved={() => setProviderLandingRoute('/provider')}

+     />

+   </ProviderProfileRoute>

+ } />

+ 

+ <Route path="/provider/listings/new" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingForm />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/edit" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingEdit />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/applications" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingApplications />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/admin" element={

+   <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <AdminDashboardShell onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   </Routes>

+ )

+ }

+ 

+ export default App

+

 Γ¥» src/App.oauth.test.jsx:15:23
     13|
     14|     expect(appSource).toContain('getRoleForEmail')
     15|     expect(appSource).toContain('getConfiguredAdminEmails')
       |                       ^
     16|     expect(appSource).toContain('.toLowerCase()')
     17|   })

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[1/28]ΓÄ»

 FAIL  src/App.oauth.test.jsx > App OAuth and role assignment logic > isProviderProfileComplete checks all required fields
AssertionError: expected '// Main app: handles Google OAuth, roΓÇª' to contain 'isProviderProfileComplete'

- Expected
+ Received

- isProviderProfileComplete
+ // Main app: handles Google OAuth, role-based routing, and admin dashboard

+ // TRICKY PARTS:

+ // - OAuth flow: Google login ΓåÆ email lookup ΓåÆ role selection (if new user)

+ // - Session persistence: browser cookies restored on page refresh

+ // - Route guards: ProtectedRoute blocks unauthorized access

+ // - State management: tracking auth, loading, roles, and pending signups

+ // - 3D animation: particle effect that reacts to mouse movement

+ 

+ import { useCallback, useEffect, useMemo, useRef, useState } from 'react'

+ import './App.css'

+ import { Routes, Route, useNavigate, useLocation, Navigate } from 'react-router-dom'

+ import Admin from './pages/Admin'

+ import Dashboard from './pages/Dashboard'

+ import ApplicantProfile from './pages/ApplicantProfile'

+ import ApplicantListingDetail from './pages/ApplicantListingDetail'

+ import Provider from './pages/Provider'

+ import ProviderProfile from './pages/ProviderProfile'

+ import ProviderListingForm from './pages/ProviderListingForm'

+ import ProviderListingEdit from './pages/ProviderListingEdit'

+ import ProviderListingApplications from './pages/ProviderListingApplications'

+ import { hasSupabaseConfig, supabase } from './lib/supabaseClient'

+ import {

+   getLandingRoute,

+   getRoleForEmail,

+   getApplicantLandingRouteForEmail,

+   getProviderLandingRouteForEmail,

+ } from './app-helpers'

+ const AdminDashboardShell = Admin

+ 

+ // ProtectedRoute: Guard component that restricts access to role-specific pages

+ // USAGE: <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}><Admin /></ProtectedRoute>

+ // LOGIC:

+ // 1. If still checking auth ΓåÆ show loading shell (don't flash wrong page)

+ // 2. If not signed in ΓåÆ redirect to home (must log in)

+ // 3. If wrong role ΓåÆ redirect to home (prevent unauthorized access)

+ // 4. All checks pass ΓåÆ render the protected content

+ function ProtectedRoute({ role, allowedRole, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   // Show loading state while session is being verified from browser cookies

+   // If we're still loading but already know the user is signed in, render

+   // the protected content so a refresh doesn't lock the user out temporarily.

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   // Unauthenticated users cannot access protected routes

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // Role mismatch: user is logged in but lacks permission for this page

+   // Example: Applicant (role="Applicant") trying to access /admin (allowedRole="Admin")

+   if (role !== allowedRole) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // All security checks passed: render the page

+   return children

+ }

+ 

+ function ProviderWorkspaceRoute({ role, signedIn, isLoading, providerLandingRoute, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (providerLandingRoute !== '/provider') {

+     return <Navigate to="/provider/profile" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function ProviderProfileRoute({ role, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function App() {

+   // Authentication state management:

+   const [role, setRole] = useState(null) // Current user's role: 'Admin', 'Provider', 'Applicant', or null

+   const [signedIn, setSignedIn] = useState(false) // True if OAuth session is active

+   const [isLoadingAuth, setIsLoadingAuth] = useState(true) // True while checking session from browser cookies

+   const [authError, setAuthError] = useState('') // Display auth errors to user

+   const [pendingEmail, setPendingEmail] = useState('') // New user email waiting for role selection

+   const [isSavingRole, setIsSavingRole] = useState(false) // True while inserting user into database

+   const [applicantLandingRoute, setApplicantLandingRoute] = useState('/dashboard')

+   const [providerLandingRoute, setProviderLandingRoute] = useState('/provider/profile')

+   const oauthTimeoutRef = useRef(null)

+ 

+   // Animation state for the 3D particle effect

+   const [pointer, setPointer] = useState({ x: 0, y: 0 })

+   const [isScrolled, setIsScrolled] = useState(false)

+   const navigate = useNavigate()

+   const location = useLocation()

+   const oauthRedirectTo =

+     import.meta.env.VITE_AUTH_REDIRECT_URL?.trim() || `${window.location.origin}/`

+ 

+   const clearOAuthTimeout = useCallback(() => {

+     if (oauthTimeoutRef.current) {

+       window.clearTimeout(oauthTimeoutRef.current)

+       oauthTimeoutRef.current = null

+     }

+   }, [])

+ 

+ // helper functions are implemented in ./app-helpers and imported above

+ 

+   // AUTH BOOTSTRAP: Restore user session on app load and listen for auth changes

+   // FLOW:

+   // 1. Check if session exists in browser cookies (user already logged in before)

+   // 2. Lookup user's role in database by email

+   // 3. Set up listener to detect sign-in/sign-out events

+   // 4. When sign-in happens, role lookup and optional role selection for new users

+   // WHY: Ensures app state matches Supabase auth session, prevents stale data on refresh

+   useEffect(() => {

+     let isMounted = true

+ 

+     const initializeAuth = async () => {

+       setIsLoadingAuth(true)

+ 

+       if (!hasSupabaseConfig) {

+         if (isMounted) {

+           setAuthError('Missing Supabase environment variables. Add VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const { data, error } = await supabase.auth.getSession()

+ 

+       if (error) {

+         if (isMounted) {

+           setAuthError('Unable to restore your session. Please sign in again.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const existingSession = data.session

+ 

+       if (!existingSession?.user?.email) {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(existingSession.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(existingSession.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(existingSession.user.email)

+             : getLandingRoute(resolvedRole)

+         if (isMounted) {

+           setSignedIn(true)

+           setRole(resolvedRole)

+           setPendingEmail(resolvedRole ? '' : existingSession.user.email)

+           setApplicantLandingRoute(resolvedLandingRoute)

+           setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+           setAuthError('')

+         }

+       } catch {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setAuthError('Signed in, but role lookup failed. Check your users table and policies.')

+         }

+       } finally {

+         if (isMounted) {

+           setIsLoadingAuth(false)

+         }

+       }

+     }

+ 

+     initializeAuth()

+ 

+     if (!hasSupabaseConfig) {

+       return () => {

+         isMounted = false

+       }

+     }

+ 

+     const {

+       data: { subscription },

+     } = supabase.auth.onAuthStateChange(async (event, session) => {

+       if (!isMounted) {

+         return

+       }

+ 

+       if (event === 'SIGNED_OUT' || !session?.user?.email) {

+         clearOAuthTimeout()

+         setSignedIn(false)

+         setRole(null)

+         setPendingEmail('')

+         setIsSavingRole(false)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setIsLoadingAuth(false)

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(session.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(session.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(session.user.email)

+             : getLandingRoute(resolvedRole)

+         if (!isMounted) {

+           return

+         }

+         clearOAuthTimeout()

+         setSignedIn(true)

+         setRole(resolvedRole)

+         setPendingEmail(resolvedRole ? '' : session.user.email)

+         setApplicantLandingRoute(resolvedLandingRoute)

+         setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+         setAuthError('')

+       } catch {

+         if (!isMounted) {

+           return

+         }

+         setSignedIn(false)

+         setRole(null)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setAuthError('OAuth succeeded, but role lookup failed. Please verify Supabase table policies.')

+       } finally {

+         if (isMounted) {

+           clearOAuthTimeout()

+           setIsLoadingAuth(false)

+         }

+       }

+     })

+ 

+     return () => {

+       clearOAuthTimeout()

+       isMounted = false

+       subscription.unsubscribe()

+     }

+   }, [clearOAuthTimeout])

+ 

+   // AUTO-REDIRECT: Send logged-in users to their role-appropriate dashboard

+   // WHEN: User has loaded auth state, is signed in, has a role, and is on home page

+   // REDIRECTS: Admin ΓåÆ /admin, Provider ΓåÆ /provider, Applicant ΓåÆ /dashboard

+   useEffect(() => {

+     const redirectedFromProtectedRoute = Boolean(location.state?.from)

+     const keepApplicantOnHome = redirectedFromProtectedRoute && role === 'Applicant'

+     const landingRoute =

+       role === 'Applicant'

+         ? applicantLandingRoute

+         : role === 'Provider'

+           ? providerLandingRoute

+           : getLandingRoute(role)

+ 

+     if (!isLoadingAuth && signedIn && role && location.pathname === '/' && !keepApplicantOnHome) {

+       navigate(landingRoute, { replace: true })

+     }

+   }, [isLoadingAuth, signedIn, role, applicantLandingRoute, providerLandingRoute, location.pathname, location.state, navigate])

+ 

+   // Clear session and reset all state when logging out

+   const handleLogout = async () => {

+     clearOAuthTimeout()

+ 

+     if (hasSupabaseConfig) {

+       await supabase.auth.signOut() // Clear session cookie

+     }

+     setSignedIn(false)

+     setRole(null)

+     setPendingEmail('')

+     setIsSavingRole(false)

+     setApplicantLandingRoute('/dashboard')

+     setProviderLandingRoute('/provider/profile')

+     navigate('/')

+   }

+ 

+   // NEW USER ROLE SELECTION: First-time users must choose their role

+   // WHY: roles (Admin/Provider/Applicant) determine which dashboard they access

+   // PROCESS:

+   // 1. User logs in with Google (new email, not in database yet)

+   // 2. We show role selection popup

+   // 3. User picks role ΓåÆ we insert into 'users' table with their email + role

+   // 4. Next login, step 2-3 are skipped (role already in database)

+   const handleRoleSelection = async (selectedRole) => {

+     if (!pendingEmail) {

+       return

+     }

+ 

+     setIsSavingRole(true)

+     setAuthError('')

+ 

+     const { data: insertedUser, error } = await supabase

+       .from('users')

+       .insert({ email: pendingEmail, role: selectedRole })

+       .select('role')

+       .single()

+ 

+     if (error) {

+       setIsSavingRole(false)

+       setAuthError('Could not save role selection. Please try again.')

+       return

+     }

+ 

+     const resolvedRole = insertedUser?.role || selectedRole

+     setRole(resolvedRole)

+     setPendingEmail('')

+     setIsSavingRole(false)

+ 

+     if (resolvedRole === 'Applicant') {

+       setApplicantLandingRoute('/profile')

+       navigate('/profile', { replace: true })

+       return

+     }

+ 

+     if (resolvedRole === 'Provider') {

+       setProviderLandingRoute('/provider/profile')

+       navigate('/provider/profile', { replace: true })

+       return

+     }

+ 

+     setApplicantLandingRoute(getLandingRoute(resolvedRole))

+     navigate(getLandingRoute(resolvedRole), { replace: true })

+   }

+ 

+   // Generate particle positions for 3D animation (748 dots in 34x22 grid)

+   // Complex math creates radial gradient effect that reacts to mouse movement

+   const dots = useMemo(() => {

+     const result = []

+     const columns = 34

+     const rows = 22

+ 

+     for (let row = 0; row < rows; row += 1) {

+       for (let col = 0; col < columns; col += 1) {

+         const index = row * columns + col

+         const nx = (col - (columns - 1) / 2) / ((columns - 1) / 2)

+         const ny = (row - (rows - 1) / 2) / ((rows - 1) / 2)

+         const r = Math.sqrt(nx * nx + ny * ny)

+         const z = (1 - Math.min(r, 1)) * 110 - Math.abs(nx) * 32

+         const dx = ((col % 5) - 2) * 1.6

+         const dy = ((row % 5) - 2) * 1.4

+         const dz = ((index % 7) - 3) * 2.2

+         const delay = `${(index % 11) * 0.18}s`

+         const duration = `${3.6 + (index % 6) * 0.6}s`

+         result.push({ nx, ny, z, id: `${row}-${col}`, dx, dy, dz, delay, duration })

+       }

+     }

+ 

+     return result

+   }, [])

+ 

+   // Track mouse position for 3D particle effect

+   const handleVisualMove = (event) => {

+     const rect = event.currentTarget.getBoundingClientRect()

+     const px = ((event.clientX - rect.left) / rect.width - 0.5) * 2

+     const py = ((event.clientY - rect.top) / rect.height - 0.5) * 2

+     setPointer({ x: px, y: py })

+   }

+ 

+   const resetVisualMove = () => setPointer({ x: 0, y: 0 })

+ 

+   // OAUTH ENTRY POINT: Initiate Google sign-in

+   // HOW: Calls Supabase.auth.signInWithOAuth() which opens Google login popup

+   // THEN: Auth listener (above) automatically detects successful login and looks up role

+   // NOTE: Don't manually handle role lookup hereΓÇölet the auth listener do it (keeps logic centralized)

+   const handleGoogleContinue = async () => {

+     setAuthError('')

+ 

+     if (!hasSupabaseConfig) {

+       setAuthError('Set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY before using Google login.')

+       return

+     }

+ 

+     setIsLoadingAuth(true)

+ 

+     clearOAuthTimeout()

+     oauthTimeoutRef.current = window.setTimeout(() => {

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in timed out. Please try again.')

+     }, 12000)

+ 

+     const { error } = await supabase.auth.signInWithOAuth({

+       provider: 'google',

+       options: {

+         redirectTo: oauthRedirectTo,

+       },

+     })

+ 

+     if (error) {

+       clearOAuthTimeout()

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in failed. Verify Supabase redirect URLs include your local host and VITE_AUTH_REDIRECT_URL value.')

+     }

+   }

+ 

+   // Fade in sections as they scroll into view

+   useEffect(() => {

+     if (location.pathname !== '/') {

+       return undefined

+     }

+ 

+     const nodes = document.querySelectorAll('.scroll-animate')

+ 

+     if (!nodes.length) {

+       return undefined

+     }

+ 

+     const observer = new IntersectionObserver(

+       (entries) => {

+         entries.forEach((entry) => {

+           if (entry.isIntersecting) {

+             entry.target.classList.add('is-visible') // Add CSS class to trigger fade-in

+           } else {

+             entry.target.classList.remove('is-visible') // Remove class when scrolled past

+           }

+         })

+       },

+       { threshold: 0.18 }, // Element must be 18% visible to trigger

+     )

+ 

+     nodes.forEach((node) => observer.observe(node))

+ 

+     return () => {

+       nodes.forEach((node) => observer.unobserve(node))

+       observer.disconnect()

+     }

+   }, [location.pathname])

+ 

+   // Track if user has scrolled past the hero section (used for topbar styling)

+   useEffect(() => {

+     const onScroll = () => setIsScrolled(window.scrollY > 120)

+     onScroll()

+     window.addEventListener('scroll', onScroll, { passive: true })

+     return () => window.removeEventListener('scroll', onScroll)

+   }, [])

+ 

+   // All routes: home page with auth, and protected role dashboards

+ return (

+   <Routes>

+     <Route path="/" element={

+       <main className="foundry-clone">

+ 

+         <header className={`global-topbar ${isScrolled ? 'is-scrolled' : ''}`}>

+           <div className="topbar-main">

+             <div className="topbar-brand">SA LEARNERSHIP FOUNDRY</div>

+             <nav className="topbar-nav">

+               <a href="#">Why Portal</a>

+               <a href="#">Pathways</a>

+               <a href="#">Team</a>

+               <a href="#">Insights</a>

+               <a href="#">Contact</a>

+             </nav>

+           </div>

+ 

+           <div className="topbar-auth">

+             {signedIn && role ? <span className="role-display">{role}</span> : null}

+             <button className="google-auth" onClick={signedIn ? handleLogout : handleGoogleContinue}>

+               {isLoadingAuth ? 'Authenticating...' : signedIn ? 'Logout' : 'Log In with Google'}

+             </button>

+           </div>

+         </header>

+ 

+         <section className="page-one">

+           <div className="hero-main">

+             <h1 className="left-title">

+               BUILDING TALENT

+               <br />

+               PATHWAYS THAT

+             </h1>

+ 

+             <div

+               className="interactive-model"

+               onMouseMove={handleVisualMove}

+               onMouseLeave={resetVisualMove}

+               style={{ '--px': pointer.x, '--py': pointer.y }}

+             >

+               <div className="orbital-core">

+                 <span className="orbit orbit-a"></span>

+                 <span className="orbit orbit-b"></span>

+                 <span className="orbit orbit-c"></span>

+               </div>

+ 

+               <div className="dot-volume">

+                 {dots.map((dot) => (

+                   <span

+                     key={dot.id}

+                     className="dot"

+                     style={{

+                       '--x': `${dot.nx * 340}px`,

+                       '--y': `${dot.ny * 240}px`,

+                       '--z': `${dot.z}px`,

+                     }}

+                   />

+                 ))}

+               </div>

+             </div>

+ 

+             <h1 className="right-title">

+               PUSH THE

+               <br />

+               FUTURE FORWARD

+             </h1>

+           </div>

+ 

+           <div className="hero-bottom">

+             <p>

+               A venture lab style platform for learnership access, application intelligence,

+               and practical support that moves candidates from skill to real work.

+             </p>

+ 

+             <div className="status-pill">

+               {signedIn && role

+                 ? `${role} workspace unlocked`

+                 : signedIn && pendingEmail

+                   ? 'Choose your role to continue'

+                   : 'Sign in with Google to continue'}

+             </div>

+           </div>

+           {signedIn && !role && pendingEmail ? (

+             <div className="role-onboarding-overlay" role="dialog" aria-modal="true" aria-labelledby="role-onboarding-title">

+               <div className="role-onboarding">

+                 <p className="role-onboarding-eyebrow">One more step</p>

+                 <h2 id="role-onboarding-title">Choose your role</h2>

+                 <p>Select how you want to use the platform for {pendingEmail}.</p>

+                 <div className="role-select">

+                   <button onClick={() => handleRoleSelection('Applicant')} disabled={isSavingRole}>

+                     Applicant

+                   </button>

+                   <button onClick={() => handleRoleSelection('Provider')} disabled={isSavingRole}>

+                     Provider

+                   </button>

+                 </div>

+               </div>

+             </div>

+           ) : null}

+           {authError ? <p className="auth-error">{authError}</p> : null}

+         </section>

+ 

+         <section className="page-two">

+           <div className="ethos-row scroll-animate">

+             <div>

+               <p className="mini-label">Why this platform</p>

+               <h2>

+                 OPEN DOORS

+                 <br />

+                 TO REAL WORK

+               </h2>

+             </div>

+ 

+             <div className="ethos-copy">

+               <p>

+                 Learnership Foundry helps applicants, providers, and moderators collaborate in one

+                 flow. Discover pathways, publish opportunities, and keep quality high with

+                 visibility from intake to placement.

+               </p>

+             </div>

+           </div>

+ 

+           <div className="card-strip scroll-animate swipe-left">

+             <article className="signal-card tone-dark">

+               <h3>Discover</h3>

+               <p>APPLICANT VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Find learnerships that match your interests and readiness level.</small>

+             </article>

+ 

+             <article className="signal-card tone-cyan">

+               <h3>Publish</h3>

+               <p>PROVIDER VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Create listings and track engagement from qualified candidates.</small>

+             </article>

+ 

+             <article className="signal-card tone-orange">

+               <h3>Moderate</h3>

+               <p>ADMIN VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Flag risk, request fixes, and approve opportunities confidently.</small>

+             </article>

+ 

+             <article className="signal-card tone-light">

+               <h3>Move</h3>

+               <p>OUTCOMES</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Turn applications into skills, credentials, and workplace entry points.</small>

+             </article>

+           </div>

+         </section>

+ 

+         <section className="page-three">

+           <span className="dot-plane" aria-hidden="true"></span>

+           <div className="focus-copy scroll-animate">

+             <p className="mini-label">Built for momentum</p>

+             <h2>

+               From FIRST CLICK

+               <br />

+               TO FIRST PAYCHECK

+             </h2>

+             <p>

+               Scroll the platform, choose your role, and move into the right workspace to continue.

+             </p>

+           </div>

+         </section>

+ 

+       </main>

+     } />

+ 

+     

+    <Route path="/dashboard" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <Dashboard onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+ <Route path="/dashboard/listings/:listingId" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ApplicantListingDetail onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   <Route path="/profile" element={

+     <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+       <ApplicantProfile onLogout={handleLogout} />

+     </ProtectedRoute>

+   } />

+ 

+ <Route path="/provider" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <Provider onLogout={handleLogout} />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/profile" element={

+   <ProviderProfileRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ProviderProfile

+       onLogout={handleLogout}

+       onProfileSaved={() => setProviderLandingRoute('/provider')}

+     />

+   </ProviderProfileRoute>

+ } />

+ 

+ <Route path="/provider/listings/new" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingForm />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/edit" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingEdit />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/applications" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingApplications />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/admin" element={

+   <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <AdminDashboardShell onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   </Routes>

+ )

+ }

+ 

+ export default App

+

 Γ¥» src/App.oauth.test.jsx:22:23
     20|     const appSource = readFileSync(resolve(cwd(), 'src/App.jsx'), 'utfΓÇª
     21|
     22|     expect(appSource).toContain('isProviderProfileComplete')
       |                       ^
     23|     expect(appSource).toContain('organisation_name')
     24|     expect(appSource).toContain('phone')

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[2/28]ΓÄ»

 FAIL  src/App.oauth.test.jsx > App OAuth and role assignment logic > isApplicantProfileComplete validates all profile fields
AssertionError: expected '// Main app: handles Google OAuth, roΓÇª' to contain 'isApplicantProfileComplete'

- Expected
+ Received

- isApplicantProfileComplete
+ // Main app: handles Google OAuth, role-based routing, and admin dashboard

+ // TRICKY PARTS:

+ // - OAuth flow: Google login ΓåÆ email lookup ΓåÆ role selection (if new user)

+ // - Session persistence: browser cookies restored on page refresh

+ // - Route guards: ProtectedRoute blocks unauthorized access

+ // - State management: tracking auth, loading, roles, and pending signups

+ // - 3D animation: particle effect that reacts to mouse movement

+ 

+ import { useCallback, useEffect, useMemo, useRef, useState } from 'react'

+ import './App.css'

+ import { Routes, Route, useNavigate, useLocation, Navigate } from 'react-router-dom'

+ import Admin from './pages/Admin'

+ import Dashboard from './pages/Dashboard'

+ import ApplicantProfile from './pages/ApplicantProfile'

+ import ApplicantListingDetail from './pages/ApplicantListingDetail'

+ import Provider from './pages/Provider'

+ import ProviderProfile from './pages/ProviderProfile'

+ import ProviderListingForm from './pages/ProviderListingForm'

+ import ProviderListingEdit from './pages/ProviderListingEdit'

+ import ProviderListingApplications from './pages/ProviderListingApplications'

+ import { hasSupabaseConfig, supabase } from './lib/supabaseClient'

+ import {

+   getLandingRoute,

+   getRoleForEmail,

+   getApplicantLandingRouteForEmail,

+   getProviderLandingRouteForEmail,

+ } from './app-helpers'

+ const AdminDashboardShell = Admin

+ 

+ // ProtectedRoute: Guard component that restricts access to role-specific pages

+ // USAGE: <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}><Admin /></ProtectedRoute>

+ // LOGIC:

+ // 1. If still checking auth ΓåÆ show loading shell (don't flash wrong page)

+ // 2. If not signed in ΓåÆ redirect to home (must log in)

+ // 3. If wrong role ΓåÆ redirect to home (prevent unauthorized access)

+ // 4. All checks pass ΓåÆ render the protected content

+ function ProtectedRoute({ role, allowedRole, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   // Show loading state while session is being verified from browser cookies

+   // If we're still loading but already know the user is signed in, render

+   // the protected content so a refresh doesn't lock the user out temporarily.

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   // Unauthenticated users cannot access protected routes

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // Role mismatch: user is logged in but lacks permission for this page

+   // Example: Applicant (role="Applicant") trying to access /admin (allowedRole="Admin")

+   if (role !== allowedRole) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // All security checks passed: render the page

+   return children

+ }

+ 

+ function ProviderWorkspaceRoute({ role, signedIn, isLoading, providerLandingRoute, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (providerLandingRoute !== '/provider') {

+     return <Navigate to="/provider/profile" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function ProviderProfileRoute({ role, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function App() {

+   // Authentication state management:

+   const [role, setRole] = useState(null) // Current user's role: 'Admin', 'Provider', 'Applicant', or null

+   const [signedIn, setSignedIn] = useState(false) // True if OAuth session is active

+   const [isLoadingAuth, setIsLoadingAuth] = useState(true) // True while checking session from browser cookies

+   const [authError, setAuthError] = useState('') // Display auth errors to user

+   const [pendingEmail, setPendingEmail] = useState('') // New user email waiting for role selection

+   const [isSavingRole, setIsSavingRole] = useState(false) // True while inserting user into database

+   const [applicantLandingRoute, setApplicantLandingRoute] = useState('/dashboard')

+   const [providerLandingRoute, setProviderLandingRoute] = useState('/provider/profile')

+   const oauthTimeoutRef = useRef(null)

+ 

+   // Animation state for the 3D particle effect

+   const [pointer, setPointer] = useState({ x: 0, y: 0 })

+   const [isScrolled, setIsScrolled] = useState(false)

+   const navigate = useNavigate()

+   const location = useLocation()

+   const oauthRedirectTo =

+     import.meta.env.VITE_AUTH_REDIRECT_URL?.trim() || `${window.location.origin}/`

+ 

+   const clearOAuthTimeout = useCallback(() => {

+     if (oauthTimeoutRef.current) {

+       window.clearTimeout(oauthTimeoutRef.current)

+       oauthTimeoutRef.current = null

+     }

+   }, [])

+ 

+ // helper functions are implemented in ./app-helpers and imported above

+ 

+   // AUTH BOOTSTRAP: Restore user session on app load and listen for auth changes

+   // FLOW:

+   // 1. Check if session exists in browser cookies (user already logged in before)

+   // 2. Lookup user's role in database by email

+   // 3. Set up listener to detect sign-in/sign-out events

+   // 4. When sign-in happens, role lookup and optional role selection for new users

+   // WHY: Ensures app state matches Supabase auth session, prevents stale data on refresh

+   useEffect(() => {

+     let isMounted = true

+ 

+     const initializeAuth = async () => {

+       setIsLoadingAuth(true)

+ 

+       if (!hasSupabaseConfig) {

+         if (isMounted) {

+           setAuthError('Missing Supabase environment variables. Add VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const { data, error } = await supabase.auth.getSession()

+ 

+       if (error) {

+         if (isMounted) {

+           setAuthError('Unable to restore your session. Please sign in again.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const existingSession = data.session

+ 

+       if (!existingSession?.user?.email) {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(existingSession.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(existingSession.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(existingSession.user.email)

+             : getLandingRoute(resolvedRole)

+         if (isMounted) {

+           setSignedIn(true)

+           setRole(resolvedRole)

+           setPendingEmail(resolvedRole ? '' : existingSession.user.email)

+           setApplicantLandingRoute(resolvedLandingRoute)

+           setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+           setAuthError('')

+         }

+       } catch {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setAuthError('Signed in, but role lookup failed. Check your users table and policies.')

+         }

+       } finally {

+         if (isMounted) {

+           setIsLoadingAuth(false)

+         }

+       }

+     }

+ 

+     initializeAuth()

+ 

+     if (!hasSupabaseConfig) {

+       return () => {

+         isMounted = false

+       }

+     }

+ 

+     const {

+       data: { subscription },

+     } = supabase.auth.onAuthStateChange(async (event, session) => {

+       if (!isMounted) {

+         return

+       }

+ 

+       if (event === 'SIGNED_OUT' || !session?.user?.email) {

+         clearOAuthTimeout()

+         setSignedIn(false)

+         setRole(null)

+         setPendingEmail('')

+         setIsSavingRole(false)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setIsLoadingAuth(false)

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(session.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(session.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(session.user.email)

+             : getLandingRoute(resolvedRole)

+         if (!isMounted) {

+           return

+         }

+         clearOAuthTimeout()

+         setSignedIn(true)

+         setRole(resolvedRole)

+         setPendingEmail(resolvedRole ? '' : session.user.email)

+         setApplicantLandingRoute(resolvedLandingRoute)

+         setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+         setAuthError('')

+       } catch {

+         if (!isMounted) {

+           return

+         }

+         setSignedIn(false)

+         setRole(null)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setAuthError('OAuth succeeded, but role lookup failed. Please verify Supabase table policies.')

+       } finally {

+         if (isMounted) {

+           clearOAuthTimeout()

+           setIsLoadingAuth(false)

+         }

+       }

+     })

+ 

+     return () => {

+       clearOAuthTimeout()

+       isMounted = false

+       subscription.unsubscribe()

+     }

+   }, [clearOAuthTimeout])

+ 

+   // AUTO-REDIRECT: Send logged-in users to their role-appropriate dashboard

+   // WHEN: User has loaded auth state, is signed in, has a role, and is on home page

+   // REDIRECTS: Admin ΓåÆ /admin, Provider ΓåÆ /provider, Applicant ΓåÆ /dashboard

+   useEffect(() => {

+     const redirectedFromProtectedRoute = Boolean(location.state?.from)

+     const keepApplicantOnHome = redirectedFromProtectedRoute && role === 'Applicant'

+     const landingRoute =

+       role === 'Applicant'

+         ? applicantLandingRoute

+         : role === 'Provider'

+           ? providerLandingRoute

+           : getLandingRoute(role)

+ 

+     if (!isLoadingAuth && signedIn && role && location.pathname === '/' && !keepApplicantOnHome) {

+       navigate(landingRoute, { replace: true })

+     }

+   }, [isLoadingAuth, signedIn, role, applicantLandingRoute, providerLandingRoute, location.pathname, location.state, navigate])

+ 

+   // Clear session and reset all state when logging out

+   const handleLogout = async () => {

+     clearOAuthTimeout()

+ 

+     if (hasSupabaseConfig) {

+       await supabase.auth.signOut() // Clear session cookie

+     }

+     setSignedIn(false)

+     setRole(null)

+     setPendingEmail('')

+     setIsSavingRole(false)

+     setApplicantLandingRoute('/dashboard')

+     setProviderLandingRoute('/provider/profile')

+     navigate('/')

+   }

+ 

+   // NEW USER ROLE SELECTION: First-time users must choose their role

+   // WHY: roles (Admin/Provider/Applicant) determine which dashboard they access

+   // PROCESS:

+   // 1. User logs in with Google (new email, not in database yet)

+   // 2. We show role selection popup

+   // 3. User picks role ΓåÆ we insert into 'users' table with their email + role

+   // 4. Next login, step 2-3 are skipped (role already in database)

+   const handleRoleSelection = async (selectedRole) => {

+     if (!pendingEmail) {

+       return

+     }

+ 

+     setIsSavingRole(true)

+     setAuthError('')

+ 

+     const { data: insertedUser, error } = await supabase

+       .from('users')

+       .insert({ email: pendingEmail, role: selectedRole })

+       .select('role')

+       .single()

+ 

+     if (error) {

+       setIsSavingRole(false)

+       setAuthError('Could not save role selection. Please try again.')

+       return

+     }

+ 

+     const resolvedRole = insertedUser?.role || selectedRole

+     setRole(resolvedRole)

+     setPendingEmail('')

+     setIsSavingRole(false)

+ 

+     if (resolvedRole === 'Applicant') {

+       setApplicantLandingRoute('/profile')

+       navigate('/profile', { replace: true })

+       return

+     }

+ 

+     if (resolvedRole === 'Provider') {

+       setProviderLandingRoute('/provider/profile')

+       navigate('/provider/profile', { replace: true })

+       return

+     }

+ 

+     setApplicantLandingRoute(getLandingRoute(resolvedRole))

+     navigate(getLandingRoute(resolvedRole), { replace: true })

+   }

+ 

+   // Generate particle positions for 3D animation (748 dots in 34x22 grid)

+   // Complex math creates radial gradient effect that reacts to mouse movement

+   const dots = useMemo(() => {

+     const result = []

+     const columns = 34

+     const rows = 22

+ 

+     for (let row = 0; row < rows; row += 1) {

+       for (let col = 0; col < columns; col += 1) {

+         const index = row * columns + col

+         const nx = (col - (columns - 1) / 2) / ((columns - 1) / 2)

+         const ny = (row - (rows - 1) / 2) / ((rows - 1) / 2)

+         const r = Math.sqrt(nx * nx + ny * ny)

+         const z = (1 - Math.min(r, 1)) * 110 - Math.abs(nx) * 32

+         const dx = ((col % 5) - 2) * 1.6

+         const dy = ((row % 5) - 2) * 1.4

+         const dz = ((index % 7) - 3) * 2.2

+         const delay = `${(index % 11) * 0.18}s`

+         const duration = `${3.6 + (index % 6) * 0.6}s`

+         result.push({ nx, ny, z, id: `${row}-${col}`, dx, dy, dz, delay, duration })

+       }

+     }

+ 

+     return result

+   }, [])

+ 

+   // Track mouse position for 3D particle effect

+   const handleVisualMove = (event) => {

+     const rect = event.currentTarget.getBoundingClientRect()

+     const px = ((event.clientX - rect.left) / rect.width - 0.5) * 2

+     const py = ((event.clientY - rect.top) / rect.height - 0.5) * 2

+     setPointer({ x: px, y: py })

+   }

+ 

+   const resetVisualMove = () => setPointer({ x: 0, y: 0 })

+ 

+   // OAUTH ENTRY POINT: Initiate Google sign-in

+   // HOW: Calls Supabase.auth.signInWithOAuth() which opens Google login popup

+   // THEN: Auth listener (above) automatically detects successful login and looks up role

+   // NOTE: Don't manually handle role lookup hereΓÇölet the auth listener do it (keeps logic centralized)

+   const handleGoogleContinue = async () => {

+     setAuthError('')

+ 

+     if (!hasSupabaseConfig) {

+       setAuthError('Set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY before using Google login.')

+       return

+     }

+ 

+     setIsLoadingAuth(true)

+ 

+     clearOAuthTimeout()

+     oauthTimeoutRef.current = window.setTimeout(() => {

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in timed out. Please try again.')

+     }, 12000)

+ 

+     const { error } = await supabase.auth.signInWithOAuth({

+       provider: 'google',

+       options: {

+         redirectTo: oauthRedirectTo,

+       },

+     })

+ 

+     if (error) {

+       clearOAuthTimeout()

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in failed. Verify Supabase redirect URLs include your local host and VITE_AUTH_REDIRECT_URL value.')

+     }

+   }

+ 

+   // Fade in sections as they scroll into view

+   useEffect(() => {

+     if (location.pathname !== '/') {

+       return undefined

+     }

+ 

+     const nodes = document.querySelectorAll('.scroll-animate')

+ 

+     if (!nodes.length) {

+       return undefined

+     }

+ 

+     const observer = new IntersectionObserver(

+       (entries) => {

+         entries.forEach((entry) => {

+           if (entry.isIntersecting) {

+             entry.target.classList.add('is-visible') // Add CSS class to trigger fade-in

+           } else {

+             entry.target.classList.remove('is-visible') // Remove class when scrolled past

+           }

+         })

+       },

+       { threshold: 0.18 }, // Element must be 18% visible to trigger

+     )

+ 

+     nodes.forEach((node) => observer.observe(node))

+ 

+     return () => {

+       nodes.forEach((node) => observer.unobserve(node))

+       observer.disconnect()

+     }

+   }, [location.pathname])

+ 

+   // Track if user has scrolled past the hero section (used for topbar styling)

+   useEffect(() => {

+     const onScroll = () => setIsScrolled(window.scrollY > 120)

+     onScroll()

+     window.addEventListener('scroll', onScroll, { passive: true })

+     return () => window.removeEventListener('scroll', onScroll)

+   }, [])

+ 

+   // All routes: home page with auth, and protected role dashboards

+ return (

+   <Routes>

+     <Route path="/" element={

+       <main className="foundry-clone">

+ 

+         <header className={`global-topbar ${isScrolled ? 'is-scrolled' : ''}`}>

+           <div className="topbar-main">

+             <div className="topbar-brand">SA LEARNERSHIP FOUNDRY</div>

+             <nav className="topbar-nav">

+               <a href="#">Why Portal</a>

+               <a href="#">Pathways</a>

+               <a href="#">Team</a>

+               <a href="#">Insights</a>

+               <a href="#">Contact</a>

+             </nav>

+           </div>

+ 

+           <div className="topbar-auth">

+             {signedIn && role ? <span className="role-display">{role}</span> : null}

+             <button className="google-auth" onClick={signedIn ? handleLogout : handleGoogleContinue}>

+               {isLoadingAuth ? 'Authenticating...' : signedIn ? 'Logout' : 'Log In with Google'}

+             </button>

+           </div>

+         </header>

+ 

+         <section className="page-one">

+           <div className="hero-main">

+             <h1 className="left-title">

+               BUILDING TALENT

+               <br />

+               PATHWAYS THAT

+             </h1>

+ 

+             <div

+               className="interactive-model"

+               onMouseMove={handleVisualMove}

+               onMouseLeave={resetVisualMove}

+               style={{ '--px': pointer.x, '--py': pointer.y }}

+             >

+               <div className="orbital-core">

+                 <span className="orbit orbit-a"></span>

+                 <span className="orbit orbit-b"></span>

+                 <span className="orbit orbit-c"></span>

+               </div>

+ 

+               <div className="dot-volume">

+                 {dots.map((dot) => (

+                   <span

+                     key={dot.id}

+                     className="dot"

+                     style={{

+                       '--x': `${dot.nx * 340}px`,

+                       '--y': `${dot.ny * 240}px`,

+                       '--z': `${dot.z}px`,

+                     }}

+                   />

+                 ))}

+               </div>

+             </div>

+ 

+             <h1 className="right-title">

+               PUSH THE

+               <br />

+               FUTURE FORWARD

+             </h1>

+           </div>

+ 

+           <div className="hero-bottom">

+             <p>

+               A venture lab style platform for learnership access, application intelligence,

+               and practical support that moves candidates from skill to real work.

+             </p>

+ 

+             <div className="status-pill">

+               {signedIn && role

+                 ? `${role} workspace unlocked`

+                 : signedIn && pendingEmail

+                   ? 'Choose your role to continue'

+                   : 'Sign in with Google to continue'}

+             </div>

+           </div>

+           {signedIn && !role && pendingEmail ? (

+             <div className="role-onboarding-overlay" role="dialog" aria-modal="true" aria-labelledby="role-onboarding-title">

+               <div className="role-onboarding">

+                 <p className="role-onboarding-eyebrow">One more step</p>

+                 <h2 id="role-onboarding-title">Choose your role</h2>

+                 <p>Select how you want to use the platform for {pendingEmail}.</p>

+                 <div className="role-select">

+                   <button onClick={() => handleRoleSelection('Applicant')} disabled={isSavingRole}>

+                     Applicant

+                   </button>

+                   <button onClick={() => handleRoleSelection('Provider')} disabled={isSavingRole}>

+                     Provider

+                   </button>

+                 </div>

+               </div>

+             </div>

+           ) : null}

+           {authError ? <p className="auth-error">{authError}</p> : null}

+         </section>

+ 

+         <section className="page-two">

+           <div className="ethos-row scroll-animate">

+             <div>

+               <p className="mini-label">Why this platform</p>

+               <h2>

+                 OPEN DOORS

+                 <br />

+                 TO REAL WORK

+               </h2>

+             </div>

+ 

+             <div className="ethos-copy">

+               <p>

+                 Learnership Foundry helps applicants, providers, and moderators collaborate in one

+                 flow. Discover pathways, publish opportunities, and keep quality high with

+                 visibility from intake to placement.

+               </p>

+             </div>

+           </div>

+ 

+           <div className="card-strip scroll-animate swipe-left">

+             <article className="signal-card tone-dark">

+               <h3>Discover</h3>

+               <p>APPLICANT VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Find learnerships that match your interests and readiness level.</small>

+             </article>

+ 

+             <article className="signal-card tone-cyan">

+               <h3>Publish</h3>

+               <p>PROVIDER VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Create listings and track engagement from qualified candidates.</small>

+             </article>

+ 

+             <article className="signal-card tone-orange">

+               <h3>Moderate</h3>

+               <p>ADMIN VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Flag risk, request fixes, and approve opportunities confidently.</small>

+             </article>

+ 

+             <article className="signal-card tone-light">

+               <h3>Move</h3>

+               <p>OUTCOMES</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Turn applications into skills, credentials, and workplace entry points.</small>

+             </article>

+           </div>

+         </section>

+ 

+         <section className="page-three">

+           <span className="dot-plane" aria-hidden="true"></span>

+           <div className="focus-copy scroll-animate">

+             <p className="mini-label">Built for momentum</p>

+             <h2>

+               From FIRST CLICK

+               <br />

+               TO FIRST PAYCHECK

+             </h2>

+             <p>

+               Scroll the platform, choose your role, and move into the right workspace to continue.

+             </p>

+           </div>

+         </section>

+ 

+       </main>

+     } />

+ 

+     

+    <Route path="/dashboard" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <Dashboard onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+ <Route path="/dashboard/listings/:listingId" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ApplicantListingDetail onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   <Route path="/profile" element={

+     <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+       <ApplicantProfile onLogout={handleLogout} />

+     </ProtectedRoute>

+   } />

+ 

+ <Route path="/provider" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <Provider onLogout={handleLogout} />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/profile" element={

+   <ProviderProfileRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ProviderProfile

+       onLogout={handleLogout}

+       onProfileSaved={() => setProviderLandingRoute('/provider')}

+     />

+   </ProviderProfileRoute>

+ } />

+ 

+ <Route path="/provider/listings/new" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingForm />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/edit" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingEdit />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/applications" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingApplications />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/admin" element={

+   <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <AdminDashboardShell onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   </Routes>

+ )

+ }

+ 

+ export default App

+

 Γ¥» src/App.oauth.test.jsx:31:23
     29|     const appSource = readFileSync(resolve(cwd(), 'src/App.jsx'), 'utfΓÇª
     30|
     31|     expect(appSource).toContain('isApplicantProfileComplete')
       |                       ^
     32|     expect(appSource).toContain('first_name')
     33|     expect(appSource).toContain('last_name')
ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[3/28]ΓÄ»

 FAIL  src/App.oauth.test.jsx > App OAuth and role assignment logic > Configured admin emails fallback to defaults
AssertionError: expected '// Main app: handles Google OAuth, roΓÇª' to contain 'getConfiguredAdminEmails'

- Expected
+ Received

- getConfiguredAdminEmails
+ // Main app: handles Google OAuth, role-based routing, and admin dashboard

+ // TRICKY PARTS:

+ // - OAuth flow: Google login ΓåÆ email lookup ΓåÆ role selection (if new user)

+ // - Session persistence: browser cookies restored on page refresh

+ // - Route guards: ProtectedRoute blocks unauthorized access

+ // - State management: tracking auth, loading, roles, and pending signups

+ // - 3D animation: particle effect that reacts to mouse movement

+ 

+ import { useCallback, useEffect, useMemo, useRef, useState } from 'react'

+ import './App.css'

+ import { Routes, Route, useNavigate, useLocation, Navigate } from 'react-router-dom'

+ import Admin from './pages/Admin'

+ import Dashboard from './pages/Dashboard'

+ import ApplicantProfile from './pages/ApplicantProfile'

+ import ApplicantListingDetail from './pages/ApplicantListingDetail'

+ import Provider from './pages/Provider'

+ import ProviderProfile from './pages/ProviderProfile'

+ import ProviderListingForm from './pages/ProviderListingForm'

+ import ProviderListingEdit from './pages/ProviderListingEdit'

+ import ProviderListingApplications from './pages/ProviderListingApplications'

+ import { hasSupabaseConfig, supabase } from './lib/supabaseClient'

+ import {

+   getLandingRoute,

+   getRoleForEmail,

+   getApplicantLandingRouteForEmail,

+   getProviderLandingRouteForEmail,

+ } from './app-helpers'

+ const AdminDashboardShell = Admin

+ 

+ // ProtectedRoute: Guard component that restricts access to role-specific pages

+ // USAGE: <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}><Admin /></ProtectedRoute>

+ // LOGIC:

+ // 1. If still checking auth ΓåÆ show loading shell (don't flash wrong page)

+ // 2. If not signed in ΓåÆ redirect to home (must log in)

+ // 3. If wrong role ΓåÆ redirect to home (prevent unauthorized access)

+ // 4. All checks pass ΓåÆ render the protected content

+ function ProtectedRoute({ role, allowedRole, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   // Show loading state while session is being verified from browser cookies

+   // If we're still loading but already know the user is signed in, render

+   // the protected content so a refresh doesn't lock the user out temporarily.

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   // Unauthenticated users cannot access protected routes

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // Role mismatch: user is logged in but lacks permission for this page

+   // Example: Applicant (role="Applicant") trying to access /admin (allowedRole="Admin")

+   if (role !== allowedRole) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // All security checks passed: render the page

+   return children

+ }

+ 

+ function ProviderWorkspaceRoute({ role, signedIn, isLoading, providerLandingRoute, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (providerLandingRoute !== '/provider') {

+     return <Navigate to="/provider/profile" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function ProviderProfileRoute({ role, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function App() {

+   // Authentication state management:

+   const [role, setRole] = useState(null) // Current user's role: 'Admin', 'Provider', 'Applicant', or null

+   const [signedIn, setSignedIn] = useState(false) // True if OAuth session is active

+   const [isLoadingAuth, setIsLoadingAuth] = useState(true) // True while checking session from browser cookies

+   const [authError, setAuthError] = useState('') // Display auth errors to user

+   const [pendingEmail, setPendingEmail] = useState('') // New user email waiting for role selection

+   const [isSavingRole, setIsSavingRole] = useState(false) // True while inserting user into database

+   const [applicantLandingRoute, setApplicantLandingRoute] = useState('/dashboard')

+   const [providerLandingRoute, setProviderLandingRoute] = useState('/provider/profile')

+   const oauthTimeoutRef = useRef(null)

+ 

+   // Animation state for the 3D particle effect

+   const [pointer, setPointer] = useState({ x: 0, y: 0 })

+   const [isScrolled, setIsScrolled] = useState(false)

+   const navigate = useNavigate()

+   const location = useLocation()

+   const oauthRedirectTo =

+     import.meta.env.VITE_AUTH_REDIRECT_URL?.trim() || `${window.location.origin}/`

+ 

+   const clearOAuthTimeout = useCallback(() => {

+     if (oauthTimeoutRef.current) {

+       window.clearTimeout(oauthTimeoutRef.current)

+       oauthTimeoutRef.current = null

+     }

+   }, [])

+ 

+ // helper functions are implemented in ./app-helpers and imported above

+ 

+   // AUTH BOOTSTRAP: Restore user session on app load and listen for auth changes

+   // FLOW:

+   // 1. Check if session exists in browser cookies (user already logged in before)

+   // 2. Lookup user's role in database by email

+   // 3. Set up listener to detect sign-in/sign-out events

+   // 4. When sign-in happens, role lookup and optional role selection for new users

+   // WHY: Ensures app state matches Supabase auth session, prevents stale data on refresh

+   useEffect(() => {

+     let isMounted = true

+ 

+     const initializeAuth = async () => {

+       setIsLoadingAuth(true)

+ 

+       if (!hasSupabaseConfig) {

+         if (isMounted) {

+           setAuthError('Missing Supabase environment variables. Add VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const { data, error } = await supabase.auth.getSession()

+ 

+       if (error) {

+         if (isMounted) {

+           setAuthError('Unable to restore your session. Please sign in again.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const existingSession = data.session

+ 

+       if (!existingSession?.user?.email) {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(existingSession.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(existingSession.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(existingSession.user.email)

+             : getLandingRoute(resolvedRole)

+         if (isMounted) {

+           setSignedIn(true)

+           setRole(resolvedRole)

+           setPendingEmail(resolvedRole ? '' : existingSession.user.email)

+           setApplicantLandingRoute(resolvedLandingRoute)

+           setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+           setAuthError('')

+         }

+       } catch {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setAuthError('Signed in, but role lookup failed. Check your users table and policies.')

+         }

+       } finally {

+         if (isMounted) {

+           setIsLoadingAuth(false)

+         }

+       }

+     }

+ 

+     initializeAuth()

+ 

+     if (!hasSupabaseConfig) {

+       return () => {

+         isMounted = false

+       }

+     }

+ 

+     const {

+       data: { subscription },

+     } = supabase.auth.onAuthStateChange(async (event, session) => {

+       if (!isMounted) {

+         return

+       }

+ 

+       if (event === 'SIGNED_OUT' || !session?.user?.email) {

+         clearOAuthTimeout()

+         setSignedIn(false)

+         setRole(null)

+         setPendingEmail('')

+         setIsSavingRole(false)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setIsLoadingAuth(false)

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(session.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(session.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(session.user.email)

+             : getLandingRoute(resolvedRole)

+         if (!isMounted) {

+           return

+         }

+         clearOAuthTimeout()

+         setSignedIn(true)

+         setRole(resolvedRole)

+         setPendingEmail(resolvedRole ? '' : session.user.email)

+         setApplicantLandingRoute(resolvedLandingRoute)

+         setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+         setAuthError('')

+       } catch {

+         if (!isMounted) {

+           return

+         }

+         setSignedIn(false)

+         setRole(null)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setAuthError('OAuth succeeded, but role lookup failed. Please verify Supabase table policies.')

+       } finally {

+         if (isMounted) {

+           clearOAuthTimeout()

+           setIsLoadingAuth(false)

+         }

+       }

+     })

+ 

+     return () => {

+       clearOAuthTimeout()

+       isMounted = false

+       subscription.unsubscribe()

+     }

+   }, [clearOAuthTimeout])

+ 

+   // AUTO-REDIRECT: Send logged-in users to their role-appropriate dashboard

+   // WHEN: User has loaded auth state, is signed in, has a role, and is on home page

+   // REDIRECTS: Admin ΓåÆ /admin, Provider ΓåÆ /provider, Applicant ΓåÆ /dashboard

+   useEffect(() => {

+     const redirectedFromProtectedRoute = Boolean(location.state?.from)

+     const keepApplicantOnHome = redirectedFromProtectedRoute && role === 'Applicant'

+     const landingRoute =

+       role === 'Applicant'

+         ? applicantLandingRoute

+         : role === 'Provider'

+           ? providerLandingRoute

+           : getLandingRoute(role)

+ 

+     if (!isLoadingAuth && signedIn && role && location.pathname === '/' && !keepApplicantOnHome) {

+       navigate(landingRoute, { replace: true })

+     }

+   }, [isLoadingAuth, signedIn, role, applicantLandingRoute, providerLandingRoute, location.pathname, location.state, navigate])

+ 

+   // Clear session and reset all state when logging out

+   const handleLogout = async () => {

+     clearOAuthTimeout()

+ 

+     if (hasSupabaseConfig) {

+       await supabase.auth.signOut() // Clear session cookie

+     }

+     setSignedIn(false)

+     setRole(null)

+     setPendingEmail('')

+     setIsSavingRole(false)

+     setApplicantLandingRoute('/dashboard')

+     setProviderLandingRoute('/provider/profile')

+     navigate('/')

+   }

+ 

+   // NEW USER ROLE SELECTION: First-time users must choose their role

+   // WHY: roles (Admin/Provider/Applicant) determine which dashboard they access

+   // PROCESS:

+   // 1. User logs in with Google (new email, not in database yet)

+   // 2. We show role selection popup

+   // 3. User picks role ΓåÆ we insert into 'users' table with their email + role

+   // 4. Next login, step 2-3 are skipped (role already in database)

+   const handleRoleSelection = async (selectedRole) => {

+     if (!pendingEmail) {

+       return

+     }

+ 

+     setIsSavingRole(true)

+     setAuthError('')

+ 

+     const { data: insertedUser, error } = await supabase

+       .from('users')

+       .insert({ email: pendingEmail, role: selectedRole })

+       .select('role')

+       .single()

+ 

+     if (error) {

+       setIsSavingRole(false)

+       setAuthError('Could not save role selection. Please try again.')

+       return

+     }

+ 

+     const resolvedRole = insertedUser?.role || selectedRole

+     setRole(resolvedRole)

+     setPendingEmail('')

+     setIsSavingRole(false)

+ 

+     if (resolvedRole === 'Applicant') {

+       setApplicantLandingRoute('/profile')

+       navigate('/profile', { replace: true })

+       return

+     }

+ 

+     if (resolvedRole === 'Provider') {

+       setProviderLandingRoute('/provider/profile')

+       navigate('/provider/profile', { replace: true })

+       return

+     }

+ 

+     setApplicantLandingRoute(getLandingRoute(resolvedRole))

+     navigate(getLandingRoute(resolvedRole), { replace: true })

+   }

+ 

+   // Generate particle positions for 3D animation (748 dots in 34x22 grid)

+   // Complex math creates radial gradient effect that reacts to mouse movement

+   const dots = useMemo(() => {

+     const result = []

+     const columns = 34

+     const rows = 22

+ 

+     for (let row = 0; row < rows; row += 1) {

+       for (let col = 0; col < columns; col += 1) {

+         const index = row * columns + col

+         const nx = (col - (columns - 1) / 2) / ((columns - 1) / 2)

+         const ny = (row - (rows - 1) / 2) / ((rows - 1) / 2)

+         const r = Math.sqrt(nx * nx + ny * ny)

+         const z = (1 - Math.min(r, 1)) * 110 - Math.abs(nx) * 32

+         const dx = ((col % 5) - 2) * 1.6

+         const dy = ((row % 5) - 2) * 1.4

+         const dz = ((index % 7) - 3) * 2.2

+         const delay = `${(index % 11) * 0.18}s`

+         const duration = `${3.6 + (index % 6) * 0.6}s`

+         result.push({ nx, ny, z, id: `${row}-${col}`, dx, dy, dz, delay, duration })

+       }

+     }

+ 

+     return result

+   }, [])

+ 

+   // Track mouse position for 3D particle effect

+   const handleVisualMove = (event) => {

+     const rect = event.currentTarget.getBoundingClientRect()

+     const px = ((event.clientX - rect.left) / rect.width - 0.5) * 2

+     const py = ((event.clientY - rect.top) / rect.height - 0.5) * 2

+     setPointer({ x: px, y: py })

+   }

+ 

+   const resetVisualMove = () => setPointer({ x: 0, y: 0 })

+ 

+   // OAUTH ENTRY POINT: Initiate Google sign-in

+   // HOW: Calls Supabase.auth.signInWithOAuth() which opens Google login popup

+   // THEN: Auth listener (above) automatically detects successful login and looks up role

+   // NOTE: Don't manually handle role lookup hereΓÇölet the auth listener do it (keeps logic centralized)

+   const handleGoogleContinue = async () => {

+     setAuthError('')

+ 

+     if (!hasSupabaseConfig) {

+       setAuthError('Set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY before using Google login.')

+       return

+     }

+ 

+     setIsLoadingAuth(true)

+ 

+     clearOAuthTimeout()

+     oauthTimeoutRef.current = window.setTimeout(() => {

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in timed out. Please try again.')

+     }, 12000)

+ 

+     const { error } = await supabase.auth.signInWithOAuth({

+       provider: 'google',

+       options: {

+         redirectTo: oauthRedirectTo,

+       },

+     })

+ 

+     if (error) {

+       clearOAuthTimeout()

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in failed. Verify Supabase redirect URLs include your local host and VITE_AUTH_REDIRECT_URL value.')

+     }

+   }

+ 

+   // Fade in sections as they scroll into view

+   useEffect(() => {

+     if (location.pathname !== '/') {

+       return undefined

+     }

+ 

+     const nodes = document.querySelectorAll('.scroll-animate')

+ 

+     if (!nodes.length) {

+       return undefined

+     }

+ 

+     const observer = new IntersectionObserver(

+       (entries) => {

+         entries.forEach((entry) => {

+           if (entry.isIntersecting) {

+             entry.target.classList.add('is-visible') // Add CSS class to trigger fade-in

+           } else {

+             entry.target.classList.remove('is-visible') // Remove class when scrolled past

+           }

+         })

+       },

+       { threshold: 0.18 }, // Element must be 18% visible to trigger

+     )

+ 

+     nodes.forEach((node) => observer.observe(node))

+ 

+     return () => {

+       nodes.forEach((node) => observer.unobserve(node))

+       observer.disconnect()

+     }

+   }, [location.pathname])

+ 

+   // Track if user has scrolled past the hero section (used for topbar styling)

+   useEffect(() => {

+     const onScroll = () => setIsScrolled(window.scrollY > 120)

+     onScroll()

+     window.addEventListener('scroll', onScroll, { passive: true })

+     return () => window.removeEventListener('scroll', onScroll)

+   }, [])

+ 

+   // All routes: home page with auth, and protected role dashboards

+ return (

+   <Routes>

+     <Route path="/" element={

+       <main className="foundry-clone">

+ 

+         <header className={`global-topbar ${isScrolled ? 'is-scrolled' : ''}`}>

+           <div className="topbar-main">

+             <div className="topbar-brand">SA LEARNERSHIP FOUNDRY</div>

+             <nav className="topbar-nav">

+               <a href="#">Why Portal</a>

+               <a href="#">Pathways</a>

+               <a href="#">Team</a>

+               <a href="#">Insights</a>

+               <a href="#">Contact</a>

+             </nav>

+           </div>

+ 

+           <div className="topbar-auth">

+             {signedIn && role ? <span className="role-display">{role}</span> : null}

+             <button className="google-auth" onClick={signedIn ? handleLogout : handleGoogleContinue}>

+               {isLoadingAuth ? 'Authenticating...' : signedIn ? 'Logout' : 'Log In with Google'}

+             </button>

+           </div>

+         </header>

+ 

+         <section className="page-one">

+           <div className="hero-main">

+             <h1 className="left-title">

+               BUILDING TALENT

+               <br />

+               PATHWAYS THAT

+             </h1>

+ 

+             <div

+               className="interactive-model"

+               onMouseMove={handleVisualMove}

+               onMouseLeave={resetVisualMove}

+               style={{ '--px': pointer.x, '--py': pointer.y }}

+             >

+               <div className="orbital-core">

+                 <span className="orbit orbit-a"></span>

+                 <span className="orbit orbit-b"></span>

+                 <span className="orbit orbit-c"></span>

+               </div>

+ 

+               <div className="dot-volume">

+                 {dots.map((dot) => (

+                   <span

+                     key={dot.id}

+                     className="dot"

+                     style={{

+                       '--x': `${dot.nx * 340}px`,

+                       '--y': `${dot.ny * 240}px`,

+                       '--z': `${dot.z}px`,

+                     }}

+                   />

+                 ))}

+               </div>

+             </div>

+ 

+             <h1 className="right-title">

+               PUSH THE

+               <br />

+               FUTURE FORWARD

+             </h1>

+           </div>

+ 

+           <div className="hero-bottom">

+             <p>

+               A venture lab style platform for learnership access, application intelligence,

+               and practical support that moves candidates from skill to real work.

+             </p>

+ 

+             <div className="status-pill">

+               {signedIn && role

+                 ? `${role} workspace unlocked`

+                 : signedIn && pendingEmail

+                   ? 'Choose your role to continue'

+                   : 'Sign in with Google to continue'}

+             </div>

+           </div>

+           {signedIn && !role && pendingEmail ? (

+             <div className="role-onboarding-overlay" role="dialog" aria-modal="true" aria-labelledby="role-onboarding-title">

+               <div className="role-onboarding">

+                 <p className="role-onboarding-eyebrow">One more step</p>

+                 <h2 id="role-onboarding-title">Choose your role</h2>

+                 <p>Select how you want to use the platform for {pendingEmail}.</p>

+                 <div className="role-select">

+                   <button onClick={() => handleRoleSelection('Applicant')} disabled={isSavingRole}>

+                     Applicant

+                   </button>

+                   <button onClick={() => handleRoleSelection('Provider')} disabled={isSavingRole}>

+                     Provider

+                   </button>

+                 </div>

+               </div>

+             </div>

+           ) : null}

+           {authError ? <p className="auth-error">{authError}</p> : null}

+         </section>

+ 

+         <section className="page-two">

+           <div className="ethos-row scroll-animate">

+             <div>

+               <p className="mini-label">Why this platform</p>

+               <h2>

+                 OPEN DOORS

+                 <br />

+                 TO REAL WORK

+               </h2>

+             </div>

+ 

+             <div className="ethos-copy">

+               <p>

+                 Learnership Foundry helps applicants, providers, and moderators collaborate in one

+                 flow. Discover pathways, publish opportunities, and keep quality high with

+                 visibility from intake to placement.

+               </p>

+             </div>

+           </div>

+ 

+           <div className="card-strip scroll-animate swipe-left">

+             <article className="signal-card tone-dark">

+               <h3>Discover</h3>

+               <p>APPLICANT VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Find learnerships that match your interests and readiness level.</small>

+             </article>

+ 

+             <article className="signal-card tone-cyan">

+               <h3>Publish</h3>

+               <p>PROVIDER VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Create listings and track engagement from qualified candidates.</small>

+             </article>

+ 

+             <article className="signal-card tone-orange">

+               <h3>Moderate</h3>

+               <p>ADMIN VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Flag risk, request fixes, and approve opportunities confidently.</small>

+             </article>

+ 

+             <article className="signal-card tone-light">

+               <h3>Move</h3>

+               <p>OUTCOMES</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Turn applications into skills, credentials, and workplace entry points.</small>

+             </article>

+           </div>

+         </section>

+ 

+         <section className="page-three">

+           <span className="dot-plane" aria-hidden="true"></span>

+           <div className="focus-copy scroll-animate">

+             <p className="mini-label">Built for momentum</p>

+             <h2>

+               From FIRST CLICK

+               <br />

+               TO FIRST PAYCHECK

+             </h2>

+             <p>

+               Scroll the platform, choose your role, and move into the right workspace to continue.

+             </p>

+           </div>

+         </section>

+ 

+       </main>

+     } />

+ 

+     

+    <Route path="/dashboard" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <Dashboard onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+ <Route path="/dashboard/listings/:listingId" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ApplicantListingDetail onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   <Route path="/profile" element={

+     <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+       <ApplicantProfile onLogout={handleLogout} />

+     </ProtectedRoute>

+   } />

+ 

+ <Route path="/provider" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <Provider onLogout={handleLogout} />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/profile" element={

+   <ProviderProfileRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ProviderProfile

+       onLogout={handleLogout}

+       onProfileSaved={() => setProviderLandingRoute('/provider')}

+     />

+   </ProviderProfileRoute>

+ } />

+ 

+ <Route path="/provider/listings/new" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingForm />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/edit" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingEdit />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/applications" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingApplications />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/admin" element={

+   <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <AdminDashboardShell onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   </Routes>

+ )

+ }

+ 

+ export default App

+

 Γ¥» src/App.oauth.test.jsx:103:23
    101|     const appSource = readFileSync(resolve(cwd(), 'src/App.jsx'), 'utfΓÇª
    102|
    103|     expect(appSource).toContain('getConfiguredAdminEmails')
       |                       ^
    104|     expect(appSource).toContain('VITE_ADMIN_EMAILS')
    105|     expect(appSource).toContain('.split(')

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[4/28]ΓÄ»

 FAIL  src/App.pure.test.jsx > App pure helpers > getLandingRoute returns correct paths
TypeError: getLandingRoute is not a function
 Γ¥» src/App.pure.test.jsx:12:12
     10| describe('App pure helpers', () => {
     11|   it('getLandingRoute returns correct paths', () => {
     12|     expect(getLandingRoute('Admin')).toBe('/admin')
       |            ^
     13|     expect(getLandingRoute('Provider')).toBe('/provider')
     14|     expect(getLandingRoute('Applicant')).toBe('/dashboard')

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[5/28]ΓÄ»

 FAIL  src/App.pure.test.jsx > App pure helpers > isProviderProfileComplete validation
TypeError: isProviderProfileComplete is not a function
 Γ¥» src/App.pure.test.jsx:21:12
     19|     const good = { id: 1, organisation_name: 'Org', phone: '123', descΓÇª
     20|     const bad = { id: 1, organisation_name: '', phone: '123', descriptΓÇª
     21|     expect(isProviderProfileComplete(good)).toBeTruthy()
       |            ^
     22|     expect(isProviderProfileComplete(bad)).toBeFalsy()
     23|   })

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[6/28]ΓÄ»

 FAIL  src/App.pure.test.jsx > App pure helpers > getProviderLandingRoute returns correct route
TypeError: getProviderLandingRoute is not a function
 Γ¥» src/App.pure.test.jsx:28:12
     26|     const complete = { id: 1, organisation_name: 'Org', phone: '123', ΓÇª
     27|     const incomplete = { id: 1 }
     28|     expect(getProviderLandingRoute(complete)).toBe('/provider')
       |            ^
     29|     expect(getProviderLandingRoute(incomplete)).toBe('/provider/profilΓÇª
     30|   })

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[7/28]ΓÄ»

 FAIL  src/App.pure.test.jsx > App pure helpers > isApplicantProfileComplete validation
TypeError: isApplicantProfileComplete is not a function
 Γ¥» src/App.pure.test.jsx:44:12
     42|     }
     43|     const bad = { id: 1, first_name: '', last_name: 'B' }
     44|     expect(isApplicantProfileComplete(good)).toBeTruthy()
       |            ^
     45|     expect(isApplicantProfileComplete(bad)).toBeFalsy()
     46|   })

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[8/28]ΓÄ»

 FAIL  src/App.pure.test.jsx > App pure helpers > getConfiguredAdminEmails returns default list when unset
TypeError: getConfiguredAdminEmails is not a function
 Γ¥» src/App.pure.test.jsx:49:20
     47|
     48|   it('getConfiguredAdminEmails returns default list when unset', () =>ΓÇª
     49|     const emails = getConfiguredAdminEmails()
       |                    ^
     50|     expect(Array.isArray(emails)).toBeTruthy()
     51|     expect(emails.length).toBeGreaterThan(0)

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[9/28]ΓÄ»

 FAIL  src/App.routes.test.jsx > Route guard components > ProtectedRoute shows loading shell when loading and not signed in
 FAIL  src/App.routes.test.jsx > Route guard components > ProtectedRoute redirects unauthenticated users to home
 FAIL  src/App.routes.test.jsx > Route guard components > ProtectedRoute redirects wrong role users to home
 FAIL  src/App.routes.test.jsx > Route guard components > ProtectedRoute allows correct role to render children
 FAIL  src/App.routes.test.jsx > Route guard components > ProviderWorkspaceRoute redirects provider without correct landing route
 FAIL  src/App.routes.test.jsx > Route guard components > ProviderProfileRoute blocks non-provider users
Error: Element type is invalid: expected a string (for built-in components) or a class/function (for composite components) but got: undefined. You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports.
 Γ¥» createFiberFromTypeAndProps node_modules/react-dom/cjs/react-dom-client.development.js:5021:28
 Γ¥» createFiberFromElement node_modules/react-dom/cjs/react-dom-client.development.js:5035:14
 Γ¥» reconcileChildFibersImpl node_modules/react-dom/cjs/react-dom-client.development.js:6920:31
 Γ¥» node_modules/react-dom/cjs/react-dom-client.development.js:7098:33
 Γ¥» reconcileChildren node_modules/react-dom/cjs/react-dom-client.development.js:9701:13
 Γ¥» beginWork node_modules/react-dom/cjs/react-dom-client.development.js:12171:13
 Γ¥» runWithFiberInDEV node_modules/react-dom/cjs/react-dom-client.development.js:874:13
 Γ¥» performUnitOfWork node_modules/react-dom/cjs/react-dom-client.development.js:17641:22
 Γ¥» workLoopSync node_modules/react-dom/cjs/react-dom-client.development.js:17469:41
 Γ¥» renderRootSync node_modules/react-dom/cjs/react-dom-client.development.js:17450:11

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[10/28]ΓÄ»

 FAIL  src/App.supabase.test.jsx > App supabase helpers > getRoleForEmail returns role when user exists
TypeError: getRoleForEmail is not a function
 Γ¥» src/App.supabase.test.jsx:27:28
     25|   it('getRoleForEmail returns role when user exists', async () => {
     26|     mockResponses.users = { data: { role: 'Provider' }, error: null }
     27|     const role = await App.getRoleForEmail('someone@test.com')
       |                            ^
     28|     expect(role).toBe('Provider')
     29|   })

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[11/28]ΓÄ»

 FAIL  src/App.supabase.test.jsx > App supabase helpers > getRoleForEmail throws when fetch error present
TypeError: getRoleForEmail is not a function
 Γ¥» src/App.supabase.test.jsx:33:22
     31|   it('getRoleForEmail throws when fetch error present', async () => {
     32|     mockResponses.users = { data: null, error: new Error('db') }
     33|     await expect(App.getRoleForEmail('fail@test.com')).rejects.toThrowΓÇª
       |                      ^
     34|   })
     35|

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[12/28]ΓÄ»

 FAIL  src/App.supabase.test.jsx > App supabase helpers > getApplicantLandingRouteForEmail returns /profile when no user row
TypeError: getApplicantLandingRouteForEmail is not a function
 Γ¥» src/App.supabase.test.jsx:38:29
     36|   it('getApplicantLandingRouteForEmail returns /profile when no user rΓÇª
     37|     mockResponses.users = { data: null }
     38|     const route = await App.getApplicantLandingRouteForEmail('nouser@tΓÇª
       |                             ^
     39|     expect(route).toBe('/profile')
     40|   })

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[13/28]ΓÄ»

 FAIL  src/App.supabase.test.jsx > App supabase helpers > getApplicantLandingRouteForEmail returns /dashboard when profile complete
TypeError: getApplicantLandingRouteForEmail is not a function
 Γ¥» src/App.supabase.test.jsx:56:29
     54|       },
     55|     }
     56|     const route = await App.getApplicantLandingRouteForEmail('ok@test.ΓÇª
       |                             ^
     57|     expect(route).toBe('/dashboard')
     58|   })

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[14/28]ΓÄ»

 FAIL  src/App.supabase.test.jsx > App supabase helpers > getProviderLandingRouteForEmail returns /provider/profile when no user
TypeError: getProviderLandingRouteForEmail is not a function
 Γ¥» src/App.supabase.test.jsx:62:29
     60|   it('getProviderLandingRouteForEmail returns /provider/profile when nΓÇª
     61|     mockResponses.users = { data: null }
     62|     const route = await App.getProviderLandingRouteForEmail('nouser2@tΓÇª
       |                             ^
     63|     expect(route).toBe('/provider/profile')
     64|   })

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[15/28]ΓÄ»

 FAIL  src/App.supabase.test.jsx > App supabase helpers > getProviderLandingRouteForEmail returns provider route when profile exists
TypeError: getProviderLandingRouteForEmail is not a function
 Γ¥» src/App.supabase.test.jsx:69:29
     67|     mockResponses.users = { data: { id: 7 } }
     68|     mockResponses.provider_profiles = { data: { id: 1, organisation_naΓÇª
     69|     const route = await App.getProviderLandingRouteForEmail('prov@testΓÇª
       |                             ^
     70|     expect(route).toBe('/provider')
     71|   })

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[16/28]ΓÄ»

 FAIL  src/App.test.jsx > Admin tests > Admin lands on /admin after login
AssertionError: expected '// Main app: handles Google OAuth, roΓÇª' to contain 'if (role === \'Admin\') return \'/admΓÇª'

- Expected
+ Received

- if (role === 'Admin') return '/admin'
+ // Main app: handles Google OAuth, role-based routing, and admin dashboard

+ // TRICKY PARTS:

+ // - OAuth flow: Google login ΓåÆ email lookup ΓåÆ role selection (if new user)

+ // - Session persistence: browser cookies restored on page refresh

+ // - Route guards: ProtectedRoute blocks unauthorized access

+ // - State management: tracking auth, loading, roles, and pending signups

+ // - 3D animation: particle effect that reacts to mouse movement

+ 

+ import { useCallback, useEffect, useMemo, useRef, useState } from 'react'

+ import './App.css'

+ import { Routes, Route, useNavigate, useLocation, Navigate } from 'react-router-dom'

+ import Admin from './pages/Admin'

+ import Dashboard from './pages/Dashboard'

+ import ApplicantProfile from './pages/ApplicantProfile'

+ import ApplicantListingDetail from './pages/ApplicantListingDetail'

+ import Provider from './pages/Provider'

+ import ProviderProfile from './pages/ProviderProfile'

+ import ProviderListingForm from './pages/ProviderListingForm'

+ import ProviderListingEdit from './pages/ProviderListingEdit'

+ import ProviderListingApplications from './pages/ProviderListingApplications'

+ import { hasSupabaseConfig, supabase } from './lib/supabaseClient'

+ import {

+   getLandingRoute,

+   getRoleForEmail,

+   getApplicantLandingRouteForEmail,

+   getProviderLandingRouteForEmail,

+ } from './app-helpers'

+ const AdminDashboardShell = Admin

+ 

+ // ProtectedRoute: Guard component that restricts access to role-specific pages

+ // USAGE: <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}><Admin /></ProtectedRoute>

+ // LOGIC:

+ // 1. If still checking auth ΓåÆ show loading shell (don't flash wrong page)

+ // 2. If not signed in ΓåÆ redirect to home (must log in)

+ // 3. If wrong role ΓåÆ redirect to home (prevent unauthorized access)

+ // 4. All checks pass ΓåÆ render the protected content

+ function ProtectedRoute({ role, allowedRole, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   // Show loading state while session is being verified from browser cookies

+   // If we're still loading but already know the user is signed in, render

+   // the protected content so a refresh doesn't lock the user out temporarily.

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   // Unauthenticated users cannot access protected routes

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // Role mismatch: user is logged in but lacks permission for this page

+   // Example: Applicant (role="Applicant") trying to access /admin (allowedRole="Admin")

+   if (role !== allowedRole) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // All security checks passed: render the page

+   return children

+ }

+ 

+ function ProviderWorkspaceRoute({ role, signedIn, isLoading, providerLandingRoute, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (providerLandingRoute !== '/provider') {

+     return <Navigate to="/provider/profile" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function ProviderProfileRoute({ role, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function App() {

+   // Authentication state management:

+   const [role, setRole] = useState(null) // Current user's role: 'Admin', 'Provider', 'Applicant', or null

+   const [signedIn, setSignedIn] = useState(false) // True if OAuth session is active

+   const [isLoadingAuth, setIsLoadingAuth] = useState(true) // True while checking session from browser cookies

+   const [authError, setAuthError] = useState('') // Display auth errors to user

+   const [pendingEmail, setPendingEmail] = useState('') // New user email waiting for role selection

+   const [isSavingRole, setIsSavingRole] = useState(false) // True while inserting user into database

+   const [applicantLandingRoute, setApplicantLandingRoute] = useState('/dashboard')

+   const [providerLandingRoute, setProviderLandingRoute] = useState('/provider/profile')

+   const oauthTimeoutRef = useRef(null)

+ 

+   // Animation state for the 3D particle effect

+   const [pointer, setPointer] = useState({ x: 0, y: 0 })

+   const [isScrolled, setIsScrolled] = useState(false)

+   const navigate = useNavigate()

+   const location = useLocation()

+   const oauthRedirectTo =

+     import.meta.env.VITE_AUTH_REDIRECT_URL?.trim() || `${window.location.origin}/`

+ 

+   const clearOAuthTimeout = useCallback(() => {

+     if (oauthTimeoutRef.current) {

+       window.clearTimeout(oauthTimeoutRef.current)

+       oauthTimeoutRef.current = null

+     }

+   }, [])

+ 

+ // helper functions are implemented in ./app-helpers and imported above

+ 

+   // AUTH BOOTSTRAP: Restore user session on app load and listen for auth changes

+   // FLOW:

+   // 1. Check if session exists in browser cookies (user already logged in before)

+   // 2. Lookup user's role in database by email

+   // 3. Set up listener to detect sign-in/sign-out events

+   // 4. When sign-in happens, role lookup and optional role selection for new users

+   // WHY: Ensures app state matches Supabase auth session, prevents stale data on refresh

+   useEffect(() => {

+     let isMounted = true

+ 

+     const initializeAuth = async () => {

+       setIsLoadingAuth(true)

+ 

+       if (!hasSupabaseConfig) {

+         if (isMounted) {

+           setAuthError('Missing Supabase environment variables. Add VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const { data, error } = await supabase.auth.getSession()

+ 

+       if (error) {

+         if (isMounted) {

+           setAuthError('Unable to restore your session. Please sign in again.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const existingSession = data.session

+ 

+       if (!existingSession?.user?.email) {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(existingSession.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(existingSession.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(existingSession.user.email)

+             : getLandingRoute(resolvedRole)

+         if (isMounted) {

+           setSignedIn(true)

+           setRole(resolvedRole)

+           setPendingEmail(resolvedRole ? '' : existingSession.user.email)

+           setApplicantLandingRoute(resolvedLandingRoute)

+           setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+           setAuthError('')

+         }

+       } catch {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setAuthError('Signed in, but role lookup failed. Check your users table and policies.')

+         }

+       } finally {

+         if (isMounted) {

+           setIsLoadingAuth(false)

+         }

+       }

+     }

+ 

+     initializeAuth()

+ 

+     if (!hasSupabaseConfig) {

+       return () => {

+         isMounted = false

+       }

+     }

+ 

+     const {

+       data: { subscription },

+     } = supabase.auth.onAuthStateChange(async (event, session) => {

+       if (!isMounted) {

+         return

+       }

+ 

+       if (event === 'SIGNED_OUT' || !session?.user?.email) {

+         clearOAuthTimeout()

+         setSignedIn(false)

+         setRole(null)

+         setPendingEmail('')

+         setIsSavingRole(false)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setIsLoadingAuth(false)

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(session.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(session.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(session.user.email)

+             : getLandingRoute(resolvedRole)

+         if (!isMounted) {

+           return

+         }

+         clearOAuthTimeout()

+         setSignedIn(true)

+         setRole(resolvedRole)

+         setPendingEmail(resolvedRole ? '' : session.user.email)

+         setApplicantLandingRoute(resolvedLandingRoute)

+         setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+         setAuthError('')

+       } catch {

+         if (!isMounted) {

+           return

+         }

+         setSignedIn(false)

+         setRole(null)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setAuthError('OAuth succeeded, but role lookup failed. Please verify Supabase table policies.')

+       } finally {

+         if (isMounted) {

+           clearOAuthTimeout()

+           setIsLoadingAuth(false)

+         }

+       }

+     })

+ 

+     return () => {

+       clearOAuthTimeout()

+       isMounted = false

+       subscription.unsubscribe()

+     }

+   }, [clearOAuthTimeout])

+ 

+   // AUTO-REDIRECT: Send logged-in users to their role-appropriate dashboard

+   // WHEN: User has loaded auth state, is signed in, has a role, and is on home page

+   // REDIRECTS: Admin ΓåÆ /admin, Provider ΓåÆ /provider, Applicant ΓåÆ /dashboard

+   useEffect(() => {

+     const redirectedFromProtectedRoute = Boolean(location.state?.from)

+     const keepApplicantOnHome = redirectedFromProtectedRoute && role === 'Applicant'

+     const landingRoute =

+       role === 'Applicant'

+         ? applicantLandingRoute

+         : role === 'Provider'

+           ? providerLandingRoute

+           : getLandingRoute(role)

+ 

+     if (!isLoadingAuth && signedIn && role && location.pathname === '/' && !keepApplicantOnHome) {

+       navigate(landingRoute, { replace: true })

+     }

+   }, [isLoadingAuth, signedIn, role, applicantLandingRoute, providerLandingRoute, location.pathname, location.state, navigate])

+ 

+   // Clear session and reset all state when logging out

+   const handleLogout = async () => {

+     clearOAuthTimeout()

+ 

+     if (hasSupabaseConfig) {

+       await supabase.auth.signOut() // Clear session cookie

+     }

+     setSignedIn(false)

+     setRole(null)

+     setPendingEmail('')

+     setIsSavingRole(false)

+     setApplicantLandingRoute('/dashboard')

+     setProviderLandingRoute('/provider/profile')

+     navigate('/')

+   }

+ 

+   // NEW USER ROLE SELECTION: First-time users must choose their role

+   // WHY: roles (Admin/Provider/Applicant) determine which dashboard they access

+   // PROCESS:

+   // 1. User logs in with Google (new email, not in database yet)

+   // 2. We show role selection popup

+   // 3. User picks role ΓåÆ we insert into 'users' table with their email + role

+   // 4. Next login, step 2-3 are skipped (role already in database)

+   const handleRoleSelection = async (selectedRole) => {

+     if (!pendingEmail) {

+       return

+     }

+ 

+     setIsSavingRole(true)

+     setAuthError('')

+ 

+     const { data: insertedUser, error } = await supabase

+       .from('users')

+       .insert({ email: pendingEmail, role: selectedRole })

+       .select('role')

+       .single()

+ 

+     if (error) {

+       setIsSavingRole(false)

+       setAuthError('Could not save role selection. Please try again.')

+       return

+     }

+ 

+     const resolvedRole = insertedUser?.role || selectedRole

+     setRole(resolvedRole)

+     setPendingEmail('')

+     setIsSavingRole(false)

+ 

+     if (resolvedRole === 'Applicant') {

+       setApplicantLandingRoute('/profile')

+       navigate('/profile', { replace: true })

+       return

+     }

+ 

+     if (resolvedRole === 'Provider') {

+       setProviderLandingRoute('/provider/profile')

+       navigate('/provider/profile', { replace: true })

+       return

+     }

+ 

+     setApplicantLandingRoute(getLandingRoute(resolvedRole))

+     navigate(getLandingRoute(resolvedRole), { replace: true })

+   }

+ 

+   // Generate particle positions for 3D animation (748 dots in 34x22 grid)

+   // Complex math creates radial gradient effect that reacts to mouse movement

+   const dots = useMemo(() => {

+     const result = []

+     const columns = 34

+     const rows = 22

+ 

+     for (let row = 0; row < rows; row += 1) {

+       for (let col = 0; col < columns; col += 1) {

+         const index = row * columns + col

+         const nx = (col - (columns - 1) / 2) / ((columns - 1) / 2)

+         const ny = (row - (rows - 1) / 2) / ((rows - 1) / 2)

+         const r = Math.sqrt(nx * nx + ny * ny)

+         const z = (1 - Math.min(r, 1)) * 110 - Math.abs(nx) * 32

+         const dx = ((col % 5) - 2) * 1.6

+         const dy = ((row % 5) - 2) * 1.4

+         const dz = ((index % 7) - 3) * 2.2

+         const delay = `${(index % 11) * 0.18}s`

+         const duration = `${3.6 + (index % 6) * 0.6}s`

+         result.push({ nx, ny, z, id: `${row}-${col}`, dx, dy, dz, delay, duration })

+       }

+     }

+ 

+     return result

+   }, [])

+ 

+   // Track mouse position for 3D particle effect

+   const handleVisualMove = (event) => {

+     const rect = event.currentTarget.getBoundingClientRect()

+     const px = ((event.clientX - rect.left) / rect.width - 0.5) * 2

+     const py = ((event.clientY - rect.top) / rect.height - 0.5) * 2

+     setPointer({ x: px, y: py })

+   }

+ 

+   const resetVisualMove = () => setPointer({ x: 0, y: 0 })

+ 

+   // OAUTH ENTRY POINT: Initiate Google sign-in

+   // HOW: Calls Supabase.auth.signInWithOAuth() which opens Google login popup

+   // THEN: Auth listener (above) automatically detects successful login and looks up role

+   // NOTE: Don't manually handle role lookup hereΓÇölet the auth listener do it (keeps logic centralized)

+   const handleGoogleContinue = async () => {

+     setAuthError('')

+ 

+     if (!hasSupabaseConfig) {

+       setAuthError('Set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY before using Google login.')

+       return

+     }

+ 

+     setIsLoadingAuth(true)

+ 

+     clearOAuthTimeout()

+     oauthTimeoutRef.current = window.setTimeout(() => {

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in timed out. Please try again.')

+     }, 12000)

+ 

+     const { error } = await supabase.auth.signInWithOAuth({

+       provider: 'google',

+       options: {

+         redirectTo: oauthRedirectTo,

+       },

+     })

+ 

+     if (error) {

+       clearOAuthTimeout()

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in failed. Verify Supabase redirect URLs include your local host and VITE_AUTH_REDIRECT_URL value.')

+     }

+   }

+ 

+   // Fade in sections as they scroll into view

+   useEffect(() => {

+     if (location.pathname !== '/') {

+       return undefined

+     }

+ 

+     const nodes = document.querySelectorAll('.scroll-animate')

+ 

+     if (!nodes.length) {

+       return undefined

+     }

+ 

+     const observer = new IntersectionObserver(

+       (entries) => {

+         entries.forEach((entry) => {

+           if (entry.isIntersecting) {

+             entry.target.classList.add('is-visible') // Add CSS class to trigger fade-in

+           } else {

+             entry.target.classList.remove('is-visible') // Remove class when scrolled past

+           }

+         })

+       },

+       { threshold: 0.18 }, // Element must be 18% visible to trigger

+     )

+ 

+     nodes.forEach((node) => observer.observe(node))

+ 

+     return () => {

+       nodes.forEach((node) => observer.unobserve(node))

+       observer.disconnect()

+     }

+   }, [location.pathname])

+ 

+   // Track if user has scrolled past the hero section (used for topbar styling)

+   useEffect(() => {

+     const onScroll = () => setIsScrolled(window.scrollY > 120)

+     onScroll()

+     window.addEventListener('scroll', onScroll, { passive: true })

+     return () => window.removeEventListener('scroll', onScroll)

+   }, [])

+ 

+   // All routes: home page with auth, and protected role dashboards

+ return (

+   <Routes>

+     <Route path="/" element={

+       <main className="foundry-clone">

+ 

+         <header className={`global-topbar ${isScrolled ? 'is-scrolled' : ''}`}>

+           <div className="topbar-main">

+             <div className="topbar-brand">SA LEARNERSHIP FOUNDRY</div>

+             <nav className="topbar-nav">

+               <a href="#">Why Portal</a>

+               <a href="#">Pathways</a>

+               <a href="#">Team</a>

+               <a href="#">Insights</a>

+               <a href="#">Contact</a>

+             </nav>

+           </div>

+ 

+           <div className="topbar-auth">

+             {signedIn && role ? <span className="role-display">{role}</span> : null}

+             <button className="google-auth" onClick={signedIn ? handleLogout : handleGoogleContinue}>

+               {isLoadingAuth ? 'Authenticating...' : signedIn ? 'Logout' : 'Log In with Google'}

+             </button>

+           </div>

+         </header>

+ 

+         <section className="page-one">

+           <div className="hero-main">

+             <h1 className="left-title">

+               BUILDING TALENT

+               <br />

+               PATHWAYS THAT

+             </h1>

+ 

+             <div

+               className="interactive-model"

+               onMouseMove={handleVisualMove}

+               onMouseLeave={resetVisualMove}

+               style={{ '--px': pointer.x, '--py': pointer.y }}

+             >

+               <div className="orbital-core">

+                 <span className="orbit orbit-a"></span>

+                 <span className="orbit orbit-b"></span>

+                 <span className="orbit orbit-c"></span>

+               </div>

+ 

+               <div className="dot-volume">

+                 {dots.map((dot) => (

+                   <span

+                     key={dot.id}

+                     className="dot"

+                     style={{

+                       '--x': `${dot.nx * 340}px`,

+                       '--y': `${dot.ny * 240}px`,

+                       '--z': `${dot.z}px`,

+                     }}

+                   />

+                 ))}

+               </div>

+             </div>

+ 

+             <h1 className="right-title">

+               PUSH THE

+               <br />

+               FUTURE FORWARD

+             </h1>

+           </div>

+ 

+           <div className="hero-bottom">

+             <p>

+               A venture lab style platform for learnership access, application intelligence,

+               and practical support that moves candidates from skill to real work.

+             </p>

+ 

+             <div className="status-pill">

+               {signedIn && role

+                 ? `${role} workspace unlocked`

+                 : signedIn && pendingEmail

+                   ? 'Choose your role to continue'

+                   : 'Sign in with Google to continue'}

+             </div>

+           </div>

+           {signedIn && !role && pendingEmail ? (

+             <div className="role-onboarding-overlay" role="dialog" aria-modal="true" aria-labelledby="role-onboarding-title">

+               <div className="role-onboarding">

+                 <p className="role-onboarding-eyebrow">One more step</p>

+                 <h2 id="role-onboarding-title">Choose your role</h2>

+                 <p>Select how you want to use the platform for {pendingEmail}.</p>

+                 <div className="role-select">

+                   <button onClick={() => handleRoleSelection('Applicant')} disabled={isSavingRole}>

+                     Applicant

+                   </button>

+                   <button onClick={() => handleRoleSelection('Provider')} disabled={isSavingRole}>

+                     Provider

+                   </button>

+                 </div>

+               </div>

+             </div>

+           ) : null}

+           {authError ? <p className="auth-error">{authError}</p> : null}

+         </section>

+ 

+         <section className="page-two">

+           <div className="ethos-row scroll-animate">

+             <div>

+               <p className="mini-label">Why this platform</p>

+               <h2>

+                 OPEN DOORS

+                 <br />

+                 TO REAL WORK

+               </h2>

+             </div>

+ 

+             <div className="ethos-copy">

+               <p>

+                 Learnership Foundry helps applicants, providers, and moderators collaborate in one

+                 flow. Discover pathways, publish opportunities, and keep quality high with

+                 visibility from intake to placement.

+               </p>

+             </div>

+           </div>

+ 

+           <div className="card-strip scroll-animate swipe-left">

+             <article className="signal-card tone-dark">

+               <h3>Discover</h3>

+               <p>APPLICANT VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Find learnerships that match your interests and readiness level.</small>

+             </article>

+ 

+             <article className="signal-card tone-cyan">

+               <h3>Publish</h3>

+               <p>PROVIDER VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Create listings and track engagement from qualified candidates.</small>

+             </article>

+ 

+             <article className="signal-card tone-orange">

+               <h3>Moderate</h3>

+               <p>ADMIN VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Flag risk, request fixes, and approve opportunities confidently.</small>

+             </article>

+ 

+             <article className="signal-card tone-light">

+               <h3>Move</h3>

+               <p>OUTCOMES</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Turn applications into skills, credentials, and workplace entry points.</small>

+             </article>

+           </div>

+         </section>

+ 

+         <section className="page-three">

+           <span className="dot-plane" aria-hidden="true"></span>

+           <div className="focus-copy scroll-animate">

+             <p className="mini-label">Built for momentum</p>

+             <h2>

+               From FIRST CLICK

+               <br />

+               TO FIRST PAYCHECK

+             </h2>

+             <p>

+               Scroll the platform, choose your role, and move into the right workspace to continue.

+             </p>

+           </div>

+         </section>

+ 

+       </main>

+     } />

+ 

+     

+    <Route path="/dashboard" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <Dashboard onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+ <Route path="/dashboard/listings/:listingId" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ApplicantListingDetail onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   <Route path="/profile" element={

+     <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+       <ApplicantProfile onLogout={handleLogout} />

+     </ProtectedRoute>

+   } />

+ 

+ <Route path="/provider" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <Provider onLogout={handleLogout} />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/profile" element={

+   <ProviderProfileRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ProviderProfile

+       onLogout={handleLogout}

+       onProfileSaved={() => setProviderLandingRoute('/provider')}

+     />

+   </ProviderProfileRoute>

+ } />

+ 

+ <Route path="/provider/listings/new" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingForm />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/edit" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingEdit />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/applications" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingApplications />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/admin" element={

+   <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <AdminDashboardShell onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   </Routes>

+ )

+ }

+ 

+ export default App

+

 Γ¥» src/App.test.jsx:81:23
     79|     const appSource = readFileSync(resolve(cwd(), 'src/App.jsx'), 'utfΓÇª
     80|
     81|     expect(appSource).toContain("if (role === 'Admin') return '/admin'ΓÇª
       |                       ^
     82|     expect(appSource).toContain('<Route path="/admin"')
     83|   })

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[17/28]ΓÄ»

 FAIL  src/App.test.jsx > Role based tests > Applicant role redirects to /dashboard
AssertionError: expected '// Main app: handles Google OAuth, roΓÇª' to contain 'return \'/dashboard\''

- Expected
+ Received

- return '/dashboard'
+ // Main app: handles Google OAuth, role-based routing, and admin dashboard

+ // TRICKY PARTS:

+ // - OAuth flow: Google login ΓåÆ email lookup ΓåÆ role selection (if new user)

+ // - Session persistence: browser cookies restored on page refresh

+ // - Route guards: ProtectedRoute blocks unauthorized access

+ // - State management: tracking auth, loading, roles, and pending signups

+ // - 3D animation: particle effect that reacts to mouse movement

+ 

+ import { useCallback, useEffect, useMemo, useRef, useState } from 'react'

+ import './App.css'

+ import { Routes, Route, useNavigate, useLocation, Navigate } from 'react-router-dom'

+ import Admin from './pages/Admin'

+ import Dashboard from './pages/Dashboard'

+ import ApplicantProfile from './pages/ApplicantProfile'

+ import ApplicantListingDetail from './pages/ApplicantListingDetail'

+ import Provider from './pages/Provider'

+ import ProviderProfile from './pages/ProviderProfile'

+ import ProviderListingForm from './pages/ProviderListingForm'

+ import ProviderListingEdit from './pages/ProviderListingEdit'

+ import ProviderListingApplications from './pages/ProviderListingApplications'

+ import { hasSupabaseConfig, supabase } from './lib/supabaseClient'

+ import {

+   getLandingRoute,

+   getRoleForEmail,

+   getApplicantLandingRouteForEmail,

+   getProviderLandingRouteForEmail,

+ } from './app-helpers'

+ const AdminDashboardShell = Admin

+ 

+ // ProtectedRoute: Guard component that restricts access to role-specific pages

+ // USAGE: <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}><Admin /></ProtectedRoute>

+ // LOGIC:

+ // 1. If still checking auth ΓåÆ show loading shell (don't flash wrong page)

+ // 2. If not signed in ΓåÆ redirect to home (must log in)

+ // 3. If wrong role ΓåÆ redirect to home (prevent unauthorized access)

+ // 4. All checks pass ΓåÆ render the protected content

+ function ProtectedRoute({ role, allowedRole, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   // Show loading state while session is being verified from browser cookies

+   // If we're still loading but already know the user is signed in, render

+   // the protected content so a refresh doesn't lock the user out temporarily.

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   // Unauthenticated users cannot access protected routes

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // Role mismatch: user is logged in but lacks permission for this page

+   // Example: Applicant (role="Applicant") trying to access /admin (allowedRole="Admin")

+   if (role !== allowedRole) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // All security checks passed: render the page

+   return children

+ }

+ 

+ function ProviderWorkspaceRoute({ role, signedIn, isLoading, providerLandingRoute, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (providerLandingRoute !== '/provider') {

+     return <Navigate to="/provider/profile" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function ProviderProfileRoute({ role, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function App() {

+   // Authentication state management:

+   const [role, setRole] = useState(null) // Current user's role: 'Admin', 'Provider', 'Applicant', or null

+   const [signedIn, setSignedIn] = useState(false) // True if OAuth session is active

+   const [isLoadingAuth, setIsLoadingAuth] = useState(true) // True while checking session from browser cookies

+   const [authError, setAuthError] = useState('') // Display auth errors to user

+   const [pendingEmail, setPendingEmail] = useState('') // New user email waiting for role selection

+   const [isSavingRole, setIsSavingRole] = useState(false) // True while inserting user into database

+   const [applicantLandingRoute, setApplicantLandingRoute] = useState('/dashboard')

+   const [providerLandingRoute, setProviderLandingRoute] = useState('/provider/profile')

+   const oauthTimeoutRef = useRef(null)

+ 

+   // Animation state for the 3D particle effect

+   const [pointer, setPointer] = useState({ x: 0, y: 0 })

+   const [isScrolled, setIsScrolled] = useState(false)

+   const navigate = useNavigate()

+   const location = useLocation()

+   const oauthRedirectTo =

+     import.meta.env.VITE_AUTH_REDIRECT_URL?.trim() || `${window.location.origin}/`

+ 

+   const clearOAuthTimeout = useCallback(() => {

+     if (oauthTimeoutRef.current) {

+       window.clearTimeout(oauthTimeoutRef.current)

+       oauthTimeoutRef.current = null

+     }

+   }, [])

+ 

+ // helper functions are implemented in ./app-helpers and imported above

+ 

+   // AUTH BOOTSTRAP: Restore user session on app load and listen for auth changes

+   // FLOW:

+   // 1. Check if session exists in browser cookies (user already logged in before)

+   // 2. Lookup user's role in database by email

+   // 3. Set up listener to detect sign-in/sign-out events

+   // 4. When sign-in happens, role lookup and optional role selection for new users

+   // WHY: Ensures app state matches Supabase auth session, prevents stale data on refresh

+   useEffect(() => {

+     let isMounted = true

+ 

+     const initializeAuth = async () => {

+       setIsLoadingAuth(true)

+ 

+       if (!hasSupabaseConfig) {

+         if (isMounted) {

+           setAuthError('Missing Supabase environment variables. Add VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const { data, error } = await supabase.auth.getSession()

+ 

+       if (error) {

+         if (isMounted) {

+           setAuthError('Unable to restore your session. Please sign in again.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const existingSession = data.session

+ 

+       if (!existingSession?.user?.email) {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(existingSession.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(existingSession.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(existingSession.user.email)

+             : getLandingRoute(resolvedRole)

+         if (isMounted) {

+           setSignedIn(true)

+           setRole(resolvedRole)

+           setPendingEmail(resolvedRole ? '' : existingSession.user.email)

+           setApplicantLandingRoute(resolvedLandingRoute)

+           setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+           setAuthError('')

+         }

+       } catch {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setAuthError('Signed in, but role lookup failed. Check your users table and policies.')

+         }

+       } finally {

+         if (isMounted) {

+           setIsLoadingAuth(false)

+         }

+       }

+     }

+ 

+     initializeAuth()

+ 

+     if (!hasSupabaseConfig) {

+       return () => {

+         isMounted = false

+       }

+     }

+ 

+     const {

+       data: { subscription },

+     } = supabase.auth.onAuthStateChange(async (event, session) => {

+       if (!isMounted) {

+         return

+       }

+ 

+       if (event === 'SIGNED_OUT' || !session?.user?.email) {

+         clearOAuthTimeout()

+         setSignedIn(false)

+         setRole(null)

+         setPendingEmail('')

+         setIsSavingRole(false)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setIsLoadingAuth(false)

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(session.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(session.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(session.user.email)

+             : getLandingRoute(resolvedRole)

+         if (!isMounted) {

+           return

+         }

+         clearOAuthTimeout()

+         setSignedIn(true)

+         setRole(resolvedRole)

+         setPendingEmail(resolvedRole ? '' : session.user.email)

+         setApplicantLandingRoute(resolvedLandingRoute)

+         setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+         setAuthError('')

+       } catch {

+         if (!isMounted) {

+           return

+         }

+         setSignedIn(false)

+         setRole(null)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setAuthError('OAuth succeeded, but role lookup failed. Please verify Supabase table policies.')

+       } finally {

+         if (isMounted) {

+           clearOAuthTimeout()

+           setIsLoadingAuth(false)

+         }

+       }

+     })

+ 

+     return () => {

+       clearOAuthTimeout()

+       isMounted = false

+       subscription.unsubscribe()

+     }

+   }, [clearOAuthTimeout])

+ 

+   // AUTO-REDIRECT: Send logged-in users to their role-appropriate dashboard

+   // WHEN: User has loaded auth state, is signed in, has a role, and is on home page

+   // REDIRECTS: Admin ΓåÆ /admin, Provider ΓåÆ /provider, Applicant ΓåÆ /dashboard

+   useEffect(() => {

+     const redirectedFromProtectedRoute = Boolean(location.state?.from)

+     const keepApplicantOnHome = redirectedFromProtectedRoute && role === 'Applicant'

+     const landingRoute =

+       role === 'Applicant'

+         ? applicantLandingRoute

+         : role === 'Provider'

+           ? providerLandingRoute

+           : getLandingRoute(role)

+ 

+     if (!isLoadingAuth && signedIn && role && location.pathname === '/' && !keepApplicantOnHome) {

+       navigate(landingRoute, { replace: true })

+     }

+   }, [isLoadingAuth, signedIn, role, applicantLandingRoute, providerLandingRoute, location.pathname, location.state, navigate])

+ 

+   // Clear session and reset all state when logging out

+   const handleLogout = async () => {

+     clearOAuthTimeout()

+ 

+     if (hasSupabaseConfig) {

+       await supabase.auth.signOut() // Clear session cookie

+     }

+     setSignedIn(false)

+     setRole(null)

+     setPendingEmail('')

+     setIsSavingRole(false)

+     setApplicantLandingRoute('/dashboard')

+     setProviderLandingRoute('/provider/profile')

+     navigate('/')

+   }

+ 

+   // NEW USER ROLE SELECTION: First-time users must choose their role

+   // WHY: roles (Admin/Provider/Applicant) determine which dashboard they access

+   // PROCESS:

+   // 1. User logs in with Google (new email, not in database yet)

+   // 2. We show role selection popup

+   // 3. User picks role ΓåÆ we insert into 'users' table with their email + role

+   // 4. Next login, step 2-3 are skipped (role already in database)

+   const handleRoleSelection = async (selectedRole) => {

+     if (!pendingEmail) {

+       return

+     }

+ 

+     setIsSavingRole(true)

+     setAuthError('')

+ 

+     const { data: insertedUser, error } = await supabase

+       .from('users')

+       .insert({ email: pendingEmail, role: selectedRole })

+       .select('role')

+       .single()

+ 

+     if (error) {

+       setIsSavingRole(false)

+       setAuthError('Could not save role selection. Please try again.')

+       return

+     }

+ 

+     const resolvedRole = insertedUser?.role || selectedRole

+     setRole(resolvedRole)

+     setPendingEmail('')

+     setIsSavingRole(false)

+ 

+     if (resolvedRole === 'Applicant') {

+       setApplicantLandingRoute('/profile')

+       navigate('/profile', { replace: true })

+       return

+     }

+ 

+     if (resolvedRole === 'Provider') {

+       setProviderLandingRoute('/provider/profile')

+       navigate('/provider/profile', { replace: true })

+       return

+     }

+ 

+     setApplicantLandingRoute(getLandingRoute(resolvedRole))

+     navigate(getLandingRoute(resolvedRole), { replace: true })

+   }

+ 

+   // Generate particle positions for 3D animation (748 dots in 34x22 grid)

+   // Complex math creates radial gradient effect that reacts to mouse movement

+   const dots = useMemo(() => {

+     const result = []

+     const columns = 34

+     const rows = 22

+ 

+     for (let row = 0; row < rows; row += 1) {

+       for (let col = 0; col < columns; col += 1) {

+         const index = row * columns + col

+         const nx = (col - (columns - 1) / 2) / ((columns - 1) / 2)

+         const ny = (row - (rows - 1) / 2) / ((rows - 1) / 2)

+         const r = Math.sqrt(nx * nx + ny * ny)

+         const z = (1 - Math.min(r, 1)) * 110 - Math.abs(nx) * 32

+         const dx = ((col % 5) - 2) * 1.6

+         const dy = ((row % 5) - 2) * 1.4

+         const dz = ((index % 7) - 3) * 2.2

+         const delay = `${(index % 11) * 0.18}s`

+         const duration = `${3.6 + (index % 6) * 0.6}s`

+         result.push({ nx, ny, z, id: `${row}-${col}`, dx, dy, dz, delay, duration })

+       }

+     }

+ 

+     return result

+   }, [])

+ 

+   // Track mouse position for 3D particle effect

+   const handleVisualMove = (event) => {

+     const rect = event.currentTarget.getBoundingClientRect()

+     const px = ((event.clientX - rect.left) / rect.width - 0.5) * 2

+     const py = ((event.clientY - rect.top) / rect.height - 0.5) * 2

+     setPointer({ x: px, y: py })

+   }

+ 

+   const resetVisualMove = () => setPointer({ x: 0, y: 0 })

+ 

+   // OAUTH ENTRY POINT: Initiate Google sign-in

+   // HOW: Calls Supabase.auth.signInWithOAuth() which opens Google login popup

+   // THEN: Auth listener (above) automatically detects successful login and looks up role

+   // NOTE: Don't manually handle role lookup hereΓÇölet the auth listener do it (keeps logic centralized)

+   const handleGoogleContinue = async () => {

+     setAuthError('')

+ 

+     if (!hasSupabaseConfig) {

+       setAuthError('Set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY before using Google login.')

+       return

+     }

+ 

+     setIsLoadingAuth(true)

+ 

+     clearOAuthTimeout()

+     oauthTimeoutRef.current = window.setTimeout(() => {

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in timed out. Please try again.')

+     }, 12000)

+ 

+     const { error } = await supabase.auth.signInWithOAuth({

+       provider: 'google',

+       options: {

+         redirectTo: oauthRedirectTo,

+       },

+     })

+ 

+     if (error) {

+       clearOAuthTimeout()

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in failed. Verify Supabase redirect URLs include your local host and VITE_AUTH_REDIRECT_URL value.')

+     }

+   }

+ 

+   // Fade in sections as they scroll into view

+   useEffect(() => {

+     if (location.pathname !== '/') {

+       return undefined

+     }

+ 

+     const nodes = document.querySelectorAll('.scroll-animate')

+ 

+     if (!nodes.length) {

+       return undefined

+     }

+ 

+     const observer = new IntersectionObserver(

+       (entries) => {

+         entries.forEach((entry) => {

+           if (entry.isIntersecting) {

+             entry.target.classList.add('is-visible') // Add CSS class to trigger fade-in

+           } else {

+             entry.target.classList.remove('is-visible') // Remove class when scrolled past

+           }

+         })

+       },

+       { threshold: 0.18 }, // Element must be 18% visible to trigger

+     )

+ 

+     nodes.forEach((node) => observer.observe(node))

+ 

+     return () => {

+       nodes.forEach((node) => observer.unobserve(node))

+       observer.disconnect()

+     }

+   }, [location.pathname])

+ 

+   // Track if user has scrolled past the hero section (used for topbar styling)

+   useEffect(() => {

+     const onScroll = () => setIsScrolled(window.scrollY > 120)

+     onScroll()

+     window.addEventListener('scroll', onScroll, { passive: true })

+     return () => window.removeEventListener('scroll', onScroll)

+   }, [])

+ 

+   // All routes: home page with auth, and protected role dashboards

+ return (

+   <Routes>

+     <Route path="/" element={

+       <main className="foundry-clone">

+ 

+         <header className={`global-topbar ${isScrolled ? 'is-scrolled' : ''}`}>

+           <div className="topbar-main">

+             <div className="topbar-brand">SA LEARNERSHIP FOUNDRY</div>

+             <nav className="topbar-nav">

+               <a href="#">Why Portal</a>

+               <a href="#">Pathways</a>

+               <a href="#">Team</a>

+               <a href="#">Insights</a>

+               <a href="#">Contact</a>

+             </nav>

+           </div>

+ 

+           <div className="topbar-auth">

+             {signedIn && role ? <span className="role-display">{role}</span> : null}

+             <button className="google-auth" onClick={signedIn ? handleLogout : handleGoogleContinue}>

+               {isLoadingAuth ? 'Authenticating...' : signedIn ? 'Logout' : 'Log In with Google'}

+             </button>

+           </div>

+         </header>

+ 

+         <section className="page-one">

+           <div className="hero-main">

+             <h1 className="left-title">

+               BUILDING TALENT

+               <br />

+               PATHWAYS THAT

+             </h1>

+ 

+             <div

+               className="interactive-model"

+               onMouseMove={handleVisualMove}

+               onMouseLeave={resetVisualMove}

+               style={{ '--px': pointer.x, '--py': pointer.y }}

+             >

+               <div className="orbital-core">

+                 <span className="orbit orbit-a"></span>

+                 <span className="orbit orbit-b"></span>

+                 <span className="orbit orbit-c"></span>

+               </div>

+ 

+               <div className="dot-volume">

+                 {dots.map((dot) => (

+                   <span

+                     key={dot.id}

+                     className="dot"

+                     style={{

+                       '--x': `${dot.nx * 340}px`,

+                       '--y': `${dot.ny * 240}px`,

+                       '--z': `${dot.z}px`,

+                     }}

+                   />

+                 ))}

+               </div>

+             </div>

+ 

+             <h1 className="right-title">

+               PUSH THE

+               <br />

+               FUTURE FORWARD

+             </h1>

+           </div>

+ 

+           <div className="hero-bottom">

+             <p>

+               A venture lab style platform for learnership access, application intelligence,

+               and practical support that moves candidates from skill to real work.

+             </p>

+ 

+             <div className="status-pill">

+               {signedIn && role

+                 ? `${role} workspace unlocked`

+                 : signedIn && pendingEmail

+                   ? 'Choose your role to continue'

+                   : 'Sign in with Google to continue'}

+             </div>

+           </div>

+           {signedIn && !role && pendingEmail ? (

+             <div className="role-onboarding-overlay" role="dialog" aria-modal="true" aria-labelledby="role-onboarding-title">

+               <div className="role-onboarding">

+                 <p className="role-onboarding-eyebrow">One more step</p>

+                 <h2 id="role-onboarding-title">Choose your role</h2>

+                 <p>Select how you want to use the platform for {pendingEmail}.</p>

+                 <div className="role-select">

+                   <button onClick={() => handleRoleSelection('Applicant')} disabled={isSavingRole}>

+                     Applicant

+                   </button>

+                   <button onClick={() => handleRoleSelection('Provider')} disabled={isSavingRole}>

+                     Provider

+                   </button>

+                 </div>

+               </div>

+             </div>

+           ) : null}

+           {authError ? <p className="auth-error">{authError}</p> : null}

+         </section>

+ 

+         <section className="page-two">

+           <div className="ethos-row scroll-animate">

+             <div>

+               <p className="mini-label">Why this platform</p>

+               <h2>

+                 OPEN DOORS

+                 <br />

+                 TO REAL WORK

+               </h2>

+             </div>

+ 

+             <div className="ethos-copy">

+               <p>

+                 Learnership Foundry helps applicants, providers, and moderators collaborate in one

+                 flow. Discover pathways, publish opportunities, and keep quality high with

+                 visibility from intake to placement.

+               </p>

+             </div>

+           </div>

+ 

+           <div className="card-strip scroll-animate swipe-left">

+             <article className="signal-card tone-dark">

+               <h3>Discover</h3>

+               <p>APPLICANT VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Find learnerships that match your interests and readiness level.</small>

+             </article>

+ 

+             <article className="signal-card tone-cyan">

+               <h3>Publish</h3>

+               <p>PROVIDER VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Create listings and track engagement from qualified candidates.</small>

+             </article>

+ 

+             <article className="signal-card tone-orange">

+               <h3>Moderate</h3>

+               <p>ADMIN VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Flag risk, request fixes, and approve opportunities confidently.</small>

+             </article>

+ 

+             <article className="signal-card tone-light">

+               <h3>Move</h3>

+               <p>OUTCOMES</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Turn applications into skills, credentials, and workplace entry points.</small>

+             </article>

+           </div>

+         </section>

+ 

+         <section className="page-three">

+           <span className="dot-plane" aria-hidden="true"></span>

+           <div className="focus-copy scroll-animate">

+             <p className="mini-label">Built for momentum</p>

+             <h2>

+               From FIRST CLICK

+               <br />

+               TO FIRST PAYCHECK

+             </h2>

+             <p>

+               Scroll the platform, choose your role, and move into the right workspace to continue.

+             </p>

+           </div>

+         </section>

+ 

+       </main>

+     } />

+ 

+     

+    <Route path="/dashboard" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <Dashboard onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+ <Route path="/dashboard/listings/:listingId" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ApplicantListingDetail onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   <Route path="/profile" element={

+     <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+       <ApplicantProfile onLogout={handleLogout} />

+     </ProtectedRoute>

+   } />

+ 

+ <Route path="/provider" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <Provider onLogout={handleLogout} />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/profile" element={

+   <ProviderProfileRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ProviderProfile

+       onLogout={handleLogout}

+       onProfileSaved={() => setProviderLandingRoute('/provider')}

+     />

+   </ProviderProfileRoute>

+ } />

+ 

+ <Route path="/provider/listings/new" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingForm />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/edit" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingEdit />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/applications" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingApplications />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/admin" element={

+   <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <AdminDashboardShell onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   </Routes>

+ )

+ }

+ 

+ export default App

+

 Γ¥» src/App.test.jsx:101:23
     99|     const appSource = readFileSync(resolve(cwd(), 'src/App.jsx'), 'utfΓÇª
    100|
    101|     expect(appSource).toContain("return '/dashboard'")
       |                       ^
    102|   })
    103|

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[18/28]ΓÄ»

 FAIL  src/App.test.jsx > Role based tests > Admin role redirects to /admin
AssertionError: expected '// Main app: handles Google OAuth, roΓÇª' to contain 'if (role === \'Admin\') return \'/admΓÇª'

- Expected
+ Received

- if (role === 'Admin') return '/admin'
+ // Main app: handles Google OAuth, role-based routing, and admin dashboard

+ // TRICKY PARTS:

+ // - OAuth flow: Google login ΓåÆ email lookup ΓåÆ role selection (if new user)

+ // - Session persistence: browser cookies restored on page refresh

+ // - Route guards: ProtectedRoute blocks unauthorized access

+ // - State management: tracking auth, loading, roles, and pending signups

+ // - 3D animation: particle effect that reacts to mouse movement

+ 

+ import { useCallback, useEffect, useMemo, useRef, useState } from 'react'

+ import './App.css'

+ import { Routes, Route, useNavigate, useLocation, Navigate } from 'react-router-dom'

+ import Admin from './pages/Admin'

+ import Dashboard from './pages/Dashboard'

+ import ApplicantProfile from './pages/ApplicantProfile'

+ import ApplicantListingDetail from './pages/ApplicantListingDetail'

+ import Provider from './pages/Provider'

+ import ProviderProfile from './pages/ProviderProfile'

+ import ProviderListingForm from './pages/ProviderListingForm'

+ import ProviderListingEdit from './pages/ProviderListingEdit'

+ import ProviderListingApplications from './pages/ProviderListingApplications'

+ import { hasSupabaseConfig, supabase } from './lib/supabaseClient'

+ import {

+   getLandingRoute,

+   getRoleForEmail,

+   getApplicantLandingRouteForEmail,

+   getProviderLandingRouteForEmail,

+ } from './app-helpers'

+ const AdminDashboardShell = Admin

+ 

+ // ProtectedRoute: Guard component that restricts access to role-specific pages

+ // USAGE: <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}><Admin /></ProtectedRoute>

+ // LOGIC:

+ // 1. If still checking auth ΓåÆ show loading shell (don't flash wrong page)

+ // 2. If not signed in ΓåÆ redirect to home (must log in)

+ // 3. If wrong role ΓåÆ redirect to home (prevent unauthorized access)

+ // 4. All checks pass ΓåÆ render the protected content

+ function ProtectedRoute({ role, allowedRole, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   // Show loading state while session is being verified from browser cookies

+   // If we're still loading but already know the user is signed in, render

+   // the protected content so a refresh doesn't lock the user out temporarily.

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   // Unauthenticated users cannot access protected routes

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // Role mismatch: user is logged in but lacks permission for this page

+   // Example: Applicant (role="Applicant") trying to access /admin (allowedRole="Admin")

+   if (role !== allowedRole) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // All security checks passed: render the page

+   return children

+ }

+ 

+ function ProviderWorkspaceRoute({ role, signedIn, isLoading, providerLandingRoute, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (providerLandingRoute !== '/provider') {

+     return <Navigate to="/provider/profile" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function ProviderProfileRoute({ role, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function App() {

+   // Authentication state management:

+   const [role, setRole] = useState(null) // Current user's role: 'Admin', 'Provider', 'Applicant', or null

+   const [signedIn, setSignedIn] = useState(false) // True if OAuth session is active

+   const [isLoadingAuth, setIsLoadingAuth] = useState(true) // True while checking session from browser cookies

+   const [authError, setAuthError] = useState('') // Display auth errors to user

+   const [pendingEmail, setPendingEmail] = useState('') // New user email waiting for role selection

+   const [isSavingRole, setIsSavingRole] = useState(false) // True while inserting user into database

+   const [applicantLandingRoute, setApplicantLandingRoute] = useState('/dashboard')

+   const [providerLandingRoute, setProviderLandingRoute] = useState('/provider/profile')

+   const oauthTimeoutRef = useRef(null)

+ 

+   // Animation state for the 3D particle effect

+   const [pointer, setPointer] = useState({ x: 0, y: 0 })

+   const [isScrolled, setIsScrolled] = useState(false)

+   const navigate = useNavigate()

+   const location = useLocation()

+   const oauthRedirectTo =

+     import.meta.env.VITE_AUTH_REDIRECT_URL?.trim() || `${window.location.origin}/`

+ 

+   const clearOAuthTimeout = useCallback(() => {

+     if (oauthTimeoutRef.current) {

+       window.clearTimeout(oauthTimeoutRef.current)

+       oauthTimeoutRef.current = null

+     }

+   }, [])

+ 

+ // helper functions are implemented in ./app-helpers and imported above

+ 

+   // AUTH BOOTSTRAP: Restore user session on app load and listen for auth changes

+   // FLOW:

+   // 1. Check if session exists in browser cookies (user already logged in before)

+   // 2. Lookup user's role in database by email

+   // 3. Set up listener to detect sign-in/sign-out events

+   // 4. When sign-in happens, role lookup and optional role selection for new users

+   // WHY: Ensures app state matches Supabase auth session, prevents stale data on refresh

+   useEffect(() => {

+     let isMounted = true

+ 

+     const initializeAuth = async () => {

+       setIsLoadingAuth(true)

+ 

+       if (!hasSupabaseConfig) {

+         if (isMounted) {

+           setAuthError('Missing Supabase environment variables. Add VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const { data, error } = await supabase.auth.getSession()

+ 

+       if (error) {

+         if (isMounted) {

+           setAuthError('Unable to restore your session. Please sign in again.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const existingSession = data.session

+ 

+       if (!existingSession?.user?.email) {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(existingSession.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(existingSession.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(existingSession.user.email)

+             : getLandingRoute(resolvedRole)

+         if (isMounted) {

+           setSignedIn(true)

+           setRole(resolvedRole)

+           setPendingEmail(resolvedRole ? '' : existingSession.user.email)

+           setApplicantLandingRoute(resolvedLandingRoute)

+           setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+           setAuthError('')

+         }

+       } catch {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setAuthError('Signed in, but role lookup failed. Check your users table and policies.')

+         }

+       } finally {

+         if (isMounted) {

+           setIsLoadingAuth(false)

+         }

+       }

+     }

+ 

+     initializeAuth()

+ 

+     if (!hasSupabaseConfig) {

+       return () => {

+         isMounted = false

+       }

+     }

+ 

+     const {

+       data: { subscription },

+     } = supabase.auth.onAuthStateChange(async (event, session) => {

+       if (!isMounted) {

+         return

+       }

+ 

+       if (event === 'SIGNED_OUT' || !session?.user?.email) {

+         clearOAuthTimeout()

+         setSignedIn(false)

+         setRole(null)

+         setPendingEmail('')

+         setIsSavingRole(false)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setIsLoadingAuth(false)

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(session.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(session.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(session.user.email)

+             : getLandingRoute(resolvedRole)

+         if (!isMounted) {

+           return

+         }

+         clearOAuthTimeout()

+         setSignedIn(true)

+         setRole(resolvedRole)

+         setPendingEmail(resolvedRole ? '' : session.user.email)

+         setApplicantLandingRoute(resolvedLandingRoute)

+         setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+         setAuthError('')

+       } catch {

+         if (!isMounted) {

+           return

+         }

+         setSignedIn(false)

+         setRole(null)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setAuthError('OAuth succeeded, but role lookup failed. Please verify Supabase table policies.')

+       } finally {

+         if (isMounted) {

+           clearOAuthTimeout()

+           setIsLoadingAuth(false)

+         }

+       }

+     })

+ 

+     return () => {

+       clearOAuthTimeout()

+       isMounted = false

+       subscription.unsubscribe()

+     }

+   }, [clearOAuthTimeout])

+ 

+   // AUTO-REDIRECT: Send logged-in users to their role-appropriate dashboard

+   // WHEN: User has loaded auth state, is signed in, has a role, and is on home page

+   // REDIRECTS: Admin ΓåÆ /admin, Provider ΓåÆ /provider, Applicant ΓåÆ /dashboard

+   useEffect(() => {

+     const redirectedFromProtectedRoute = Boolean(location.state?.from)

+     const keepApplicantOnHome = redirectedFromProtectedRoute && role === 'Applicant'

+     const landingRoute =

+       role === 'Applicant'

+         ? applicantLandingRoute

+         : role === 'Provider'

+           ? providerLandingRoute

+           : getLandingRoute(role)

+ 

+     if (!isLoadingAuth && signedIn && role && location.pathname === '/' && !keepApplicantOnHome) {

+       navigate(landingRoute, { replace: true })

+     }

+   }, [isLoadingAuth, signedIn, role, applicantLandingRoute, providerLandingRoute, location.pathname, location.state, navigate])

+ 

+   // Clear session and reset all state when logging out

+   const handleLogout = async () => {

+     clearOAuthTimeout()

+ 

+     if (hasSupabaseConfig) {

+       await supabase.auth.signOut() // Clear session cookie

+     }

+     setSignedIn(false)

+     setRole(null)

+     setPendingEmail('')

+     setIsSavingRole(false)

+     setApplicantLandingRoute('/dashboard')

+     setProviderLandingRoute('/provider/profile')

+     navigate('/')

+   }

+ 

+   // NEW USER ROLE SELECTION: First-time users must choose their role

+   // WHY: roles (Admin/Provider/Applicant) determine which dashboard they access

+   // PROCESS:

+   // 1. User logs in with Google (new email, not in database yet)

+   // 2. We show role selection popup

+   // 3. User picks role ΓåÆ we insert into 'users' table with their email + role

+   // 4. Next login, step 2-3 are skipped (role already in database)

+   const handleRoleSelection = async (selectedRole) => {

+     if (!pendingEmail) {

+       return

+     }

+ 

+     setIsSavingRole(true)

+     setAuthError('')

+ 

+     const { data: insertedUser, error } = await supabase

+       .from('users')

+       .insert({ email: pendingEmail, role: selectedRole })

+       .select('role')

+       .single()

+ 

+     if (error) {

+       setIsSavingRole(false)

+       setAuthError('Could not save role selection. Please try again.')

+       return

+     }

+ 

+     const resolvedRole = insertedUser?.role || selectedRole

+     setRole(resolvedRole)

+     setPendingEmail('')

+     setIsSavingRole(false)

+ 

+     if (resolvedRole === 'Applicant') {

+       setApplicantLandingRoute('/profile')

+       navigate('/profile', { replace: true })

+       return

+     }

+ 

+     if (resolvedRole === 'Provider') {

+       setProviderLandingRoute('/provider/profile')

+       navigate('/provider/profile', { replace: true })

+       return

+     }

+ 

+     setApplicantLandingRoute(getLandingRoute(resolvedRole))

+     navigate(getLandingRoute(resolvedRole), { replace: true })

+   }

+ 

+   // Generate particle positions for 3D animation (748 dots in 34x22 grid)

+   // Complex math creates radial gradient effect that reacts to mouse movement

+   const dots = useMemo(() => {

+     const result = []

+     const columns = 34

+     const rows = 22

+ 

+     for (let row = 0; row < rows; row += 1) {

+       for (let col = 0; col < columns; col += 1) {

+         const index = row * columns + col

+         const nx = (col - (columns - 1) / 2) / ((columns - 1) / 2)

+         const ny = (row - (rows - 1) / 2) / ((rows - 1) / 2)

+         const r = Math.sqrt(nx * nx + ny * ny)

+         const z = (1 - Math.min(r, 1)) * 110 - Math.abs(nx) * 32

+         const dx = ((col % 5) - 2) * 1.6

+         const dy = ((row % 5) - 2) * 1.4

+         const dz = ((index % 7) - 3) * 2.2

+         const delay = `${(index % 11) * 0.18}s`

+         const duration = `${3.6 + (index % 6) * 0.6}s`

+         result.push({ nx, ny, z, id: `${row}-${col}`, dx, dy, dz, delay, duration })

+       }

+     }

+ 

+     return result

+   }, [])

+ 

+   // Track mouse position for 3D particle effect

+   const handleVisualMove = (event) => {

+     const rect = event.currentTarget.getBoundingClientRect()

+     const px = ((event.clientX - rect.left) / rect.width - 0.5) * 2

+     const py = ((event.clientY - rect.top) / rect.height - 0.5) * 2

+     setPointer({ x: px, y: py })

+   }

+ 

+   const resetVisualMove = () => setPointer({ x: 0, y: 0 })

+ 

+   // OAUTH ENTRY POINT: Initiate Google sign-in

+   // HOW: Calls Supabase.auth.signInWithOAuth() which opens Google login popup

+   // THEN: Auth listener (above) automatically detects successful login and looks up role

+   // NOTE: Don't manually handle role lookup hereΓÇölet the auth listener do it (keeps logic centralized)

+   const handleGoogleContinue = async () => {

+     setAuthError('')

+ 

+     if (!hasSupabaseConfig) {

+       setAuthError('Set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY before using Google login.')

+       return

+     }

+ 

+     setIsLoadingAuth(true)

+ 

+     clearOAuthTimeout()

+     oauthTimeoutRef.current = window.setTimeout(() => {

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in timed out. Please try again.')

+     }, 12000)

+ 

+     const { error } = await supabase.auth.signInWithOAuth({

+       provider: 'google',

+       options: {

+         redirectTo: oauthRedirectTo,

+       },

+     })

+ 

+     if (error) {

+       clearOAuthTimeout()

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in failed. Verify Supabase redirect URLs include your local host and VITE_AUTH_REDIRECT_URL value.')

+     }

+   }

+ 

+   // Fade in sections as they scroll into view

+   useEffect(() => {

+     if (location.pathname !== '/') {

+       return undefined

+     }

+ 

+     const nodes = document.querySelectorAll('.scroll-animate')

+ 

+     if (!nodes.length) {

+       return undefined

+     }

+ 

+     const observer = new IntersectionObserver(

+       (entries) => {

+         entries.forEach((entry) => {

+           if (entry.isIntersecting) {

+             entry.target.classList.add('is-visible') // Add CSS class to trigger fade-in

+           } else {

+             entry.target.classList.remove('is-visible') // Remove class when scrolled past

+           }

+         })

+       },

+       { threshold: 0.18 }, // Element must be 18% visible to trigger

+     )

+ 

+     nodes.forEach((node) => observer.observe(node))

+ 

+     return () => {

+       nodes.forEach((node) => observer.unobserve(node))

+       observer.disconnect()

+     }

+   }, [location.pathname])

+ 

+   // Track if user has scrolled past the hero section (used for topbar styling)

+   useEffect(() => {

+     const onScroll = () => setIsScrolled(window.scrollY > 120)

+     onScroll()

+     window.addEventListener('scroll', onScroll, { passive: true })

+     return () => window.removeEventListener('scroll', onScroll)

+   }, [])

+ 

+   // All routes: home page with auth, and protected role dashboards

+ return (

+   <Routes>

+     <Route path="/" element={

+       <main className="foundry-clone">

+ 

+         <header className={`global-topbar ${isScrolled ? 'is-scrolled' : ''}`}>

+           <div className="topbar-main">

+             <div className="topbar-brand">SA LEARNERSHIP FOUNDRY</div>

+             <nav className="topbar-nav">

+               <a href="#">Why Portal</a>

+               <a href="#">Pathways</a>

+               <a href="#">Team</a>

+               <a href="#">Insights</a>

+               <a href="#">Contact</a>

+             </nav>

+           </div>

+ 

+           <div className="topbar-auth">

+             {signedIn && role ? <span className="role-display">{role}</span> : null}

+             <button className="google-auth" onClick={signedIn ? handleLogout : handleGoogleContinue}>

+               {isLoadingAuth ? 'Authenticating...' : signedIn ? 'Logout' : 'Log In with Google'}

+             </button>

+           </div>

+         </header>

+ 

+         <section className="page-one">

+           <div className="hero-main">

+             <h1 className="left-title">

+               BUILDING TALENT

+               <br />

+               PATHWAYS THAT

+             </h1>

+ 

+             <div

+               className="interactive-model"

+               onMouseMove={handleVisualMove}

+               onMouseLeave={resetVisualMove}

+               style={{ '--px': pointer.x, '--py': pointer.y }}

+             >

+               <div className="orbital-core">

+                 <span className="orbit orbit-a"></span>

+                 <span className="orbit orbit-b"></span>

+                 <span className="orbit orbit-c"></span>

+               </div>

+ 

+               <div className="dot-volume">

+                 {dots.map((dot) => (

+                   <span

+                     key={dot.id}

+                     className="dot"

+                     style={{

+                       '--x': `${dot.nx * 340}px`,

+                       '--y': `${dot.ny * 240}px`,

+                       '--z': `${dot.z}px`,

+                     }}

+                   />

+                 ))}

+               </div>

+             </div>

+ 

+             <h1 className="right-title">

+               PUSH THE

+               <br />

+               FUTURE FORWARD

+             </h1>

+           </div>

+ 

+           <div className="hero-bottom">

+             <p>

+               A venture lab style platform for learnership access, application intelligence,

+               and practical support that moves candidates from skill to real work.

+             </p>

+ 

+             <div className="status-pill">

+               {signedIn && role

+                 ? `${role} workspace unlocked`

+                 : signedIn && pendingEmail

+                   ? 'Choose your role to continue'

+                   : 'Sign in with Google to continue'}

+             </div>

+           </div>

+           {signedIn && !role && pendingEmail ? (

+             <div className="role-onboarding-overlay" role="dialog" aria-modal="true" aria-labelledby="role-onboarding-title">

+               <div className="role-onboarding">

+                 <p className="role-onboarding-eyebrow">One more step</p>

+                 <h2 id="role-onboarding-title">Choose your role</h2>

+                 <p>Select how you want to use the platform for {pendingEmail}.</p>

+                 <div className="role-select">

+                   <button onClick={() => handleRoleSelection('Applicant')} disabled={isSavingRole}>

+                     Applicant

+                   </button>

+                   <button onClick={() => handleRoleSelection('Provider')} disabled={isSavingRole}>

+                     Provider

+                   </button>

+                 </div>

+               </div>

+             </div>

+           ) : null}

+           {authError ? <p className="auth-error">{authError}</p> : null}

+         </section>

+ 

+         <section className="page-two">

+           <div className="ethos-row scroll-animate">

+             <div>

+               <p className="mini-label">Why this platform</p>

+               <h2>

+                 OPEN DOORS

+                 <br />

+                 TO REAL WORK

+               </h2>

+             </div>

+ 

+             <div className="ethos-copy">

+               <p>

+                 Learnership Foundry helps applicants, providers, and moderators collaborate in one

+                 flow. Discover pathways, publish opportunities, and keep quality high with

+                 visibility from intake to placement.

+               </p>

+             </div>

+           </div>

+ 

+           <div className="card-strip scroll-animate swipe-left">

+             <article className="signal-card tone-dark">

+               <h3>Discover</h3>

+               <p>APPLICANT VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Find learnerships that match your interests and readiness level.</small>

+             </article>

+ 

+             <article className="signal-card tone-cyan">

+               <h3>Publish</h3>

+               <p>PROVIDER VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Create listings and track engagement from qualified candidates.</small>

+             </article>

+ 

+             <article className="signal-card tone-orange">

+               <h3>Moderate</h3>

+               <p>ADMIN VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Flag risk, request fixes, and approve opportunities confidently.</small>

+             </article>

+ 

+             <article className="signal-card tone-light">

+               <h3>Move</h3>

+               <p>OUTCOMES</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Turn applications into skills, credentials, and workplace entry points.</small>

+             </article>

+           </div>

+         </section>

+ 

+         <section className="page-three">

+           <span className="dot-plane" aria-hidden="true"></span>

+           <div className="focus-copy scroll-animate">

+             <p className="mini-label">Built for momentum</p>

+             <h2>

+               From FIRST CLICK

+               <br />

+               TO FIRST PAYCHECK

+             </h2>

+             <p>

+               Scroll the platform, choose your role, and move into the right workspace to continue.

+             </p>

+           </div>

+         </section>

+ 

+       </main>

+     } />

+ 

+     

+    <Route path="/dashboard" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <Dashboard onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+ <Route path="/dashboard/listings/:listingId" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ApplicantListingDetail onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   <Route path="/profile" element={

+     <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+       <ApplicantProfile onLogout={handleLogout} />

+     </ProtectedRoute>

+   } />

+ 

+ <Route path="/provider" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <Provider onLogout={handleLogout} />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/profile" element={

+   <ProviderProfileRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ProviderProfile

+       onLogout={handleLogout}

+       onProfileSaved={() => setProviderLandingRoute('/provider')}

+     />

+   </ProviderProfileRoute>

+ } />

+ 

+ <Route path="/provider/listings/new" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingForm />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/edit" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingEdit />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/applications" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingApplications />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/admin" element={

+   <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <AdminDashboardShell onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   </Routes>

+ )

+ }

+ 

+ export default App

+

 Γ¥» src/App.test.jsx:115:23
    113|     const appSource = readFileSync(resolve(cwd(), 'src/App.jsx'), 'utfΓÇª
    114|
    115|     expect(appSource).toContain("if (role === 'Admin') return '/admin'ΓÇª
       |                       ^
    116|   })
    117|

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[19/28]ΓÄ»

 FAIL  src/App.test.jsx > Role based tests > getLandingRoute function routes each role correctly
AssertionError: expected '// Main app: handles Google OAuth, roΓÇª' to contain 'function getLandingRoute(role)'

- Expected
+ Received

- function getLandingRoute(role)
+ // Main app: handles Google OAuth, role-based routing, and admin dashboard

+ // TRICKY PARTS:

+ // - OAuth flow: Google login ΓåÆ email lookup ΓåÆ role selection (if new user)

+ // - Session persistence: browser cookies restored on page refresh

+ // - Route guards: ProtectedRoute blocks unauthorized access

+ // - State management: tracking auth, loading, roles, and pending signups

+ // - 3D animation: particle effect that reacts to mouse movement

+ 

+ import { useCallback, useEffect, useMemo, useRef, useState } from 'react'

+ import './App.css'

+ import { Routes, Route, useNavigate, useLocation, Navigate } from 'react-router-dom'

+ import Admin from './pages/Admin'

+ import Dashboard from './pages/Dashboard'

+ import ApplicantProfile from './pages/ApplicantProfile'

+ import ApplicantListingDetail from './pages/ApplicantListingDetail'

+ import Provider from './pages/Provider'

+ import ProviderProfile from './pages/ProviderProfile'

+ import ProviderListingForm from './pages/ProviderListingForm'

+ import ProviderListingEdit from './pages/ProviderListingEdit'

+ import ProviderListingApplications from './pages/ProviderListingApplications'

+ import { hasSupabaseConfig, supabase } from './lib/supabaseClient'

+ import {

+   getLandingRoute,

+   getRoleForEmail,

+   getApplicantLandingRouteForEmail,

+   getProviderLandingRouteForEmail,

+ } from './app-helpers'

+ const AdminDashboardShell = Admin

+ 

+ // ProtectedRoute: Guard component that restricts access to role-specific pages

+ // USAGE: <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}><Admin /></ProtectedRoute>

+ // LOGIC:

+ // 1. If still checking auth ΓåÆ show loading shell (don't flash wrong page)

+ // 2. If not signed in ΓåÆ redirect to home (must log in)

+ // 3. If wrong role ΓåÆ redirect to home (prevent unauthorized access)

+ // 4. All checks pass ΓåÆ render the protected content

+ function ProtectedRoute({ role, allowedRole, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   // Show loading state while session is being verified from browser cookies

+   // If we're still loading but already know the user is signed in, render

+   // the protected content so a refresh doesn't lock the user out temporarily.

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   // Unauthenticated users cannot access protected routes

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // Role mismatch: user is logged in but lacks permission for this page

+   // Example: Applicant (role="Applicant") trying to access /admin (allowedRole="Admin")

+   if (role !== allowedRole) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // All security checks passed: render the page

+   return children

+ }

+ 

+ function ProviderWorkspaceRoute({ role, signedIn, isLoading, providerLandingRoute, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (providerLandingRoute !== '/provider') {

+     return <Navigate to="/provider/profile" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function ProviderProfileRoute({ role, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function App() {

+   // Authentication state management:

+   const [role, setRole] = useState(null) // Current user's role: 'Admin', 'Provider', 'Applicant', or null

+   const [signedIn, setSignedIn] = useState(false) // True if OAuth session is active

+   const [isLoadingAuth, setIsLoadingAuth] = useState(true) // True while checking session from browser cookies

+   const [authError, setAuthError] = useState('') // Display auth errors to user

+   const [pendingEmail, setPendingEmail] = useState('') // New user email waiting for role selection

+   const [isSavingRole, setIsSavingRole] = useState(false) // True while inserting user into database

+   const [applicantLandingRoute, setApplicantLandingRoute] = useState('/dashboard')

+   const [providerLandingRoute, setProviderLandingRoute] = useState('/provider/profile')

+   const oauthTimeoutRef = useRef(null)

+ 

+   // Animation state for the 3D particle effect

+   const [pointer, setPointer] = useState({ x: 0, y: 0 })

+   const [isScrolled, setIsScrolled] = useState(false)

+   const navigate = useNavigate()

+   const location = useLocation()

+   const oauthRedirectTo =

+     import.meta.env.VITE_AUTH_REDIRECT_URL?.trim() || `${window.location.origin}/`

+ 

+   const clearOAuthTimeout = useCallback(() => {

+     if (oauthTimeoutRef.current) {

+       window.clearTimeout(oauthTimeoutRef.current)

+       oauthTimeoutRef.current = null

+     }

+   }, [])

+ 

+ // helper functions are implemented in ./app-helpers and imported above

+ 

+   // AUTH BOOTSTRAP: Restore user session on app load and listen for auth changes

+   // FLOW:

+   // 1. Check if session exists in browser cookies (user already logged in before)

+   // 2. Lookup user's role in database by email

+   // 3. Set up listener to detect sign-in/sign-out events

+   // 4. When sign-in happens, role lookup and optional role selection for new users

+   // WHY: Ensures app state matches Supabase auth session, prevents stale data on refresh

+   useEffect(() => {

+     let isMounted = true

+ 

+     const initializeAuth = async () => {

+       setIsLoadingAuth(true)

+ 

+       if (!hasSupabaseConfig) {

+         if (isMounted) {

+           setAuthError('Missing Supabase environment variables. Add VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const { data, error } = await supabase.auth.getSession()

+ 

+       if (error) {

+         if (isMounted) {

+           setAuthError('Unable to restore your session. Please sign in again.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const existingSession = data.session

+ 

+       if (!existingSession?.user?.email) {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(existingSession.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(existingSession.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(existingSession.user.email)

+             : getLandingRoute(resolvedRole)

+         if (isMounted) {

+           setSignedIn(true)

+           setRole(resolvedRole)

+           setPendingEmail(resolvedRole ? '' : existingSession.user.email)

+           setApplicantLandingRoute(resolvedLandingRoute)

+           setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+           setAuthError('')

+         }

+       } catch {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setAuthError('Signed in, but role lookup failed. Check your users table and policies.')

+         }

+       } finally {

+         if (isMounted) {

+           setIsLoadingAuth(false)

+         }

+       }

+     }

+ 

+     initializeAuth()

+ 

+     if (!hasSupabaseConfig) {

+       return () => {

+         isMounted = false

+       }

+     }

+ 

+     const {

+       data: { subscription },

+     } = supabase.auth.onAuthStateChange(async (event, session) => {

+       if (!isMounted) {

+         return

+       }

+ 

+       if (event === 'SIGNED_OUT' || !session?.user?.email) {

+         clearOAuthTimeout()

+         setSignedIn(false)

+         setRole(null)

+         setPendingEmail('')

+         setIsSavingRole(false)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setIsLoadingAuth(false)

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(session.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(session.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(session.user.email)

+             : getLandingRoute(resolvedRole)

+         if (!isMounted) {

+           return

+         }

+         clearOAuthTimeout()

+         setSignedIn(true)

+         setRole(resolvedRole)

+         setPendingEmail(resolvedRole ? '' : session.user.email)

+         setApplicantLandingRoute(resolvedLandingRoute)

+         setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+         setAuthError('')

+       } catch {

+         if (!isMounted) {

+           return

+         }

+         setSignedIn(false)

+         setRole(null)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setAuthError('OAuth succeeded, but role lookup failed. Please verify Supabase table policies.')

+       } finally {

+         if (isMounted) {

+           clearOAuthTimeout()

+           setIsLoadingAuth(false)

+         }

+       }

+     })

+ 

+     return () => {

+       clearOAuthTimeout()

+       isMounted = false

+       subscription.unsubscribe()

+     }

+   }, [clearOAuthTimeout])

+ 

+   // AUTO-REDIRECT: Send logged-in users to their role-appropriate dashboard

+   // WHEN: User has loaded auth state, is signed in, has a role, and is on home page

+   // REDIRECTS: Admin ΓåÆ /admin, Provider ΓåÆ /provider, Applicant ΓåÆ /dashboard

+   useEffect(() => {

+     const redirectedFromProtectedRoute = Boolean(location.state?.from)

+     const keepApplicantOnHome = redirectedFromProtectedRoute && role === 'Applicant'

+     const landingRoute =

+       role === 'Applicant'

+         ? applicantLandingRoute

+         : role === 'Provider'

+           ? providerLandingRoute

+           : getLandingRoute(role)

+ 

+     if (!isLoadingAuth && signedIn && role && location.pathname === '/' && !keepApplicantOnHome) {

+       navigate(landingRoute, { replace: true })

+     }

+   }, [isLoadingAuth, signedIn, role, applicantLandingRoute, providerLandingRoute, location.pathname, location.state, navigate])

+ 

+   // Clear session and reset all state when logging out

+   const handleLogout = async () => {

+     clearOAuthTimeout()

+ 

+     if (hasSupabaseConfig) {

+       await supabase.auth.signOut() // Clear session cookie

+     }

+     setSignedIn(false)

+     setRole(null)

+     setPendingEmail('')

+     setIsSavingRole(false)

+     setApplicantLandingRoute('/dashboard')

+     setProviderLandingRoute('/provider/profile')

+     navigate('/')

+   }

+ 

+   // NEW USER ROLE SELECTION: First-time users must choose their role

+   // WHY: roles (Admin/Provider/Applicant) determine which dashboard they access

+   // PROCESS:

+   // 1. User logs in with Google (new email, not in database yet)

+   // 2. We show role selection popup

+   // 3. User picks role ΓåÆ we insert into 'users' table with their email + role

+   // 4. Next login, step 2-3 are skipped (role already in database)

+   const handleRoleSelection = async (selectedRole) => {

+     if (!pendingEmail) {

+       return

+     }

+ 

+     setIsSavingRole(true)

+     setAuthError('')

+ 

+     const { data: insertedUser, error } = await supabase

+       .from('users')

+       .insert({ email: pendingEmail, role: selectedRole })

+       .select('role')

+       .single()

+ 

+     if (error) {

+       setIsSavingRole(false)

+       setAuthError('Could not save role selection. Please try again.')

+       return

+     }

+ 

+     const resolvedRole = insertedUser?.role || selectedRole

+     setRole(resolvedRole)

+     setPendingEmail('')

+     setIsSavingRole(false)

+ 

+     if (resolvedRole === 'Applicant') {

+       setApplicantLandingRoute('/profile')

+       navigate('/profile', { replace: true })

+       return

+     }

+ 

+     if (resolvedRole === 'Provider') {

+       setProviderLandingRoute('/provider/profile')

+       navigate('/provider/profile', { replace: true })

+       return

+     }

+ 

+     setApplicantLandingRoute(getLandingRoute(resolvedRole))

+     navigate(getLandingRoute(resolvedRole), { replace: true })

+   }

+ 

+   // Generate particle positions for 3D animation (748 dots in 34x22 grid)

+   // Complex math creates radial gradient effect that reacts to mouse movement

+   const dots = useMemo(() => {

+     const result = []

+     const columns = 34

+     const rows = 22

+ 

+     for (let row = 0; row < rows; row += 1) {

+       for (let col = 0; col < columns; col += 1) {

+         const index = row * columns + col

+         const nx = (col - (columns - 1) / 2) / ((columns - 1) / 2)

+         const ny = (row - (rows - 1) / 2) / ((rows - 1) / 2)

+         const r = Math.sqrt(nx * nx + ny * ny)

+         const z = (1 - Math.min(r, 1)) * 110 - Math.abs(nx) * 32

+         const dx = ((col % 5) - 2) * 1.6

+         const dy = ((row % 5) - 2) * 1.4

+         const dz = ((index % 7) - 3) * 2.2

+         const delay = `${(index % 11) * 0.18}s`

+         const duration = `${3.6 + (index % 6) * 0.6}s`

+         result.push({ nx, ny, z, id: `${row}-${col}`, dx, dy, dz, delay, duration })

+       }

+     }

+ 

+     return result

+   }, [])

+ 

+   // Track mouse position for 3D particle effect

+   const handleVisualMove = (event) => {

+     const rect = event.currentTarget.getBoundingClientRect()

+     const px = ((event.clientX - rect.left) / rect.width - 0.5) * 2

+     const py = ((event.clientY - rect.top) / rect.height - 0.5) * 2

+     setPointer({ x: px, y: py })

+   }

+ 

+   const resetVisualMove = () => setPointer({ x: 0, y: 0 })

+ 

+   // OAUTH ENTRY POINT: Initiate Google sign-in

+   // HOW: Calls Supabase.auth.signInWithOAuth() which opens Google login popup

+   // THEN: Auth listener (above) automatically detects successful login and looks up role

+   // NOTE: Don't manually handle role lookup hereΓÇölet the auth listener do it (keeps logic centralized)

+   const handleGoogleContinue = async () => {

+     setAuthError('')

+ 

+     if (!hasSupabaseConfig) {

+       setAuthError('Set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY before using Google login.')

+       return

+     }

+ 

+     setIsLoadingAuth(true)

+ 

+     clearOAuthTimeout()

+     oauthTimeoutRef.current = window.setTimeout(() => {

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in timed out. Please try again.')

+     }, 12000)

+ 

+     const { error } = await supabase.auth.signInWithOAuth({

+       provider: 'google',

+       options: {

+         redirectTo: oauthRedirectTo,

+       },

+     })

+ 

+     if (error) {

+       clearOAuthTimeout()

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in failed. Verify Supabase redirect URLs include your local host and VITE_AUTH_REDIRECT_URL value.')

+     }

+   }

+ 

+   // Fade in sections as they scroll into view

+   useEffect(() => {

+     if (location.pathname !== '/') {

+       return undefined

+     }

+ 

+     const nodes = document.querySelectorAll('.scroll-animate')

+ 

+     if (!nodes.length) {

+       return undefined

+     }

+ 

+     const observer = new IntersectionObserver(

+       (entries) => {

+         entries.forEach((entry) => {

+           if (entry.isIntersecting) {

+             entry.target.classList.add('is-visible') // Add CSS class to trigger fade-in

+           } else {

+             entry.target.classList.remove('is-visible') // Remove class when scrolled past

+           }

+         })

+       },

+       { threshold: 0.18 }, // Element must be 18% visible to trigger

+     )

+ 

+     nodes.forEach((node) => observer.observe(node))

+ 

+     return () => {

+       nodes.forEach((node) => observer.unobserve(node))

+       observer.disconnect()

+     }

+   }, [location.pathname])

+ 

+   // Track if user has scrolled past the hero section (used for topbar styling)

+   useEffect(() => {

+     const onScroll = () => setIsScrolled(window.scrollY > 120)

+     onScroll()

+     window.addEventListener('scroll', onScroll, { passive: true })

+     return () => window.removeEventListener('scroll', onScroll)

+   }, [])

+ 

+   // All routes: home page with auth, and protected role dashboards

+ return (

+   <Routes>

+     <Route path="/" element={

+       <main className="foundry-clone">

+ 

+         <header className={`global-topbar ${isScrolled ? 'is-scrolled' : ''}`}>

+           <div className="topbar-main">

+             <div className="topbar-brand">SA LEARNERSHIP FOUNDRY</div>

+             <nav className="topbar-nav">

+               <a href="#">Why Portal</a>

+               <a href="#">Pathways</a>

+               <a href="#">Team</a>

+               <a href="#">Insights</a>

+               <a href="#">Contact</a>

+             </nav>

+           </div>

+ 

+           <div className="topbar-auth">

+             {signedIn && role ? <span className="role-display">{role}</span> : null}

+             <button className="google-auth" onClick={signedIn ? handleLogout : handleGoogleContinue}>

+               {isLoadingAuth ? 'Authenticating...' : signedIn ? 'Logout' : 'Log In with Google'}

+             </button>

+           </div>

+         </header>

+ 

+         <section className="page-one">

+           <div className="hero-main">

+             <h1 className="left-title">

+               BUILDING TALENT

+               <br />

+               PATHWAYS THAT

+             </h1>

+ 

+             <div

+               className="interactive-model"

+               onMouseMove={handleVisualMove}

+               onMouseLeave={resetVisualMove}

+               style={{ '--px': pointer.x, '--py': pointer.y }}

+             >

+               <div className="orbital-core">

+                 <span className="orbit orbit-a"></span>

+                 <span className="orbit orbit-b"></span>

+                 <span className="orbit orbit-c"></span>

+               </div>

+ 

+               <div className="dot-volume">

+                 {dots.map((dot) => (

+                   <span

+                     key={dot.id}

+                     className="dot"

+                     style={{

+                       '--x': `${dot.nx * 340}px`,

+                       '--y': `${dot.ny * 240}px`,

+                       '--z': `${dot.z}px`,

+                     }}

+                   />

+                 ))}

+               </div>

+             </div>

+ 

+             <h1 className="right-title">

+               PUSH THE

+               <br />

+               FUTURE FORWARD

+             </h1>

+           </div>

+ 

+           <div className="hero-bottom">

+             <p>

+               A venture lab style platform for learnership access, application intelligence,

+               and practical support that moves candidates from skill to real work.

+             </p>

+ 

+             <div className="status-pill">

+               {signedIn && role

+                 ? `${role} workspace unlocked`

+                 : signedIn && pendingEmail

+                   ? 'Choose your role to continue'

+                   : 'Sign in with Google to continue'}

+             </div>

+           </div>

+           {signedIn && !role && pendingEmail ? (

+             <div className="role-onboarding-overlay" role="dialog" aria-modal="true" aria-labelledby="role-onboarding-title">

+               <div className="role-onboarding">

+                 <p className="role-onboarding-eyebrow">One more step</p>

+                 <h2 id="role-onboarding-title">Choose your role</h2>

+                 <p>Select how you want to use the platform for {pendingEmail}.</p>

+                 <div className="role-select">

+                   <button onClick={() => handleRoleSelection('Applicant')} disabled={isSavingRole}>

+                     Applicant

+                   </button>

+                   <button onClick={() => handleRoleSelection('Provider')} disabled={isSavingRole}>

+                     Provider

+                   </button>

+                 </div>

+               </div>

+             </div>

+           ) : null}

+           {authError ? <p className="auth-error">{authError}</p> : null}

+         </section>

+ 

+         <section className="page-two">

+           <div className="ethos-row scroll-animate">

+             <div>

+               <p className="mini-label">Why this platform</p>

+               <h2>

+                 OPEN DOORS

+                 <br />

+                 TO REAL WORK

+               </h2>

+             </div>

+ 

+             <div className="ethos-copy">

+               <p>

+                 Learnership Foundry helps applicants, providers, and moderators collaborate in one

+                 flow. Discover pathways, publish opportunities, and keep quality high with

+                 visibility from intake to placement.

+               </p>

+             </div>

+           </div>

+ 

+           <div className="card-strip scroll-animate swipe-left">

+             <article className="signal-card tone-dark">

+               <h3>Discover</h3>

+               <p>APPLICANT VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Find learnerships that match your interests and readiness level.</small>

+             </article>

+ 

+             <article className="signal-card tone-cyan">

+               <h3>Publish</h3>

+               <p>PROVIDER VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Create listings and track engagement from qualified candidates.</small>

+             </article>

+ 

+             <article className="signal-card tone-orange">

+               <h3>Moderate</h3>

+               <p>ADMIN VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Flag risk, request fixes, and approve opportunities confidently.</small>

+             </article>

+ 

+             <article className="signal-card tone-light">

+               <h3>Move</h3>

+               <p>OUTCOMES</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Turn applications into skills, credentials, and workplace entry points.</small>

+             </article>

+           </div>

+         </section>

+ 

+         <section className="page-three">

+           <span className="dot-plane" aria-hidden="true"></span>

+           <div className="focus-copy scroll-animate">

+             <p className="mini-label">Built for momentum</p>

+             <h2>

+               From FIRST CLICK

+               <br />

+               TO FIRST PAYCHECK

+             </h2>

+             <p>

+               Scroll the platform, choose your role, and move into the right workspace to continue.

+             </p>

+           </div>

+         </section>

+ 

+       </main>

+     } />

+ 

+     

+    <Route path="/dashboard" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <Dashboard onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+ <Route path="/dashboard/listings/:listingId" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ApplicantListingDetail onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   <Route path="/profile" element={

+     <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+       <ApplicantProfile onLogout={handleLogout} />

+     </ProtectedRoute>

+   } />

+ 

+ <Route path="/provider" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <Provider onLogout={handleLogout} />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/profile" element={

+   <ProviderProfileRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ProviderProfile

+       onLogout={handleLogout}

+       onProfileSaved={() => setProviderLandingRoute('/provider')}

+     />

+   </ProviderProfileRoute>

+ } />

+ 

+ <Route path="/provider/listings/new" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingForm />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/edit" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingEdit />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/applications" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingApplications />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/admin" element={

+   <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <AdminDashboardShell onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   </Routes>

+ )

+ }

+ 

+ export default App

+

 Γ¥» src/App.test.jsx:160:23
    158|     const appSource = readFileSync(resolve(cwd(), 'src/App.jsx'), 'utfΓÇª
    159|
    160|     expect(appSource).toContain('function getLandingRoute(role)')
       |                       ^
    161|     expect(appSource).toContain("if (role === 'Admin') return '/admin'ΓÇª
    162|     expect(appSource).toContain("if (role === 'Provider') return '/proΓÇª

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[20/28]ΓÄ»

 FAIL  src/pages/AdminPage.test.jsx > ADMIN-ACCEPTANCE-2: OAuth callback checks email against admin list and assigns Admin role
AssertionError: expected '// Main app: handles Google OAuth, roΓÇª' to contain 'if (role === \'Admin\')'

- Expected
+ Received

- if (role === 'Admin')
+ // Main app: handles Google OAuth, role-based routing, and admin dashboard

+ // TRICKY PARTS:

+ // - OAuth flow: Google login ΓåÆ email lookup ΓåÆ role selection (if new user)

+ // - Session persistence: browser cookies restored on page refresh

+ // - Route guards: ProtectedRoute blocks unauthorized access

+ // - State management: tracking auth, loading, roles, and pending signups

+ // - 3D animation: particle effect that reacts to mouse movement

+ 

+ import { useCallback, useEffect, useMemo, useRef, useState } from 'react'

+ import './App.css'

+ import { Routes, Route, useNavigate, useLocation, Navigate } from 'react-router-dom'

+ import Admin from './pages/Admin'

+ import Dashboard from './pages/Dashboard'

+ import ApplicantProfile from './pages/ApplicantProfile'

+ import ApplicantListingDetail from './pages/ApplicantListingDetail'

+ import Provider from './pages/Provider'

+ import ProviderProfile from './pages/ProviderProfile'

+ import ProviderListingForm from './pages/ProviderListingForm'

+ import ProviderListingEdit from './pages/ProviderListingEdit'

+ import ProviderListingApplications from './pages/ProviderListingApplications'

+ import { hasSupabaseConfig, supabase } from './lib/supabaseClient'

+ import {

+   getLandingRoute,

+   getRoleForEmail,

+   getApplicantLandingRouteForEmail,

+   getProviderLandingRouteForEmail,

+ } from './app-helpers'

+ const AdminDashboardShell = Admin

+ 

+ // ProtectedRoute: Guard component that restricts access to role-specific pages

+ // USAGE: <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}><Admin /></ProtectedRoute>

+ // LOGIC:

+ // 1. If still checking auth ΓåÆ show loading shell (don't flash wrong page)

+ // 2. If not signed in ΓåÆ redirect to home (must log in)

+ // 3. If wrong role ΓåÆ redirect to home (prevent unauthorized access)

+ // 4. All checks pass ΓåÆ render the protected content

+ function ProtectedRoute({ role, allowedRole, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   // Show loading state while session is being verified from browser cookies

+   // If we're still loading but already know the user is signed in, render

+   // the protected content so a refresh doesn't lock the user out temporarily.

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   // Unauthenticated users cannot access protected routes

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // Role mismatch: user is logged in but lacks permission for this page

+   // Example: Applicant (role="Applicant") trying to access /admin (allowedRole="Admin")

+   if (role !== allowedRole) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // All security checks passed: render the page

+   return children

+ }

+ 

+ function ProviderWorkspaceRoute({ role, signedIn, isLoading, providerLandingRoute, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (providerLandingRoute !== '/provider') {

+     return <Navigate to="/provider/profile" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function ProviderProfileRoute({ role, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function App() {

+   // Authentication state management:

+   const [role, setRole] = useState(null) // Current user's role: 'Admin', 'Provider', 'Applicant', or null

+   const [signedIn, setSignedIn] = useState(false) // True if OAuth session is active

+   const [isLoadingAuth, setIsLoadingAuth] = useState(true) // True while checking session from browser cookies

+   const [authError, setAuthError] = useState('') // Display auth errors to user

+   const [pendingEmail, setPendingEmail] = useState('') // New user email waiting for role selection

+   const [isSavingRole, setIsSavingRole] = useState(false) // True while inserting user into database

+   const [applicantLandingRoute, setApplicantLandingRoute] = useState('/dashboard')

+   const [providerLandingRoute, setProviderLandingRoute] = useState('/provider/profile')

+   const oauthTimeoutRef = useRef(null)

+ 

+   // Animation state for the 3D particle effect

+   const [pointer, setPointer] = useState({ x: 0, y: 0 })

+   const [isScrolled, setIsScrolled] = useState(false)

+   const navigate = useNavigate()

+   const location = useLocation()

+   const oauthRedirectTo =

+     import.meta.env.VITE_AUTH_REDIRECT_URL?.trim() || `${window.location.origin}/`

+ 

+   const clearOAuthTimeout = useCallback(() => {

+     if (oauthTimeoutRef.current) {

+       window.clearTimeout(oauthTimeoutRef.current)

+       oauthTimeoutRef.current = null

+     }

+   }, [])

+ 

+ // helper functions are implemented in ./app-helpers and imported above

+ 

+   // AUTH BOOTSTRAP: Restore user session on app load and listen for auth changes

+   // FLOW:

+   // 1. Check if session exists in browser cookies (user already logged in before)

+   // 2. Lookup user's role in database by email

+   // 3. Set up listener to detect sign-in/sign-out events

+   // 4. When sign-in happens, role lookup and optional role selection for new users

+   // WHY: Ensures app state matches Supabase auth session, prevents stale data on refresh

+   useEffect(() => {

+     let isMounted = true

+ 

+     const initializeAuth = async () => {

+       setIsLoadingAuth(true)

+ 

+       if (!hasSupabaseConfig) {

+         if (isMounted) {

+           setAuthError('Missing Supabase environment variables. Add VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const { data, error } = await supabase.auth.getSession()

+ 

+       if (error) {

+         if (isMounted) {

+           setAuthError('Unable to restore your session. Please sign in again.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const existingSession = data.session

+ 

+       if (!existingSession?.user?.email) {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(existingSession.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(existingSession.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(existingSession.user.email)

+             : getLandingRoute(resolvedRole)

+         if (isMounted) {

+           setSignedIn(true)

+           setRole(resolvedRole)

+           setPendingEmail(resolvedRole ? '' : existingSession.user.email)

+           setApplicantLandingRoute(resolvedLandingRoute)

+           setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+           setAuthError('')

+         }

+       } catch {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setAuthError('Signed in, but role lookup failed. Check your users table and policies.')

+         }

+       } finally {

+         if (isMounted) {

+           setIsLoadingAuth(false)

+         }

+       }

+     }

+ 

+     initializeAuth()

+ 

+     if (!hasSupabaseConfig) {

+       return () => {

+         isMounted = false

+       }

+     }

+ 

+     const {

+       data: { subscription },

+     } = supabase.auth.onAuthStateChange(async (event, session) => {

+       if (!isMounted) {

+         return

+       }

+ 

+       if (event === 'SIGNED_OUT' || !session?.user?.email) {

+         clearOAuthTimeout()

+         setSignedIn(false)

+         setRole(null)

+         setPendingEmail('')

+         setIsSavingRole(false)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setIsLoadingAuth(false)

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(session.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(session.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(session.user.email)

+             : getLandingRoute(resolvedRole)

+         if (!isMounted) {

+           return

+         }

+         clearOAuthTimeout()

+         setSignedIn(true)

+         setRole(resolvedRole)

+         setPendingEmail(resolvedRole ? '' : session.user.email)

+         setApplicantLandingRoute(resolvedLandingRoute)

+         setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+         setAuthError('')

+       } catch {

+         if (!isMounted) {

+           return

+         }

+         setSignedIn(false)

+         setRole(null)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setAuthError('OAuth succeeded, but role lookup failed. Please verify Supabase table policies.')

+       } finally {

+         if (isMounted) {

+           clearOAuthTimeout()

+           setIsLoadingAuth(false)

+         }

+       }

+     })

+ 

+     return () => {

+       clearOAuthTimeout()

+       isMounted = false

+       subscription.unsubscribe()

+     }

+   }, [clearOAuthTimeout])

+ 

+   // AUTO-REDIRECT: Send logged-in users to their role-appropriate dashboard

+   // WHEN: User has loaded auth state, is signed in, has a role, and is on home page

+   // REDIRECTS: Admin ΓåÆ /admin, Provider ΓåÆ /provider, Applicant ΓåÆ /dashboard

+   useEffect(() => {

+     const redirectedFromProtectedRoute = Boolean(location.state?.from)

+     const keepApplicantOnHome = redirectedFromProtectedRoute && role === 'Applicant'

+     const landingRoute =

+       role === 'Applicant'

+         ? applicantLandingRoute

+         : role === 'Provider'

+           ? providerLandingRoute

+           : getLandingRoute(role)

+ 

+     if (!isLoadingAuth && signedIn && role && location.pathname === '/' && !keepApplicantOnHome) {

+       navigate(landingRoute, { replace: true })

+     }

+   }, [isLoadingAuth, signedIn, role, applicantLandingRoute, providerLandingRoute, location.pathname, location.state, navigate])

+ 

+   // Clear session and reset all state when logging out

+   const handleLogout = async () => {

+     clearOAuthTimeout()

+ 

+     if (hasSupabaseConfig) {

+       await supabase.auth.signOut() // Clear session cookie

+     }

+     setSignedIn(false)

+     setRole(null)

+     setPendingEmail('')

+     setIsSavingRole(false)

+     setApplicantLandingRoute('/dashboard')

+     setProviderLandingRoute('/provider/profile')

+     navigate('/')

+   }

+ 

+   // NEW USER ROLE SELECTION: First-time users must choose their role

+   // WHY: roles (Admin/Provider/Applicant) determine which dashboard they access

+   // PROCESS:

+   // 1. User logs in with Google (new email, not in database yet)

+   // 2. We show role selection popup

+   // 3. User picks role ΓåÆ we insert into 'users' table with their email + role

+   // 4. Next login, step 2-3 are skipped (role already in database)

+   const handleRoleSelection = async (selectedRole) => {

+     if (!pendingEmail) {

+       return

+     }

+ 

+     setIsSavingRole(true)

+     setAuthError('')

+ 

+     const { data: insertedUser, error } = await supabase

+       .from('users')

+       .insert({ email: pendingEmail, role: selectedRole })

+       .select('role')

+       .single()

+ 

+     if (error) {

+       setIsSavingRole(false)

+       setAuthError('Could not save role selection. Please try again.')

+       return

+     }

+ 

+     const resolvedRole = insertedUser?.role || selectedRole

+     setRole(resolvedRole)

+     setPendingEmail('')

+     setIsSavingRole(false)

+ 

+     if (resolvedRole === 'Applicant') {

+       setApplicantLandingRoute('/profile')

+       navigate('/profile', { replace: true })

+       return

+     }

+ 

+     if (resolvedRole === 'Provider') {

+       setProviderLandingRoute('/provider/profile')

+       navigate('/provider/profile', { replace: true })

+       return

+     }

+ 

+     setApplicantLandingRoute(getLandingRoute(resolvedRole))

+     navigate(getLandingRoute(resolvedRole), { replace: true })

+   }

+ 

+   // Generate particle positions for 3D animation (748 dots in 34x22 grid)

+   // Complex math creates radial gradient effect that reacts to mouse movement

+   const dots = useMemo(() => {

+     const result = []

+     const columns = 34

+     const rows = 22

+ 

+     for (let row = 0; row < rows; row += 1) {

+       for (let col = 0; col < columns; col += 1) {

+         const index = row * columns + col

+         const nx = (col - (columns - 1) / 2) / ((columns - 1) / 2)

+         const ny = (row - (rows - 1) / 2) / ((rows - 1) / 2)

+         const r = Math.sqrt(nx * nx + ny * ny)

+         const z = (1 - Math.min(r, 1)) * 110 - Math.abs(nx) * 32

+         const dx = ((col % 5) - 2) * 1.6

+         const dy = ((row % 5) - 2) * 1.4

+         const dz = ((index % 7) - 3) * 2.2

+         const delay = `${(index % 11) * 0.18}s`

+         const duration = `${3.6 + (index % 6) * 0.6}s`

+         result.push({ nx, ny, z, id: `${row}-${col}`, dx, dy, dz, delay, duration })

+       }

+     }

+ 

+     return result

+   }, [])

+ 

+   // Track mouse position for 3D particle effect

+   const handleVisualMove = (event) => {

+     const rect = event.currentTarget.getBoundingClientRect()

+     const px = ((event.clientX - rect.left) / rect.width - 0.5) * 2

+     const py = ((event.clientY - rect.top) / rect.height - 0.5) * 2

+     setPointer({ x: px, y: py })

+   }

+ 

+   const resetVisualMove = () => setPointer({ x: 0, y: 0 })

+ 

+   // OAUTH ENTRY POINT: Initiate Google sign-in

+   // HOW: Calls Supabase.auth.signInWithOAuth() which opens Google login popup

+   // THEN: Auth listener (above) automatically detects successful login and looks up role

+   // NOTE: Don't manually handle role lookup hereΓÇölet the auth listener do it (keeps logic centralized)

+   const handleGoogleContinue = async () => {

+     setAuthError('')

+ 

+     if (!hasSupabaseConfig) {

+       setAuthError('Set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY before using Google login.')

+       return

+     }

+ 

+     setIsLoadingAuth(true)

+ 

+     clearOAuthTimeout()

+     oauthTimeoutRef.current = window.setTimeout(() => {

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in timed out. Please try again.')

+     }, 12000)

+ 

+     const { error } = await supabase.auth.signInWithOAuth({

+       provider: 'google',

+       options: {

+         redirectTo: oauthRedirectTo,

+       },

+     })

+ 

+     if (error) {

+       clearOAuthTimeout()

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in failed. Verify Supabase redirect URLs include your local host and VITE_AUTH_REDIRECT_URL value.')

+     }

+   }

+ 

+   // Fade in sections as they scroll into view

+   useEffect(() => {

+     if (location.pathname !== '/') {

+       return undefined

+     }

+ 

+     const nodes = document.querySelectorAll('.scroll-animate')

+ 

+     if (!nodes.length) {

+       return undefined

+     }

+ 

+     const observer = new IntersectionObserver(

+       (entries) => {

+         entries.forEach((entry) => {

+           if (entry.isIntersecting) {

+             entry.target.classList.add('is-visible') // Add CSS class to trigger fade-in

+           } else {

+             entry.target.classList.remove('is-visible') // Remove class when scrolled past

+           }

+         })

+       },

+       { threshold: 0.18 }, // Element must be 18% visible to trigger

+     )

+ 

+     nodes.forEach((node) => observer.observe(node))

+ 

+     return () => {

+       nodes.forEach((node) => observer.unobserve(node))

+       observer.disconnect()

+     }

+   }, [location.pathname])

+ 

+   // Track if user has scrolled past the hero section (used for topbar styling)

+   useEffect(() => {

+     const onScroll = () => setIsScrolled(window.scrollY > 120)

+     onScroll()

+     window.addEventListener('scroll', onScroll, { passive: true })

+     return () => window.removeEventListener('scroll', onScroll)

+   }, [])

+ 

+   // All routes: home page with auth, and protected role dashboards

+ return (

+   <Routes>

+     <Route path="/" element={

+       <main className="foundry-clone">

+ 

+         <header className={`global-topbar ${isScrolled ? 'is-scrolled' : ''}`}>

+           <div className="topbar-main">

+             <div className="topbar-brand">SA LEARNERSHIP FOUNDRY</div>

+             <nav className="topbar-nav">

+               <a href="#">Why Portal</a>

+               <a href="#">Pathways</a>

+               <a href="#">Team</a>

+               <a href="#">Insights</a>

+               <a href="#">Contact</a>

+             </nav>

+           </div>

+ 

+           <div className="topbar-auth">

+             {signedIn && role ? <span className="role-display">{role}</span> : null}

+             <button className="google-auth" onClick={signedIn ? handleLogout : handleGoogleContinue}>

+               {isLoadingAuth ? 'Authenticating...' : signedIn ? 'Logout' : 'Log In with Google'}

+             </button>

+           </div>

+         </header>

+ 

+         <section className="page-one">

+           <div className="hero-main">

+             <h1 className="left-title">

+               BUILDING TALENT

+               <br />

+               PATHWAYS THAT

+             </h1>

+ 

+             <div

+               className="interactive-model"

+               onMouseMove={handleVisualMove}

+               onMouseLeave={resetVisualMove}

+               style={{ '--px': pointer.x, '--py': pointer.y }}

+             >

+               <div className="orbital-core">

+                 <span className="orbit orbit-a"></span>

+                 <span className="orbit orbit-b"></span>

+                 <span className="orbit orbit-c"></span>

+               </div>

+ 

+               <div className="dot-volume">

+                 {dots.map((dot) => (

+                   <span

+                     key={dot.id}

+                     className="dot"

+                     style={{

+                       '--x': `${dot.nx * 340}px`,

+                       '--y': `${dot.ny * 240}px`,

+                       '--z': `${dot.z}px`,

+                     }}

+                   />

+                 ))}

+               </div>

+             </div>

+ 

+             <h1 className="right-title">

+               PUSH THE

+               <br />

+               FUTURE FORWARD

+             </h1>

+           </div>

+ 

+           <div className="hero-bottom">

+             <p>

+               A venture lab style platform for learnership access, application intelligence,

+               and practical support that moves candidates from skill to real work.

+             </p>

+ 

+             <div className="status-pill">

+               {signedIn && role

+                 ? `${role} workspace unlocked`

+                 : signedIn && pendingEmail

+                   ? 'Choose your role to continue'

+                   : 'Sign in with Google to continue'}

+             </div>

+           </div>

+           {signedIn && !role && pendingEmail ? (

+             <div className="role-onboarding-overlay" role="dialog" aria-modal="true" aria-labelledby="role-onboarding-title">

+               <div className="role-onboarding">

+                 <p className="role-onboarding-eyebrow">One more step</p>

+                 <h2 id="role-onboarding-title">Choose your role</h2>

+                 <p>Select how you want to use the platform for {pendingEmail}.</p>

+                 <div className="role-select">

+                   <button onClick={() => handleRoleSelection('Applicant')} disabled={isSavingRole}>

+                     Applicant

+                   </button>

+                   <button onClick={() => handleRoleSelection('Provider')} disabled={isSavingRole}>

+                     Provider

+                   </button>

+                 </div>

+               </div>

+             </div>

+           ) : null}

+           {authError ? <p className="auth-error">{authError}</p> : null}

+         </section>

+ 

+         <section className="page-two">

+           <div className="ethos-row scroll-animate">

+             <div>

+               <p className="mini-label">Why this platform</p>

+               <h2>

+                 OPEN DOORS

+                 <br />

+                 TO REAL WORK

+               </h2>

+             </div>

+ 

+             <div className="ethos-copy">

+               <p>

+                 Learnership Foundry helps applicants, providers, and moderators collaborate in one

+                 flow. Discover pathways, publish opportunities, and keep quality high with

+                 visibility from intake to placement.

+               </p>

+             </div>

+           </div>

+ 

+           <div className="card-strip scroll-animate swipe-left">

+             <article className="signal-card tone-dark">

+               <h3>Discover</h3>

+               <p>APPLICANT VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Find learnerships that match your interests and readiness level.</small>

+             </article>

+ 

+             <article className="signal-card tone-cyan">

+               <h3>Publish</h3>

+               <p>PROVIDER VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Create listings and track engagement from qualified candidates.</small>

+             </article>

+ 

+             <article className="signal-card tone-orange">

+               <h3>Moderate</h3>

+               <p>ADMIN VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Flag risk, request fixes, and approve opportunities confidently.</small>

+             </article>

+ 

+             <article className="signal-card tone-light">

+               <h3>Move</h3>

+               <p>OUTCOMES</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Turn applications into skills, credentials, and workplace entry points.</small>

+             </article>

+           </div>

+         </section>

+ 

+         <section className="page-three">

+           <span className="dot-plane" aria-hidden="true"></span>

+           <div className="focus-copy scroll-animate">

+             <p className="mini-label">Built for momentum</p>

+             <h2>

+               From FIRST CLICK

+               <br />

+               TO FIRST PAYCHECK

+             </h2>

+             <p>

+               Scroll the platform, choose your role, and move into the right workspace to continue.

+             </p>

+           </div>

+         </section>

+ 

+       </main>

+     } />

+ 

+     

+    <Route path="/dashboard" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <Dashboard onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+ <Route path="/dashboard/listings/:listingId" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ApplicantListingDetail onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   <Route path="/profile" element={

+     <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+       <ApplicantProfile onLogout={handleLogout} />

+     </ProtectedRoute>

+   } />

+ 

+ <Route path="/provider" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <Provider onLogout={handleLogout} />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/profile" element={

+   <ProviderProfileRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ProviderProfile

+       onLogout={handleLogout}

+       onProfileSaved={() => setProviderLandingRoute('/provider')}

+     />

+   </ProviderProfileRoute>

+ } />

+ 

+ <Route path="/provider/listings/new" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingForm />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/edit" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingEdit />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/applications" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingApplications />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/admin" element={

+   <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <AdminDashboardShell onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   </Routes>

+ )

+ }

+ 

+ export default App

+

 Γ¥» src/pages/AdminPage.test.jsx:50:21
     48|   const appSource = readFileSync(resolve(cwd(), 'src/App.jsx'), 'utf8')
     49|
     50|   expect(appSource).toContain("if (role === 'Admin')")
       |                     ^
     51|   expect(appSource).toContain('handleRoleSelection')
     52| })

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[21/28]ΓÄ»

 FAIL  src/pages/AdminPage.test.jsx > ADMIN-ACCEPTANCE-3: Admin dashboard shell exists at /admin route with moderation controls
AssertionError: expected '// Main app: handles Google OAuth, roΓÇª' to contain 'if (role === \'Admin\') return \'/admΓÇª'

- Expected
+ Received

- if (role === 'Admin') return '/admin'
+ // Main app: handles Google OAuth, role-based routing, and admin dashboard

+ // TRICKY PARTS:

+ // - OAuth flow: Google login ΓåÆ email lookup ΓåÆ role selection (if new user)

+ // - Session persistence: browser cookies restored on page refresh

+ // - Route guards: ProtectedRoute blocks unauthorized access

+ // - State management: tracking auth, loading, roles, and pending signups

+ // - 3D animation: particle effect that reacts to mouse movement

+ 

+ import { useCallback, useEffect, useMemo, useRef, useState } from 'react'

+ import './App.css'

+ import { Routes, Route, useNavigate, useLocation, Navigate } from 'react-router-dom'

+ import Admin from './pages/Admin'

+ import Dashboard from './pages/Dashboard'

+ import ApplicantProfile from './pages/ApplicantProfile'

+ import ApplicantListingDetail from './pages/ApplicantListingDetail'

+ import Provider from './pages/Provider'

+ import ProviderProfile from './pages/ProviderProfile'

+ import ProviderListingForm from './pages/ProviderListingForm'

+ import ProviderListingEdit from './pages/ProviderListingEdit'

+ import ProviderListingApplications from './pages/ProviderListingApplications'

+ import { hasSupabaseConfig, supabase } from './lib/supabaseClient'

+ import {

+   getLandingRoute,

+   getRoleForEmail,

+   getApplicantLandingRouteForEmail,

+   getProviderLandingRouteForEmail,

+ } from './app-helpers'

+ const AdminDashboardShell = Admin

+ 

+ // ProtectedRoute: Guard component that restricts access to role-specific pages

+ // USAGE: <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}><Admin /></ProtectedRoute>

+ // LOGIC:

+ // 1. If still checking auth ΓåÆ show loading shell (don't flash wrong page)

+ // 2. If not signed in ΓåÆ redirect to home (must log in)

+ // 3. If wrong role ΓåÆ redirect to home (prevent unauthorized access)

+ // 4. All checks pass ΓåÆ render the protected content

+ function ProtectedRoute({ role, allowedRole, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   // Show loading state while session is being verified from browser cookies

+   // If we're still loading but already know the user is signed in, render

+   // the protected content so a refresh doesn't lock the user out temporarily.

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   // Unauthenticated users cannot access protected routes

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // Role mismatch: user is logged in but lacks permission for this page

+   // Example: Applicant (role="Applicant") trying to access /admin (allowedRole="Admin")

+   if (role !== allowedRole) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // All security checks passed: render the page

+   return children

+ }

+ 

+ function ProviderWorkspaceRoute({ role, signedIn, isLoading, providerLandingRoute, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (providerLandingRoute !== '/provider') {

+     return <Navigate to="/provider/profile" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function ProviderProfileRoute({ role, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function App() {

+   // Authentication state management:

+   const [role, setRole] = useState(null) // Current user's role: 'Admin', 'Provider', 'Applicant', or null

+   const [signedIn, setSignedIn] = useState(false) // True if OAuth session is active

+   const [isLoadingAuth, setIsLoadingAuth] = useState(true) // True while checking session from browser cookies

+   const [authError, setAuthError] = useState('') // Display auth errors to user

+   const [pendingEmail, setPendingEmail] = useState('') // New user email waiting for role selection

+   const [isSavingRole, setIsSavingRole] = useState(false) // True while inserting user into database

+   const [applicantLandingRoute, setApplicantLandingRoute] = useState('/dashboard')

+   const [providerLandingRoute, setProviderLandingRoute] = useState('/provider/profile')

+   const oauthTimeoutRef = useRef(null)

+ 

+   // Animation state for the 3D particle effect

+   const [pointer, setPointer] = useState({ x: 0, y: 0 })

+   const [isScrolled, setIsScrolled] = useState(false)

+   const navigate = useNavigate()

+   const location = useLocation()

+   const oauthRedirectTo =

+     import.meta.env.VITE_AUTH_REDIRECT_URL?.trim() || `${window.location.origin}/`

+ 

+   const clearOAuthTimeout = useCallback(() => {

+     if (oauthTimeoutRef.current) {

+       window.clearTimeout(oauthTimeoutRef.current)

+       oauthTimeoutRef.current = null

+     }

+   }, [])

+ 

+ // helper functions are implemented in ./app-helpers and imported above

+ 

+   // AUTH BOOTSTRAP: Restore user session on app load and listen for auth changes

+   // FLOW:

+   // 1. Check if session exists in browser cookies (user already logged in before)

+   // 2. Lookup user's role in database by email

+   // 3. Set up listener to detect sign-in/sign-out events

+   // 4. When sign-in happens, role lookup and optional role selection for new users

+   // WHY: Ensures app state matches Supabase auth session, prevents stale data on refresh

+   useEffect(() => {

+     let isMounted = true

+ 

+     const initializeAuth = async () => {

+       setIsLoadingAuth(true)

+ 

+       if (!hasSupabaseConfig) {

+         if (isMounted) {

+           setAuthError('Missing Supabase environment variables. Add VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const { data, error } = await supabase.auth.getSession()

+ 

+       if (error) {

+         if (isMounted) {

+           setAuthError('Unable to restore your session. Please sign in again.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const existingSession = data.session

+ 

+       if (!existingSession?.user?.email) {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(existingSession.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(existingSession.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(existingSession.user.email)

+             : getLandingRoute(resolvedRole)

+         if (isMounted) {

+           setSignedIn(true)

+           setRole(resolvedRole)

+           setPendingEmail(resolvedRole ? '' : existingSession.user.email)

+           setApplicantLandingRoute(resolvedLandingRoute)

+           setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+           setAuthError('')

+         }

+       } catch {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setAuthError('Signed in, but role lookup failed. Check your users table and policies.')

+         }

+       } finally {

+         if (isMounted) {

+           setIsLoadingAuth(false)

+         }

+       }

+     }

+ 

+     initializeAuth()

+ 

+     if (!hasSupabaseConfig) {

+       return () => {

+         isMounted = false

+       }

+     }

+ 

+     const {

+       data: { subscription },

+     } = supabase.auth.onAuthStateChange(async (event, session) => {

+       if (!isMounted) {

+         return

+       }

+ 

+       if (event === 'SIGNED_OUT' || !session?.user?.email) {

+         clearOAuthTimeout()

+         setSignedIn(false)

+         setRole(null)

+         setPendingEmail('')

+         setIsSavingRole(false)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setIsLoadingAuth(false)

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(session.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(session.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(session.user.email)

+             : getLandingRoute(resolvedRole)

+         if (!isMounted) {

+           return

+         }

+         clearOAuthTimeout()

+         setSignedIn(true)

+         setRole(resolvedRole)

+         setPendingEmail(resolvedRole ? '' : session.user.email)

+         setApplicantLandingRoute(resolvedLandingRoute)

+         setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+         setAuthError('')

+       } catch {

+         if (!isMounted) {

+           return

+         }

+         setSignedIn(false)

+         setRole(null)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setAuthError('OAuth succeeded, but role lookup failed. Please verify Supabase table policies.')

+       } finally {

+         if (isMounted) {

+           clearOAuthTimeout()

+           setIsLoadingAuth(false)

+         }

+       }

+     })

+ 

+     return () => {

+       clearOAuthTimeout()

+       isMounted = false

+       subscription.unsubscribe()

+     }

+   }, [clearOAuthTimeout])

+ 

+   // AUTO-REDIRECT: Send logged-in users to their role-appropriate dashboard

+   // WHEN: User has loaded auth state, is signed in, has a role, and is on home page

+   // REDIRECTS: Admin ΓåÆ /admin, Provider ΓåÆ /provider, Applicant ΓåÆ /dashboard

+   useEffect(() => {

+     const redirectedFromProtectedRoute = Boolean(location.state?.from)

+     const keepApplicantOnHome = redirectedFromProtectedRoute && role === 'Applicant'

+     const landingRoute =

+       role === 'Applicant'

+         ? applicantLandingRoute

+         : role === 'Provider'

+           ? providerLandingRoute

+           : getLandingRoute(role)

+ 

+     if (!isLoadingAuth && signedIn && role && location.pathname === '/' && !keepApplicantOnHome) {

+       navigate(landingRoute, { replace: true })

+     }

+   }, [isLoadingAuth, signedIn, role, applicantLandingRoute, providerLandingRoute, location.pathname, location.state, navigate])

+ 

+   // Clear session and reset all state when logging out

+   const handleLogout = async () => {

+     clearOAuthTimeout()

+ 

+     if (hasSupabaseConfig) {

+       await supabase.auth.signOut() // Clear session cookie

+     }

+     setSignedIn(false)

+     setRole(null)

+     setPendingEmail('')

+     setIsSavingRole(false)

+     setApplicantLandingRoute('/dashboard')

+     setProviderLandingRoute('/provider/profile')

+     navigate('/')

+   }

+ 

+   // NEW USER ROLE SELECTION: First-time users must choose their role

+   // WHY: roles (Admin/Provider/Applicant) determine which dashboard they access

+   // PROCESS:

+   // 1. User logs in with Google (new email, not in database yet)

+   // 2. We show role selection popup

+   // 3. User picks role ΓåÆ we insert into 'users' table with their email + role

+   // 4. Next login, step 2-3 are skipped (role already in database)

+   const handleRoleSelection = async (selectedRole) => {

+     if (!pendingEmail) {

+       return

+     }

+ 

+     setIsSavingRole(true)

+     setAuthError('')

+ 

+     const { data: insertedUser, error } = await supabase

+       .from('users')

+       .insert({ email: pendingEmail, role: selectedRole })

+       .select('role')

+       .single()

+ 

+     if (error) {

+       setIsSavingRole(false)

+       setAuthError('Could not save role selection. Please try again.')

+       return

+     }

+ 

+     const resolvedRole = insertedUser?.role || selectedRole

+     setRole(resolvedRole)

+     setPendingEmail('')

+     setIsSavingRole(false)

+ 

+     if (resolvedRole === 'Applicant') {

+       setApplicantLandingRoute('/profile')

+       navigate('/profile', { replace: true })

+       return

+     }

+ 

+     if (resolvedRole === 'Provider') {

+       setProviderLandingRoute('/provider/profile')

+       navigate('/provider/profile', { replace: true })

+       return

+     }

+ 

+     setApplicantLandingRoute(getLandingRoute(resolvedRole))

+     navigate(getLandingRoute(resolvedRole), { replace: true })

+   }

+ 

+   // Generate particle positions for 3D animation (748 dots in 34x22 grid)

+   // Complex math creates radial gradient effect that reacts to mouse movement

+   const dots = useMemo(() => {

+     const result = []

+     const columns = 34

+     const rows = 22

+ 

+     for (let row = 0; row < rows; row += 1) {

+       for (let col = 0; col < columns; col += 1) {

+         const index = row * columns + col

+         const nx = (col - (columns - 1) / 2) / ((columns - 1) / 2)

+         const ny = (row - (rows - 1) / 2) / ((rows - 1) / 2)

+         const r = Math.sqrt(nx * nx + ny * ny)

+         const z = (1 - Math.min(r, 1)) * 110 - Math.abs(nx) * 32

+         const dx = ((col % 5) - 2) * 1.6

+         const dy = ((row % 5) - 2) * 1.4

+         const dz = ((index % 7) - 3) * 2.2

+         const delay = `${(index % 11) * 0.18}s`

+         const duration = `${3.6 + (index % 6) * 0.6}s`

+         result.push({ nx, ny, z, id: `${row}-${col}`, dx, dy, dz, delay, duration })

+       }

+     }

+ 

+     return result

+   }, [])

+ 

+   // Track mouse position for 3D particle effect

+   const handleVisualMove = (event) => {

+     const rect = event.currentTarget.getBoundingClientRect()

+     const px = ((event.clientX - rect.left) / rect.width - 0.5) * 2

+     const py = ((event.clientY - rect.top) / rect.height - 0.5) * 2

+     setPointer({ x: px, y: py })

+   }

+ 

+   const resetVisualMove = () => setPointer({ x: 0, y: 0 })

+ 

+   // OAUTH ENTRY POINT: Initiate Google sign-in

+   // HOW: Calls Supabase.auth.signInWithOAuth() which opens Google login popup

+   // THEN: Auth listener (above) automatically detects successful login and looks up role

+   // NOTE: Don't manually handle role lookup hereΓÇölet the auth listener do it (keeps logic centralized)

+   const handleGoogleContinue = async () => {

+     setAuthError('')

+ 

+     if (!hasSupabaseConfig) {

+       setAuthError('Set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY before using Google login.')

+       return

+     }

+ 

+     setIsLoadingAuth(true)

+ 

+     clearOAuthTimeout()

+     oauthTimeoutRef.current = window.setTimeout(() => {

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in timed out. Please try again.')

+     }, 12000)

+ 

+     const { error } = await supabase.auth.signInWithOAuth({

+       provider: 'google',

+       options: {

+         redirectTo: oauthRedirectTo,

+       },

+     })

+ 

+     if (error) {

+       clearOAuthTimeout()

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in failed. Verify Supabase redirect URLs include your local host and VITE_AUTH_REDIRECT_URL value.')

+     }

+   }

+ 

+   // Fade in sections as they scroll into view

+   useEffect(() => {

+     if (location.pathname !== '/') {

+       return undefined

+     }

+ 

+     const nodes = document.querySelectorAll('.scroll-animate')

+ 

+     if (!nodes.length) {

+       return undefined

+     }

+ 

+     const observer = new IntersectionObserver(

+       (entries) => {

+         entries.forEach((entry) => {

+           if (entry.isIntersecting) {

+             entry.target.classList.add('is-visible') // Add CSS class to trigger fade-in

+           } else {

+             entry.target.classList.remove('is-visible') // Remove class when scrolled past

+           }

+         })

+       },

+       { threshold: 0.18 }, // Element must be 18% visible to trigger

+     )

+ 

+     nodes.forEach((node) => observer.observe(node))

+ 

+     return () => {

+       nodes.forEach((node) => observer.unobserve(node))

+       observer.disconnect()

+     }

+   }, [location.pathname])

+ 

+   // Track if user has scrolled past the hero section (used for topbar styling)

+   useEffect(() => {

+     const onScroll = () => setIsScrolled(window.scrollY > 120)

+     onScroll()

+     window.addEventListener('scroll', onScroll, { passive: true })

+     return () => window.removeEventListener('scroll', onScroll)

+   }, [])

+ 

+   // All routes: home page with auth, and protected role dashboards

+ return (

+   <Routes>

+     <Route path="/" element={

+       <main className="foundry-clone">

+ 

+         <header className={`global-topbar ${isScrolled ? 'is-scrolled' : ''}`}>

+           <div className="topbar-main">

+             <div className="topbar-brand">SA LEARNERSHIP FOUNDRY</div>

+             <nav className="topbar-nav">

+               <a href="#">Why Portal</a>

+               <a href="#">Pathways</a>

+               <a href="#">Team</a>

+               <a href="#">Insights</a>

+               <a href="#">Contact</a>

+             </nav>

+           </div>

+ 

+           <div className="topbar-auth">

+             {signedIn && role ? <span className="role-display">{role}</span> : null}

+             <button className="google-auth" onClick={signedIn ? handleLogout : handleGoogleContinue}>

+               {isLoadingAuth ? 'Authenticating...' : signedIn ? 'Logout' : 'Log In with Google'}

+             </button>

+           </div>

+         </header>

+ 

+         <section className="page-one">

+           <div className="hero-main">

+             <h1 className="left-title">

+               BUILDING TALENT

+               <br />

+               PATHWAYS THAT

+             </h1>

+ 

+             <div

+               className="interactive-model"

+               onMouseMove={handleVisualMove}

+               onMouseLeave={resetVisualMove}

+               style={{ '--px': pointer.x, '--py': pointer.y }}

+             >

+               <div className="orbital-core">

+                 <span className="orbit orbit-a"></span>

+                 <span className="orbit orbit-b"></span>

+                 <span className="orbit orbit-c"></span>

+               </div>

+ 

+               <div className="dot-volume">

+                 {dots.map((dot) => (

+                   <span

+                     key={dot.id}

+                     className="dot"

+                     style={{

+                       '--x': `${dot.nx * 340}px`,

+                       '--y': `${dot.ny * 240}px`,

+                       '--z': `${dot.z}px`,

+                     }}

+                   />

+                 ))}

+               </div>

+             </div>

+ 

+             <h1 className="right-title">

+               PUSH THE

+               <br />

+               FUTURE FORWARD

+             </h1>

+           </div>

+ 

+           <div className="hero-bottom">

+             <p>

+               A venture lab style platform for learnership access, application intelligence,

+               and practical support that moves candidates from skill to real work.

+             </p>

+ 

+             <div className="status-pill">

+               {signedIn && role

+                 ? `${role} workspace unlocked`

+                 : signedIn && pendingEmail

+                   ? 'Choose your role to continue'

+                   : 'Sign in with Google to continue'}

+             </div>

+           </div>

+           {signedIn && !role && pendingEmail ? (

+             <div className="role-onboarding-overlay" role="dialog" aria-modal="true" aria-labelledby="role-onboarding-title">

+               <div className="role-onboarding">

+                 <p className="role-onboarding-eyebrow">One more step</p>

+                 <h2 id="role-onboarding-title">Choose your role</h2>

+                 <p>Select how you want to use the platform for {pendingEmail}.</p>

+                 <div className="role-select">

+                   <button onClick={() => handleRoleSelection('Applicant')} disabled={isSavingRole}>

+                     Applicant

+                   </button>

+                   <button onClick={() => handleRoleSelection('Provider')} disabled={isSavingRole}>

+                     Provider

+                   </button>

+                 </div>

+               </div>

+             </div>

+           ) : null}

+           {authError ? <p className="auth-error">{authError}</p> : null}

+         </section>

+ 

+         <section className="page-two">

+           <div className="ethos-row scroll-animate">

+             <div>

+               <p className="mini-label">Why this platform</p>

+               <h2>

+                 OPEN DOORS

+                 <br />

+                 TO REAL WORK

+               </h2>

+             </div>

+ 

+             <div className="ethos-copy">

+               <p>

+                 Learnership Foundry helps applicants, providers, and moderators collaborate in one

+                 flow. Discover pathways, publish opportunities, and keep quality high with

+                 visibility from intake to placement.

+               </p>

+             </div>

+           </div>

+ 

+           <div className="card-strip scroll-animate swipe-left">

+             <article className="signal-card tone-dark">

+               <h3>Discover</h3>

+               <p>APPLICANT VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Find learnerships that match your interests and readiness level.</small>

+             </article>

+ 

+             <article className="signal-card tone-cyan">

+               <h3>Publish</h3>

+               <p>PROVIDER VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Create listings and track engagement from qualified candidates.</small>

+             </article>

+ 

+             <article className="signal-card tone-orange">

+               <h3>Moderate</h3>

+               <p>ADMIN VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Flag risk, request fixes, and approve opportunities confidently.</small>

+             </article>

+ 

+             <article className="signal-card tone-light">

+               <h3>Move</h3>

+               <p>OUTCOMES</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Turn applications into skills, credentials, and workplace entry points.</small>

+             </article>

+           </div>

+         </section>

+ 

+         <section className="page-three">

+           <span className="dot-plane" aria-hidden="true"></span>

+           <div className="focus-copy scroll-animate">

+             <p className="mini-label">Built for momentum</p>

+             <h2>

+               From FIRST CLICK

+               <br />

+               TO FIRST PAYCHECK

+             </h2>

+             <p>

+               Scroll the platform, choose your role, and move into the right workspace to continue.

+             </p>

+           </div>

+         </section>

+ 

+       </main>

+     } />

+ 

+     

+    <Route path="/dashboard" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <Dashboard onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+ <Route path="/dashboard/listings/:listingId" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ApplicantListingDetail onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   <Route path="/profile" element={

+     <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+       <ApplicantProfile onLogout={handleLogout} />

+     </ProtectedRoute>

+   } />

+ 

+ <Route path="/provider" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <Provider onLogout={handleLogout} />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/profile" element={

+   <ProviderProfileRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ProviderProfile

+       onLogout={handleLogout}

+       onProfileSaved={() => setProviderLandingRoute('/provider')}

+     />

+   </ProviderProfileRoute>

+ } />

+ 

+ <Route path="/provider/listings/new" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingForm />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/edit" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingEdit />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/applications" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingApplications />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/admin" element={

+   <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <AdminDashboardShell onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   </Routes>

+ )

+ }

+ 

+ export default App

+

 Γ¥» src/pages/AdminPage.test.jsx:58:21
     56|
     57|   expect(appSource).toContain('<Route path="/admin"')
     58|   expect(appSource).toContain('if (role === \'Admin\') return \'/adminΓÇª
       |                     ^
     59| })
     60|

ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[22/28]ΓÄ»

 FAIL  src/pages/ProviderPage.test.jsx > PROVIDER-ACCEPTANCE-4: Successful Provider login landing route is /provider
AssertionError: expected '// Main app: handles Google OAuth, roΓÇª' to contain 'if (role === \'Provider\') return \'/ΓÇª'

- Expected
+ Received

- if (role === 'Provider') return '/provider'
+ // Main app: handles Google OAuth, role-based routing, and admin dashboard

+ // TRICKY PARTS:

+ // - OAuth flow: Google login ΓåÆ email lookup ΓåÆ role selection (if new user)

+ // - Session persistence: browser cookies restored on page refresh

+ // - Route guards: ProtectedRoute blocks unauthorized access

+ // - State management: tracking auth, loading, roles, and pending signups

+ // - 3D animation: particle effect that reacts to mouse movement

+ 

+ import { useCallback, useEffect, useMemo, useRef, useState } from 'react'

+ import './App.css'

+ import { Routes, Route, useNavigate, useLocation, Navigate } from 'react-router-dom'

+ import Admin from './pages/Admin'

+ import Dashboard from './pages/Dashboard'

+ import ApplicantProfile from './pages/ApplicantProfile'

+ import ApplicantListingDetail from './pages/ApplicantListingDetail'

+ import Provider from './pages/Provider'

+ import ProviderProfile from './pages/ProviderProfile'

+ import ProviderListingForm from './pages/ProviderListingForm'

+ import ProviderListingEdit from './pages/ProviderListingEdit'

+ import ProviderListingApplications from './pages/ProviderListingApplications'

+ import { hasSupabaseConfig, supabase } from './lib/supabaseClient'

+ import {

+   getLandingRoute,

+   getRoleForEmail,

+   getApplicantLandingRouteForEmail,

+   getProviderLandingRouteForEmail,

+ } from './app-helpers'

+ const AdminDashboardShell = Admin

+ 

+ // ProtectedRoute: Guard component that restricts access to role-specific pages

+ // USAGE: <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}><Admin /></ProtectedRoute>

+ // LOGIC:

+ // 1. If still checking auth ΓåÆ show loading shell (don't flash wrong page)

+ // 2. If not signed in ΓåÆ redirect to home (must log in)

+ // 3. If wrong role ΓåÆ redirect to home (prevent unauthorized access)

+ // 4. All checks pass ΓåÆ render the protected content

+ function ProtectedRoute({ role, allowedRole, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   // Show loading state while session is being verified from browser cookies

+   // If we're still loading but already know the user is signed in, render

+   // the protected content so a refresh doesn't lock the user out temporarily.

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   // Unauthenticated users cannot access protected routes

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // Role mismatch: user is logged in but lacks permission for this page

+   // Example: Applicant (role="Applicant") trying to access /admin (allowedRole="Admin")

+   if (role !== allowedRole) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   // All security checks passed: render the page

+   return children

+ }

+ 

+ function ProviderWorkspaceRoute({ role, signedIn, isLoading, providerLandingRoute, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (providerLandingRoute !== '/provider') {

+     return <Navigate to="/provider/profile" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function ProviderProfileRoute({ role, signedIn, isLoading, children }) {

+   const location = useLocation()

+ 

+   if (isLoading && !signedIn) {

+     return (

+       <main className="auth-loading-shell" aria-busy="true" aria-live="polite">

+         <p>Checking your session...</p>

+       </main>

+     )

+   }

+ 

+   if (!signedIn) {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   if (role !== 'Provider') {

+     return <Navigate to="/" replace state={{ from: location }} />

+   }

+ 

+   return children

+ }

+ 

+ function App() {

+   // Authentication state management:

+   const [role, setRole] = useState(null) // Current user's role: 'Admin', 'Provider', 'Applicant', or null

+   const [signedIn, setSignedIn] = useState(false) // True if OAuth session is active

+   const [isLoadingAuth, setIsLoadingAuth] = useState(true) // True while checking session from browser cookies

+   const [authError, setAuthError] = useState('') // Display auth errors to user

+   const [pendingEmail, setPendingEmail] = useState('') // New user email waiting for role selection

+   const [isSavingRole, setIsSavingRole] = useState(false) // True while inserting user into database

+   const [applicantLandingRoute, setApplicantLandingRoute] = useState('/dashboard')

+   const [providerLandingRoute, setProviderLandingRoute] = useState('/provider/profile')

+   const oauthTimeoutRef = useRef(null)

+ 

+   // Animation state for the 3D particle effect

+   const [pointer, setPointer] = useState({ x: 0, y: 0 })

+   const [isScrolled, setIsScrolled] = useState(false)

+   const navigate = useNavigate()

+   const location = useLocation()

+   const oauthRedirectTo =

+     import.meta.env.VITE_AUTH_REDIRECT_URL?.trim() || `${window.location.origin}/`

+ 

+   const clearOAuthTimeout = useCallback(() => {

+     if (oauthTimeoutRef.current) {

+       window.clearTimeout(oauthTimeoutRef.current)

+       oauthTimeoutRef.current = null

+     }

+   }, [])

+ 

+ // helper functions are implemented in ./app-helpers and imported above

+ 

+   // AUTH BOOTSTRAP: Restore user session on app load and listen for auth changes

+   // FLOW:

+   // 1. Check if session exists in browser cookies (user already logged in before)

+   // 2. Lookup user's role in database by email

+   // 3. Set up listener to detect sign-in/sign-out events

+   // 4. When sign-in happens, role lookup and optional role selection for new users

+   // WHY: Ensures app state matches Supabase auth session, prevents stale data on refresh

+   useEffect(() => {

+     let isMounted = true

+ 

+     const initializeAuth = async () => {

+       setIsLoadingAuth(true)

+ 

+       if (!hasSupabaseConfig) {

+         if (isMounted) {

+           setAuthError('Missing Supabase environment variables. Add VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const { data, error } = await supabase.auth.getSession()

+ 

+       if (error) {

+         if (isMounted) {

+           setAuthError('Unable to restore your session. Please sign in again.')

+           setSignedIn(false)

+           setRole(null)

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       const existingSession = data.session

+ 

+       if (!existingSession?.user?.email) {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setIsLoadingAuth(false)

+         }

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(existingSession.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(existingSession.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(existingSession.user.email)

+             : getLandingRoute(resolvedRole)

+         if (isMounted) {

+           setSignedIn(true)

+           setRole(resolvedRole)

+           setPendingEmail(resolvedRole ? '' : existingSession.user.email)

+           setApplicantLandingRoute(resolvedLandingRoute)

+           setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+           setAuthError('')

+         }

+       } catch {

+         if (isMounted) {

+           setSignedIn(false)

+           setRole(null)

+           setApplicantLandingRoute('/dashboard')

+           setProviderLandingRoute('/provider/profile')

+           setAuthError('Signed in, but role lookup failed. Check your users table and policies.')

+         }

+       } finally {

+         if (isMounted) {

+           setIsLoadingAuth(false)

+         }

+       }

+     }

+ 

+     initializeAuth()

+ 

+     if (!hasSupabaseConfig) {

+       return () => {

+         isMounted = false

+       }

+     }

+ 

+     const {

+       data: { subscription },

+     } = supabase.auth.onAuthStateChange(async (event, session) => {

+       if (!isMounted) {

+         return

+       }

+ 

+       if (event === 'SIGNED_OUT' || !session?.user?.email) {

+         clearOAuthTimeout()

+         setSignedIn(false)

+         setRole(null)

+         setPendingEmail('')

+         setIsSavingRole(false)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setIsLoadingAuth(false)

+         return

+       }

+ 

+       try {

+         const resolvedRole = await getRoleForEmail(session.user.email)

+         const resolvedLandingRoute =

+           resolvedRole === 'Applicant'

+             ? await getApplicantLandingRouteForEmail(session.user.email)

+             : resolvedRole === 'Provider'

+               ? await getProviderLandingRouteForEmail(session.user.email)

+             : getLandingRoute(resolvedRole)

+         if (!isMounted) {

+           return

+         }

+         clearOAuthTimeout()

+         setSignedIn(true)

+         setRole(resolvedRole)

+         setPendingEmail(resolvedRole ? '' : session.user.email)

+         setApplicantLandingRoute(resolvedLandingRoute)

+         setProviderLandingRoute(resolvedRole === 'Provider' ? resolvedLandingRoute : '/provider/profile')

+         setAuthError('')

+       } catch {

+         if (!isMounted) {

+           return

+         }

+         setSignedIn(false)

+         setRole(null)

+         setApplicantLandingRoute('/dashboard')

+         setProviderLandingRoute('/provider/profile')

+         setAuthError('OAuth succeeded, but role lookup failed. Please verify Supabase table policies.')

+       } finally {

+         if (isMounted) {

+           clearOAuthTimeout()

+           setIsLoadingAuth(false)

+         }

+       }

+     })

+ 

+     return () => {

+       clearOAuthTimeout()

+       isMounted = false

+       subscription.unsubscribe()

+     }

+   }, [clearOAuthTimeout])

+ 

+   // AUTO-REDIRECT: Send logged-in users to their role-appropriate dashboard

+   // WHEN: User has loaded auth state, is signed in, has a role, and is on home page

+   // REDIRECTS: Admin ΓåÆ /admin, Provider ΓåÆ /provider, Applicant ΓåÆ /dashboard

+   useEffect(() => {

+     const redirectedFromProtectedRoute = Boolean(location.state?.from)

+     const keepApplicantOnHome = redirectedFromProtectedRoute && role === 'Applicant'

+     const landingRoute =

+       role === 'Applicant'

+         ? applicantLandingRoute

+         : role === 'Provider'

+           ? providerLandingRoute

+           : getLandingRoute(role)

+ 

+     if (!isLoadingAuth && signedIn && role && location.pathname === '/' && !keepApplicantOnHome) {

+       navigate(landingRoute, { replace: true })

+     }

+   }, [isLoadingAuth, signedIn, role, applicantLandingRoute, providerLandingRoute, location.pathname, location.state, navigate])

+ 

+   // Clear session and reset all state when logging out

+   const handleLogout = async () => {

+     clearOAuthTimeout()

+ 

+     if (hasSupabaseConfig) {

+       await supabase.auth.signOut() // Clear session cookie

+     }

+     setSignedIn(false)

+     setRole(null)

+     setPendingEmail('')

+     setIsSavingRole(false)

+     setApplicantLandingRoute('/dashboard')

+     setProviderLandingRoute('/provider/profile')

+     navigate('/')

+   }

+ 

+   // NEW USER ROLE SELECTION: First-time users must choose their role

+   // WHY: roles (Admin/Provider/Applicant) determine which dashboard they access

+   // PROCESS:

+   // 1. User logs in with Google (new email, not in database yet)

+   // 2. We show role selection popup

+   // 3. User picks role ΓåÆ we insert into 'users' table with their email + role

+   // 4. Next login, step 2-3 are skipped (role already in database)

+   const handleRoleSelection = async (selectedRole) => {

+     if (!pendingEmail) {

+       return

+     }

+ 

+     setIsSavingRole(true)

+     setAuthError('')

+ 

+     const { data: insertedUser, error } = await supabase

+       .from('users')

+       .insert({ email: pendingEmail, role: selectedRole })

+       .select('role')

+       .single()

+ 

+     if (error) {

+       setIsSavingRole(false)

+       setAuthError('Could not save role selection. Please try again.')

+       return

+     }

+ 

+     const resolvedRole = insertedUser?.role || selectedRole

+     setRole(resolvedRole)

+     setPendingEmail('')

+     setIsSavingRole(false)

+ 

+     if (resolvedRole === 'Applicant') {

+       setApplicantLandingRoute('/profile')

+       navigate('/profile', { replace: true })

+       return

+     }

+ 

+     if (resolvedRole === 'Provider') {

+       setProviderLandingRoute('/provider/profile')

+       navigate('/provider/profile', { replace: true })

+       return

+     }

+ 

+     setApplicantLandingRoute(getLandingRoute(resolvedRole))

+     navigate(getLandingRoute(resolvedRole), { replace: true })

+   }

+ 

+   // Generate particle positions for 3D animation (748 dots in 34x22 grid)

+   // Complex math creates radial gradient effect that reacts to mouse movement

+   const dots = useMemo(() => {

+     const result = []

+     const columns = 34

+     const rows = 22

+ 

+     for (let row = 0; row < rows; row += 1) {

+       for (let col = 0; col < columns; col += 1) {

+         const index = row * columns + col

+         const nx = (col - (columns - 1) / 2) / ((columns - 1) / 2)

+         const ny = (row - (rows - 1) / 2) / ((rows - 1) / 2)

+         const r = Math.sqrt(nx * nx + ny * ny)

+         const z = (1 - Math.min(r, 1)) * 110 - Math.abs(nx) * 32

+         const dx = ((col % 5) - 2) * 1.6

+         const dy = ((row % 5) - 2) * 1.4

+         const dz = ((index % 7) - 3) * 2.2

+         const delay = `${(index % 11) * 0.18}s`

+         const duration = `${3.6 + (index % 6) * 0.6}s`

+         result.push({ nx, ny, z, id: `${row}-${col}`, dx, dy, dz, delay, duration })

+       }

+     }

+ 

+     return result

+   }, [])

+ 

+   // Track mouse position for 3D particle effect

+   const handleVisualMove = (event) => {

+     const rect = event.currentTarget.getBoundingClientRect()

+     const px = ((event.clientX - rect.left) / rect.width - 0.5) * 2

+     const py = ((event.clientY - rect.top) / rect.height - 0.5) * 2

+     setPointer({ x: px, y: py })

+   }

+ 

+   const resetVisualMove = () => setPointer({ x: 0, y: 0 })

+ 

+   // OAUTH ENTRY POINT: Initiate Google sign-in

+   // HOW: Calls Supabase.auth.signInWithOAuth() which opens Google login popup

+   // THEN: Auth listener (above) automatically detects successful login and looks up role

+   // NOTE: Don't manually handle role lookup hereΓÇölet the auth listener do it (keeps logic centralized)

+   const handleGoogleContinue = async () => {

+     setAuthError('')

+ 

+     if (!hasSupabaseConfig) {

+       setAuthError('Set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY before using Google login.')

+       return

+     }

+ 

+     setIsLoadingAuth(true)

+ 

+     clearOAuthTimeout()

+     oauthTimeoutRef.current = window.setTimeout(() => {

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in timed out. Please try again.')

+     }, 12000)

+ 

+     const { error } = await supabase.auth.signInWithOAuth({

+       provider: 'google',

+       options: {

+         redirectTo: oauthRedirectTo,

+       },

+     })

+ 

+     if (error) {

+       clearOAuthTimeout()

+       setIsLoadingAuth(false)

+       setAuthError('Google sign-in failed. Verify Supabase redirect URLs include your local host and VITE_AUTH_REDIRECT_URL value.')

+     }

+   }

+ 

+   // Fade in sections as they scroll into view

+   useEffect(() => {

+     if (location.pathname !== '/') {

+       return undefined

+     }

+ 

+     const nodes = document.querySelectorAll('.scroll-animate')

+ 

+     if (!nodes.length) {

+       return undefined

+     }

+ 

+     const observer = new IntersectionObserver(

+       (entries) => {

+         entries.forEach((entry) => {

+           if (entry.isIntersecting) {

+             entry.target.classList.add('is-visible') // Add CSS class to trigger fade-in

+           } else {

+             entry.target.classList.remove('is-visible') // Remove class when scrolled past

+           }

+         })

+       },

+       { threshold: 0.18 }, // Element must be 18% visible to trigger

+     )

+ 

+     nodes.forEach((node) => observer.observe(node))

+ 

+     return () => {

+       nodes.forEach((node) => observer.unobserve(node))

+       observer.disconnect()

+     }

+   }, [location.pathname])

+ 

+   // Track if user has scrolled past the hero section (used for topbar styling)

+   useEffect(() => {

+     const onScroll = () => setIsScrolled(window.scrollY > 120)

+     onScroll()

+     window.addEventListener('scroll', onScroll, { passive: true })

+     return () => window.removeEventListener('scroll', onScroll)

+   }, [])

+ 

+   // All routes: home page with auth, and protected role dashboards

+ return (

+   <Routes>

+     <Route path="/" element={

+       <main className="foundry-clone">

+ 

+         <header className={`global-topbar ${isScrolled ? 'is-scrolled' : ''}`}>

+           <div className="topbar-main">

+             <div className="topbar-brand">SA LEARNERSHIP FOUNDRY</div>

+             <nav className="topbar-nav">

+               <a href="#">Why Portal</a>

+               <a href="#">Pathways</a>

+               <a href="#">Team</a>

+               <a href="#">Insights</a>

+               <a href="#">Contact</a>

+             </nav>

+           </div>

+ 

+           <div className="topbar-auth">

+             {signedIn && role ? <span className="role-display">{role}</span> : null}

+             <button className="google-auth" onClick={signedIn ? handleLogout : handleGoogleContinue}>

+               {isLoadingAuth ? 'Authenticating...' : signedIn ? 'Logout' : 'Log In with Google'}

+             </button>

+           </div>

+         </header>

+ 

+         <section className="page-one">

+           <div className="hero-main">

+             <h1 className="left-title">

+               BUILDING TALENT

+               <br />

+               PATHWAYS THAT

+             </h1>

+ 

+             <div

+               className="interactive-model"

+               onMouseMove={handleVisualMove}

+               onMouseLeave={resetVisualMove}

+               style={{ '--px': pointer.x, '--py': pointer.y }}

+             >

+               <div className="orbital-core">

+                 <span className="orbit orbit-a"></span>

+                 <span className="orbit orbit-b"></span>

+                 <span className="orbit orbit-c"></span>

+               </div>

+ 

+               <div className="dot-volume">

+                 {dots.map((dot) => (

+                   <span

+                     key={dot.id}

+                     className="dot"

+                     style={{

+                       '--x': `${dot.nx * 340}px`,

+                       '--y': `${dot.ny * 240}px`,

+                       '--z': `${dot.z}px`,

+                     }}

+                   />

+                 ))}

+               </div>

+             </div>

+ 

+             <h1 className="right-title">

+               PUSH THE

+               <br />

+               FUTURE FORWARD

+             </h1>

+           </div>

+ 

+           <div className="hero-bottom">

+             <p>

+               A venture lab style platform for learnership access, application intelligence,

+               and practical support that moves candidates from skill to real work.

+             </p>

+ 

+             <div className="status-pill">

+               {signedIn && role

+                 ? `${role} workspace unlocked`

+                 : signedIn && pendingEmail

+                   ? 'Choose your role to continue'

+                   : 'Sign in with Google to continue'}

+             </div>

+           </div>

+           {signedIn && !role && pendingEmail ? (

+             <div className="role-onboarding-overlay" role="dialog" aria-modal="true" aria-labelledby="role-onboarding-title">

+               <div className="role-onboarding">

+                 <p className="role-onboarding-eyebrow">One more step</p>

+                 <h2 id="role-onboarding-title">Choose your role</h2>

+                 <p>Select how you want to use the platform for {pendingEmail}.</p>

+                 <div className="role-select">

+                   <button onClick={() => handleRoleSelection('Applicant')} disabled={isSavingRole}>

+                     Applicant

+                   </button>

+                   <button onClick={() => handleRoleSelection('Provider')} disabled={isSavingRole}>

+                     Provider

+                   </button>

+                 </div>

+               </div>

+             </div>

+           ) : null}

+           {authError ? <p className="auth-error">{authError}</p> : null}

+         </section>

+ 

+         <section className="page-two">

+           <div className="ethos-row scroll-animate">

+             <div>

+               <p className="mini-label">Why this platform</p>

+               <h2>

+                 OPEN DOORS

+                 <br />

+                 TO REAL WORK

+               </h2>

+             </div>

+ 

+             <div className="ethos-copy">

+               <p>

+                 Learnership Foundry helps applicants, providers, and moderators collaborate in one

+                 flow. Discover pathways, publish opportunities, and keep quality high with

+                 visibility from intake to placement.

+               </p>

+             </div>

+           </div>

+ 

+           <div className="card-strip scroll-animate swipe-left">

+             <article className="signal-card tone-dark">

+               <h3>Discover</h3>

+               <p>APPLICANT VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Find learnerships that match your interests and readiness level.</small>

+             </article>

+ 

+             <article className="signal-card tone-cyan">

+               <h3>Publish</h3>

+               <p>PROVIDER VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Create listings and track engagement from qualified candidates.</small>

+             </article>

+ 

+             <article className="signal-card tone-orange">

+               <h3>Moderate</h3>

+               <p>ADMIN VIEW</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Flag risk, request fixes, and approve opportunities confidently.</small>

+             </article>

+ 

+             <article className="signal-card tone-light">

+               <h3>Move</h3>

+               <p>OUTCOMES</p>

+               <span className="card-art" aria-hidden="true"></span>

+               <small>Turn applications into skills, credentials, and workplace entry points.</small>

+             </article>

+           </div>

+         </section>

+ 

+         <section className="page-three">

+           <span className="dot-plane" aria-hidden="true"></span>

+           <div className="focus-copy scroll-animate">

+             <p className="mini-label">Built for momentum</p>

+             <h2>

+               From FIRST CLICK

+               <br />

+               TO FIRST PAYCHECK

+             </h2>

+             <p>

+               Scroll the platform, choose your role, and move into the right workspace to continue.

+             </p>

+           </div>

+         </section>

+ 

+       </main>

+     } />

+ 

+     

+    <Route path="/dashboard" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <Dashboard onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+ <Route path="/dashboard/listings/:listingId" element={

+   <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ApplicantListingDetail onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   <Route path="/profile" element={

+     <ProtectedRoute role={role} allowedRole="Applicant" signedIn={signedIn} isLoading={isLoadingAuth}>

+       <ApplicantProfile onLogout={handleLogout} />

+     </ProtectedRoute>

+   } />

+ 

+ <Route path="/provider" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <Provider onLogout={handleLogout} />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/profile" element={

+   <ProviderProfileRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth}>

+     <ProviderProfile

+       onLogout={handleLogout}

+       onProfileSaved={() => setProviderLandingRoute('/provider')}

+     />

+   </ProviderProfileRoute>

+ } />

+ 

+ <Route path="/provider/listings/new" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingForm />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/edit" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingEdit />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/provider/listings/:listingId/applications" element={

+   <ProviderWorkspaceRoute role={role} signedIn={signedIn} isLoading={isLoadingAuth} providerLandingRoute={providerLandingRoute}>

+     <ProviderListingApplications />

+   </ProviderWorkspaceRoute>

+ } />

+ 

+ <Route path="/admin" element={

+   <ProtectedRoute role={role} allowedRole="Admin" signedIn={signedIn} isLoading={isLoadingAuth}>

+     <AdminDashboardShell onLogout={handleLogout} />

+   </ProtectedRoute>

+ } />

+ 

+   </Routes>

+ )

+ }

+ 

+ export default App

+

 Γ¥» src/pages/ProviderPage.test.jsx:86:21
     84|   const appSource = readFileSync(resolve(cwd(), 'src/App.jsx'), 'utf8')
     85|
     86|   expect(appSource).toContain("if (role === 'Provider') return '/proviΓÇª
       |                     ^
     87|   expect(appSource).toContain('<Route path="/provider"')
     88| })
ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»ΓÄ»[23/28]ΓÄ»


[2m Test Files [22m [1m[31m7 failed[39m[22m[2m | [22m[1m[32m33 passed[39m[22m[90m (40)[39m
[2m      Tests [22m [1m[31m28 failed[39m[22m[2m | [22m[1m[32m307 passed[39m[22m[2m | [22m[33m7 skipped[39m[2m | [22m[90m12 todo[39m[90m (354)[39m
[2m   Start at [22m 11:43:42
[2m   Duration [22m 38.71s[2m (transform 4.07s, setup 897ms, import 30.74s, tests 65.19s, environment 128.08s)[22m

